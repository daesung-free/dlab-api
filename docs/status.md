# 구현 현황

`scripts/status-report.py` 가 코드에서 뽑는다. **직접 고치지 말 것** — 다음 실행에 덮어써진다.
사유·블로커는 `docs/status-notes.md` 에 적으면 마지막 열에 붙는다.

기준 커밋 `8f46e4f` · 마이그레이션 72개 · 앱 API 79개 · 관리자 API 317개

| 도메인 | 테이블 | 서비스 | 앱 API | 관리자 API | 테스트 | 비고 |
|---|---:|---:|---:|---:|---:|---|
| `admission` | 4 | 2 | — | — | 1 | 홈페이지 입학예약 수신 전용. 앱 API 가 없는 것이 정상이다. |
| `appconfig` | 5 | 3 | 7 | 6 | 2 | 온라인 질의응답 메뉴 숨김 플래그가 없다 — 0908 회신에 「숨김 제공」으로 나갔다. |
| `approval` | 3 | 4 | 5 | 8 | 12 |  |
| `attendance` | 6 | 13 | 6 | 12 | 86 | 병원영수증 첨부 후 벌점 취소가 **파일 업로드 기능이 없어** 막혀 있다(0908 반영 예정). |
| `audit` | 0 | 0 | — | 2 | — | 엔티티가 `domain/audit/AuditLog` 한 개라 테이블 수집기에 안 잡힌다. 없는 게 아니다. |
| `chat` | 0 | 0 | — | — | — | **미착수.** SDK 미정이라 벤더 패키지를 못 만든다. `integration/sendbird` 는 빈 폴더다. |
| `consult` | 5 | 2 | 4 | 15 | 2 | 슬롯이 날짜 단위라 **요일별 반복 등록이 없다** — 매주 같은 시간대를 날짜마다 열어야 한다. |
| `facility` | 3 | 4 | — | 15 | 7 | 앱 API 가 없는 것은 좌석 이탈 앱 연동이 **2차 이관**(I-16)됐기 때문이다. |
| `firewall` | 3 | 3 | 1 | 5 | 1 | `integration/zyxel` 에 골격만 있다. **실제 장비 연동은 자격증명(E-1) 미수령**으로 대기. |
| `grade` | 4 | 3 | 5 | 6 | 3 | 엑셀 업로드 파서 **미착수**. 평가원 파일은 있고 **더프 회차 파일 실물 대기**. 2단 헤더라 `common/excel` 의 `ColumnMapping` 을 그대로 못 쓴다. |
| `holiday` | 1 | 2 | — | 4 | 2 |  |
| `kiosk` | 3 | 11 | — | — | 9 | DSA 호환 25개 계약. 앱·관리자 API 가 없는 것이 정상이다. |
| `lecture` | 5 | 1 | 4 | 17 | 2 | 수강료가 **안내용**이라 결제가 생성되지 않는다(PG 규격 대기). 유형 마스터 작업 중. 홍보용 PDF·사진은 **파일 업로드 없어** 막혀 있다. |
| `master` | 9 | 3 | — | 49 | 6 |  |
| `meal` | 6 | 7 | 4 | 16 | 9 | 급식업체 **조회용 계정 미착수**(0908 반영 예정, 사용자 유형 신설). 환불 금액 계산까지만 — **실제 PG 취소 없음**. |
| `notice` | 2 | 1 | 4 | 8 | 3 |  |
| `notification` | 2 | 4 | — | 10 | 4 | 템플릿·수신설정·토큰 등록까지 됐고 **실제 발송이 없다** — FCM/APNs(E-7)·알림톡 심사(E-5) 대기. 지금은 로그로만 남는다. |
| `payment` | 7 | 9 | — | 23 | 11 | 청구·환불 계산까지. **PG 연동 없음** — MID·연동규격·테스트 가맹점 미수령. 그래서 가상계좌 발급·입금 만료 처리가 돌지 않는다. |
| `penalty` | 3 | 3 | — | 14 | 7 | 규칙 엔진은 돌지만 **`penalty_rule.active` 가 전부 false** — 트리거→점수 매핑(I-5) 미확정. |
| `period` | 1 | 1 | — | 4 | 9 |  |
| `plan` | 3 | 2 | 7 | 7 | 2 | 중단일에 **입력이 막히지 않는다.** 미작성 집계에서만 빠진다(0908 결정: 막기로 함). |
| `qna` | 2 | 1 | 4 | 5 | — | **오프라인 질의응답만** 있다. 온라인 미착수. 슬롯에 **과목 축이 없어** 과목별 일정을 못 만든다. |
| `report` | 2 | 3 | 5 | — | 1 | 집계 전용. 관리자 API 가 없는 것이 정상이다. |
| `routine` | 2 | 1 | 2 | 9 | 3 | **회차 축이 없다.** 항목(이름·과목)까지만 등록한다 — 월간 표 반영 시 추가. |
| `schedule` | 2 | 2 | 5 | 5 | 1 |  |
| `scholarship` | 2 | 2 | — | 9 | 2 | 판정 로직은 있으나 **취소 규칙이 비활성** — 수능 기준 장학의 판정 시점 미확정. |
| `search` | 1 | 1 | — | — | — | 공통 검색 모듈. 도메인이 아니라 `SearchScope`·`SearchPredicates` 다. |
| `statistics` | 0 | 1 | — | 3 | 1 | 집계 전용이라 테이블이 없다. |
| `survey` | 6 | 1 | 4 | 8 | 1 |  |
| `user` | 11 | 17 | 12 | 57 | 87 | 회원가입·인증·계정 관리. **인증번호 발송이 목업**이라 문자가 실제로 안 나간다(SMS 업체 미정). |

## 읽는 법

- **앱 API 가 `—`** 인데 앱 요구사항이 있으면 미착수다.
- **테이블만 있고 서비스가 0** 이면 스키마만 잡아둔 상태다.
- 숫자는 있고 비고가 비어 있으면 별도 블로커 없이 도는 것이다.

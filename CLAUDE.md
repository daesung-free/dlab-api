# CLAUDE.md — dlab-backend

이 파일은 Claude Code가 세션마다 참고하는 프로젝트 컨텍스트다. 작업 시작 전 반드시 이 안의 원칙·블로커·컨벤션을 기준으로 판단할 것.

## ★ 정본 우선순위

**1순위: `md/개발문서.md`에 링크된 구글 시트 2종.** 가장 자주 갱신되고 가장 구체적이다 — 판단이 갈리면 무조건 이쪽이다.
- **DSA_DLab_요구사항정의서** — 화면별 요구사항(통합관리 `F-*` / 앱 `A-*`), 데이터·업무로직, 메뉴 매핑, **오픈이슈 관리대장**(D/S/E/I 번호의 출처)
- **DSA_DLab_개발착수_실행가이드** — Phase별 실행순서·착수 체크리스트·주의사항

**2순위: `D.Lab 통합기획서`(구글 문서/Word).** 큰 방향과 원안.

**3순위: 이 파일.** 위 둘의 요약 + 회의 확정사항 기록이다.

> ⚠️ **시트는 수시로 바뀐다.** 작업 착수 전 한 번은 다시 받아볼 것. 실제로 0803 갱신에서 결제 구조가 확정되고, 좌석이탈 앱 쓰기가 삭제되고, 화면 번호 체계가 통째로 바뀌었다.
> 시트가 이 파일과 다르면 **시트가 맞다고 보고 이 파일을 고친다.** 단 시트에 없고 이 파일에만 있는 것(회의 확정사항)은 누락이 아니라 추가 정보다.

## 1. 프로젝트 한 줄

D.Lab(대성학원)이 대성전산 소유의 레거시 DSA(웹 관리프로그램)에서 벗어나, **학원이 직접 소유하는 통합 플랫폼**(관리 웹 + 재원생 앱)을 신규 구축한다. 도메인 본질: 앱·키오스크·관리 웹이 실시간으로 같은 상태(출결/벌점/승인)를 공유하는 승인 워크플로우 — 학생 행위(출결·사유제출·신청) → 학부모 승인 → 자동 처리(방화벽 해제, 벌점 산정 등)의 3자 상태머신이다.

## 2. 현재 코드베이스 상태

<!-- 실제 진행 상황에 맞춰 이 섹션을 계속 갱신할 것. 아래는 착수 시점 템플릿. -->

- 스택: Spring Boot (Java) · **AWS RDS for PostgreSQL** · JPA (§2 결정로그 — 원래 Supabase였으나 전환)
- 진입점/패키지 구조: §5 참고
- 현재까지 구현 (Phase 0):
    - 공통 계층: `ApiResponse` / `ErrorCode` / `BusinessException` / `GlobalExceptionHandler`, `BaseEntity`(공통컬럼 §7), `Clock` 빈
    - Flyway V1~V5: 기반(지점·계정·학생2단·학부모·직원·반·좌석/구역·교시) / 승인 / 출결 / 방화벽+상벌점 / 알림
    - `domain/approval`: 타임아웃 에스컬레이션 3케이스 판별 + 조건부 UPDATE 동시성 처리. **사유신청·정기일정·방화벽 공용**
    - `domain/firewall`: 해제 신청만 담당(승인은 approval에 위임)
    - `domain/attendance`: 태깅 원장(7종 DSA 코드) + 일자 집계 + 미등원 감지 스케줄러
    - `domain/notification`: 이벤트-채널 매핑, 학생명 변수 필수 검증, dedupKey 중복발송 방지. **문구 미확정이라 실제 발송은 보류(SKIPPED)**
    - `domain/penalty`: 상벌점 + 규칙(멱등키). 규칙 매핑(I-5) 미확정이라 자동부여는 비활성
    - 아직 없음: 컨트롤러(`api/**`), 인증(JWT/Security), DSA 호환 구획(`api/kiosk`), 실제 발송 연동체
- 앱 기동 가능 여부: **기동 확인됨.** `docker compose up -d` 후 `--spring.profiles.active=local`로 기동 시 Flyway 5개 적용 + JPA `validate` 통과.
- 테스트: `./gradlew test` (로컬 PostgreSQL 필요). 최초 1회 `CREATE DATABASE dlab_test OWNER dlab;` — 설정은 `src/test/resources/application.yml`.

📌 **결정 로그** *(기획서에 명시 안 됐지만 Claude Code 세션 중 추가/합의된 사항을 여기 기록)*
- 예: springdoc-openapi 도입 → API 스펙 자동화 목적
- ~~기획서 3.6의 "사감 QR 순찰" 방식은 채택 안 함 — 대신 **자리이탈** 여부만 추적. 트리거는 키오스크 카드 태깅뿐이고 앱은 조회 전용. Webhook으로 백엔드 수신, 기존 `integration/kiosk` 채널 재사용.~~
  → **QR 순찰 미채택만 유효하고 나머지 3가지는 전부 이후 정정됨** (이 줄은 이력용으로만 남긴다):
  ① 앱도 **쓰기 가능**(패드 소지 시 신청, F-4.11-8 — 아래 "자리이탈 트리거 경로 재정정")
  ② **Webhook 아님** — 키오스크가 우리를 호출한다(아래 "키오스크 연동 방향 정정")
  ③ `integration/kiosk` **두지 않음** — `api/kiosk/`가 맞다(§5)
- **방화벽 해제 승인 흐름 변경** (기획서 3.7 원안: 학부모 단독 승인 → 변경): 학생 신청 시 **학부모+선생님(관리자)에게 동시 발송**, 둘 중 먼저 승인한 쪽으로 처리. 나중에 확인하는 쪽 화면에는 "○○ 승인으로 자동 승인되었습니다" 표시.
- **수납·급식 결제 구조 변경** (기획서 6.1 원 확정사항 "디멤버·수납 개발 주체=대성전산" 뒤집힘): 대성전산 수납 프로그램 사용 안 함. **수납·급식 결제 전부 D.Lab 자체 PG사**로 통합 처리 — 급식업체 명의 PG사 아님.
- (신규 결정 생길 때마다 이 형식으로 한 줄씩 추가)
- **컨트롤러 계층을 도메인 폴더에서 분리해 `api/{app,admin,webhook}/도메인명/`으로 재구성.** 이유: "관리하기 편함"(클라이언트별로 폴더 나눠서 각자 자기 것만 보게) + 비즈니스 로직 중복 방지(payment 등 여러 클라이언트가 공유하는 로직은 domain에 하나만 유지). 서비스/엔티티/리포지토리는 그대로 domain 아래.
- **Flyway 도입.** 요구사항이 계속 변경되는 프로젝트 특성상(수납 구조, 자리이탈, 방화벽 승인 흐름 등 이미 여러 번 변경됨) 스키마 변경을 버전관리할 필요 → `src/main/resources/db/migration/V*.sql`. 앱 기동 시 자동 적용.
- **DB를 Supabase → AWS RDS for PostgreSQL로 전환.** 이유: "학원 소유" 원칙은 계약/명의 문제일 뿐 Supabase여야만 하는 게 아니었음(§3 참고) + 백엔드(EC2/ECS 오토스케일링)와 같은 VPC에 둬서 네트워크 구조 단순화 + 5,000명 이상으로 확장 시 인스턴스 스펙업 → 필요하면 Aurora PostgreSQL Serverless v2로 전환 경로 확보. 둘 다 순정 PostgreSQL이라 애플리케이션 코드 변경 없이 연결정보만 교체하면 됨.
- **채팅 방식은 아직 미확정** (Sendbird 등 SDK vs 자체구현, §4 블로커). 자체구현으로 갈 경우의 설계 방향만 미리 검토해둔 상태: 앱↔서버 WebSocket(STOMP), 서버가 여러 인스턴스라 인스턴스 간 메시지 중계는 Redis Pub/Sub(ElastiCache) 필요, 오프라인 사용자는 기존 `notification` 도메인(FCM/카카오)이 담당. 이건 확정된 설계가 아니라 "이 방향으로 가면 이렇게 짠다"는 참고 메모일 뿐 — 착수 전 반드시 SDK vs 자체구현 결정부터.
- **Redis 도입 (채팅과 무관, `user`/인증 도메인 용도로 확정).** Refresh Token 저장, 로그아웃 시 Access Token 블랙리스트 처리, 로그인 실패 rate limiting 등에 사용. 로컬은 `docker-compose.yml`에 Redis 컨테이너 추가. (채팅 자체구현이 나중에 확정되면 그때는 Pub/Sub 용도로 같은 Redis를 재사용하되 별도 인프라 추가는 아님.)
- **방화벽 해제 승인 로직을 "단순 레이스"에서 "타임아웃+에스컬레이션 모델"로 정교화** (#40 / I-20, I-12): 학부모 우선 승인 → 타임아웃 지나면 담임 에스컬레이션 승인 → 이때 학부모에겐 "시간 지나 담임이 승인" 알림. 반면 타임아웃 전에 학생이 재촉해서 담임이 먼저 승인하면 **다른 문구**("시간 남았지만 담임이 먼저 승인")로 안내해야 함 — 상세는 §3 참고. 스키마에 `timeout`·`escalation`·`default_approver` 필드는 Phase0에 미리 마련해두고, 정확한 타임아웃 값과 사전지정 UI 여부는 §4 블로커로 대기.
- **방화벽 에스컬레이션 승인자 = 담당선생님(사감)으로 확정, 타임아웃 = 10분으로 확정.** (§4 블로커 해소)
- **담당선생님(사감)-학생 매핑은 "반배정" 시 자동 연동.** 반(class) 엔티티에 담임(담당선생님) 필드를 두고, "반 관리" 화면에서 반별로 담임을 지정 → 학생을 그 반에 배정하면 담당선생님이 자동으로 따라옴. **학생별 승인자 사전지정 UI는 불필요** — #40 "사전지정 UI 여부" 블로커 해소, 대신 반 관리 화면에 담임 필드만 추가.
- **선생님 role은 두 종류: 담당선생님(사감) / 행정선생님.** 공지(notice) 작성 권한이 이 구분에 따라 갈림 — **조회는 관리자 전체 공유**, **작성은 역할별 제한**: 전체공지는 행정선생님만, 반공지는 담당선생님만 작성 가능. `notice` 도메인에 작성 권한 체크 로직 필요.
- **회원가입 흐름 확정 — 학생과 학부모가 완전히 다르다.**
    - 학생: 휴대폰 인증 + 내신·모의고사 성적 입력 → **관리자 승인 요청** → 승인 전엔 앱 접근 완전 차단(중간 상태 없음, PENDING→ACTIVE 이분법) → 승인되면 가입 완료
    - 학부모: 휴대폰 인증 + **학생 고유ID** 입력 → 승인 절차 없이 **즉시 자동 가입**
    - 학생 고유ID는 마이페이지 화면에 노출 (문자 발송·별도 인쇄 등 다른 전달 수단 없음). 이 ID를 아는 사람이면 학부모 본인 확인 없이 연결 가능하다는 리스크가 있으나, 이건 클라이언트가 감수하기로 한 부분 — 추가 검증 로직 만들지 말 것.
    - 모의고사 성적은 **더프리미엄 API로 자동 조회**한다(학생 입력 불필요). ⚠️ **2026-08-30에 "API가 답안만 준다"로 확인됐다가 08-31에 성적 조회 엔드포인트가 추가되며 다시 뒤집혔다** — 아래 결정로그. **내신**은 여전히 학생 직접 입력.
    - 기획서의 "성인" 분류는 **n수생**을 의미하는 것으로 확정 (고2/고3/n수생 3분류).
- **학부모 다자녀 계정 구조: 계정 1개 + 자녀 여러 명 연결(로그인 1번 + 앱 내 자녀전환 UI)로 확정.** 자녀 수만큼 완전히 분리된 별도 계정을 만드는 방식은 기각 — 사용성(로그인/로그아웃 반복 불편)과 알림 발송 로직(어느 계정 기준으로 보낼지 모호해짐) 둘 다 더 복잡해짐.
- **미등원 알림 로직 확정.** 등원시간(지점 공통, 학생별 아님) 정각에 **스케줄러(배치)** 실행 → 그 시각까지 키오스크 태깅 기록이 없는 학생만 필터링 → 학부모에게 **카카오 알림톡** 발송(FCM Push 아님, 중요도 때문에 격상). **사전에 결석 사유를 제출한 학생은 제외** — 무단결석인 경우만 대상. 이건 지금까지의 로직(키오스크 이벤트 발생 시 반응)과 반대로 **"이벤트가 없었음"을 감지**해야 하는 유일한 유형이라 별도 스케줄러 구현 필요(§3 참고).
- **알림 템플릿 공통 원칙: 모든 템플릿에 학생명 변수 필수.** 다자녀 학부모가 "이거 누구 얘기지" 헷갈리지 않도록, 어떤 알림이든 `{학생명}` 변수를 반드시 포함해야 함.
- **채팅은 SDK 사용으로 확정, 자체구현 폐기.** 여러 SDK 후보(Sendbird, Stream Chat 등)를 **우리 팀이 직접 프로토타입으로 제작**해서 클라이언트에 전달 → 클라이언트가 최종 선택. 프로토타입 제작 자체도 개발원칙(§6-2, 연동은 핵심 도메인 완성 후)을 따라 `user` 등 핵심 도메인이 안정된 뒤 착수.
- **결제 PG사 = "대성 PG"로 확정.** 대성전산과 무관한 별도 계열사로 확인됨 — "학원 소유 DB" 원칙과 충돌 없음. 계약서/문서는 클라이언트가 전달 예정, 세부 법적요건(전자금융거래법 등)은 여전히 §4 블로커.
- **급식 PG 관련 오해 해소**: 요구사항정의서(E-3, "급식업체가 이미 쓰는 PG 그대로 사용, 신규 선정 아님")와 위 "대성 PG" 결정이 충돌하는 것처럼 보였으나, **급식업체가 원래 쓰던 PG 자체가 대성 PG였음**이 확인됨 — 신규 벤더 선정이 아니라 기존 것을 수납까지 확장해서 쓰는 것. 충돌 아님.
- ~~**자리이탈 트리거 경로 재정정 — 키오스크 + 앱 둘 다 가능**(F-4.11-8 "패드 소지 시 앱에서 신청")~~
  → **2026-08-03 시트 갱신으로 다시 뒤집힘. 앱은 조회 전용이다** (I-16 ▷[0803] "앱 신청 경로 없음(조회 전용) 확정", 앱 요구사항 `A-5`도 "좌석 이탈/복귀 **현황 표시**"). **세 번 뒤집힌 항목**이니 다음에 또 바뀔 수 있다고 보고, 쓰기 경로를 스키마에 미리 박지 말 것. 두 채널 충돌 규칙 문제도 이걸로 소멸.
- **자리이탈 리더/구역 식별 이슈 해소.** 별도 구역 식별 없이 기존 키오스크 호출을 그대로 받아 처리하면 되는 것으로 확인됨 (§4 블로커였던 항목 해소).
- **★ DSA 기능확인 회신 수령 (2026-08-04) — 재구축 범위가 실제로 줄었다.** 클라이언트가 DSA 50개 기능에 사용 여부를 체크해 회신했다(`doc/상세기능.md`, 원본 `DSA_기능확인요청서_(1).docx`). **사용 19건 / 미사용 31건**, 추가 기재란 1~9는 전부 공란이라 별도 요청 없음. 요약:
    - **NEW 접수·모집·현황 16건 전부 미사용** — 접수창 설정·지원기준·선택과목·장학성적·표준점수·**합격생등록**·마감·청구부여·입학관리·등록현황·접수현황 등. 이 계열 화면을 DSA 참고자료로 삼지 말 것
    - **메모/기타 9건 미사용** (`금일 수정 이력`만 사용)
    - 그 외 미사용: **반배정**(대신 `시간표·이동수업 > 고정반 관리`를 쓴다) · **모의고사** · **출결현황** · 선생님 · 선생님 교재 관리 · **독서실 신청**
    - **⭐ 클라이언트가 직접 추가한 항목: `독서실 > 독서실 좌석배치표 = 사용`.** DSA에 **좌석배치표(좌석맵)가 이미 있다**는 뜻이라 V2에서 만든 `study_area`·`seat_master`(좌표)·`seat_assignment`의 참조 화면이 존재한다 — 실사 가치 있음. 반면 **독서실 "신청" flow는 없으므로**(미사용) 앱 신청은 순수 신규다
    - "별도 생성 예정"으로 표기하며 **사용**에 체크한 4건(급식신청·특강·결제·앱 관련)은 *"현재 유사 업무가 있고 신규 앱으로 대체 예정"*으로 해석한다
- **해석 주의 3가지** (위 회신을 요구사항과 대조할 때):
    ① **"합격생등록 미사용"인데 요구사항 F-4.1-4는 신규 접수 등록 + 학번 채번을 요구한다.** DSA의 그 메뉴를 안 쓴다는 뜻이지 기능이 불필요하다는 게 아니다 — 신규 시스템에서는 만든다. 다만 **현재 학생 등록을 어디서 하는지** 확인 필요
    ② **"출결현황 미사용"이지만 `D.Lab` 메뉴는 사용**이다. 출결을 D.Lab 메뉴에서 보는 것으로 해석된다(DSA 실사의 "D.Lab > 출결 관리"와 일치). 출결 화면 참조 시 대상을 헷갈리지 말 것
    ③ **"미사용" = "현재 DSA에 그 흐름이 없다"이지 "불필요"가 아니다.** 앱 신규 기능(독서실 신청 등)은 그대로 개발 대상
- **상담(입시/학습 상담, CS상담과는 별개) — 목업 존재, 이를 기준으로 진행.** 상담 종류가 여러 개이며 실제 목업이 있어 그대로 활용 가능 (§4 블로커 사실상 해소, 다만 도메인 패키지 구조는 목업 검토 후 별도 확정). CS 상담(민원성)은 기존대로 구글시트 유지, 변경 없음.
- **★ 키오스크 연동 방향 정정 — "Webhook 수신"이 아니라 "DSA 호환 API 제공"이다.** 키오스크는 지금까지 대성전산 서버(`api.dshw.co.kr`)의 `POST /kiosk/*`를 **호출**해 왔다. 앞으로 **base URL만 우리 서버로 바꾸고 키오스크 코드는 수정하지 않는다** → 우리가 DSA와 동일한 경로·요청·응답을 제공하는 **응답 서버**가 된다. 엔드포인트 25개 계약은 `docs/dsa-compat.md`에 정리. **이 결정으로 요구사항정의서 오픈이슈 #2(D-2, 최우선 "키오스크 payload·RFID 매핑 미확인")가 해소**되어 출결 Phase1 착수가 가능해졌다.
- **DSA를 감싸는(프록시) 게 아니라 대체한다.** 우리 DB에서 직접 서빙한다. 따라서 `integration/daesung`(대성전산 호출)은 용도가 사라진다.
- **가이드 문서 3종을 `docs/`에 분리.** CLAUDE.md는 원칙·블로커·판정만 담고 상세는 아래로 뺀다 — `docs/dsa-compat.md`(키오스크 API 25개 계약·함정), `docs/schema-guide.md`(테이블별 설계 근거), `docs/domain-map.md`(도메인↔요구사항정의서 F번호↔오픈이슈, 착수 가능 여부). ※ `doc/`(단수)는 `.gitignore` 대상(레거시 SQL)이므로 문서는 반드시 `docs/`(복수)에 둘 것.
- **★ 키오스크는 손대지 않는다 — DSA 호환 25개는 "과도기"가 아니라 영구 인터페이스다.** 키오스크 백엔드(`doc/kiosk/deasung-kiosk-backend`, Spring Boot + MySQL)를 확보해 확인한 결과, DSA 주소가 `dsa.api.base-url` **설정 한 줄**이고 인증정보는 `stores` 테이블 3컬럼(`dsa_acad_cd`/`dsa_client_id`/`dsa_secret_id`)이다. → **전환은 설정·데이터 변경만으로 되고 키오스크 코드 수정이 전혀 없다.** 따라서 "나중에 우리 네이티브 API로 갈아탄다"는 계획은 없으며, 25개 계약을 **영구히 그대로 유지**한다. 키오스크 백엔드가 자체 DB에 출결·좌석을 이중 보관하며 동기화하는 것도 그쪽 사정이므로 관여하지 않는다 — 우리는 DSA 역할만 정확히 수행한다.
    → **★ "손댈 수 없다"가 아니라 "손대지 않아도 된다"였다 (2026-08-18 정정).** **키오스크는 우리(당사)가 개발한 것이라 코드를 고칠 수 있다.** 위 항목은 *"전환에 키오스크 수정이 필요 없다"*는 뜻으로 계속 유효하지만, **"키오스크 무수정"을 제약으로 쓰던 판단은 전부 재검토 대상이다.** 실제로 두 가지가 풀린다:
        - **동적 QR(D-2)** — §3이 *"TTL·회전 토큰을 얹으면 키오스크 수정이 필요해져 무수정 전제와 상충한다"*며 정적 `rfid_no`로 갔는데, **그 상충이 없다.** 단말 검증부를 우리가 고칠 수 있으므로 `A-4`의 "동적 QR"을 요구사항대로 만들 수 있다.
        - **좌석이탈 로그 수신** — 키오스크 쪽 push 구현·재시도를 "요청"해야 하는 외부 협의 건이 아니라 **우리 작업**이다. 대성전산 회신을 기다릴 이유가 없다.
    - 다만 **공짜는 아니다.** 별도 레포(Spring Boot + MySQL)이고 9개 지점 단말에 배포해야 하므로, 서버만 고치는 것보다 무겁다. **여전히 25개 계약을 바꾸지 않고 되는 일이면 그쪽이 낫다** — 계약을 흔들면 이중 보관·동기화까지 같이 흔들린다.
- **미확보 스키마 4종이 키오스크 백엔드에서 해소됨.** Flyway 마이그레이션 53개에 `attendances`·`seats`(+`area_cd`/`area_nm`)·`seat_usages`·`seat_change_requests`·`outings`·`seat_leaves`·`seat_leave_reasons`·`students`·`stores`가 전부 있다. **MySQL이라 복사가 아니라 이식**이며, 어차피 신규 설계 원칙이라 참고자료로만 쓴다. 남은 미상은 `reg_gn` 값 목록 하나뿐(§4).
- **★ 지점은 11개이고 코드가 전부 확정됐다 (2026-08-26).**

  | 분당 | 일산 | 동탄 | 김포 | 부천 | 이매 | 광명 | 목동 | 송파 | 대전 | 대구 |
  |---|---|---|---|---|---|---|---|---|---|---|
  | 31 | 32 | 33 | 34 | 42 | 43 | 44 | 45 | 46 | **47** | **48** |

    - **31~34 다음이 42로 건너뛴다** — 대성전산이 부여한 임의값이라 연속이라 가정하지 말 것. 반면 **46 다음 47·48은 신규 오픈분이 순서대로 붙은 것**이다(대전 6월·대구 7월 오픈, 키오스크 설치 완료).
    - **★ 이 값은 대성전산이 준 게 아니다.** 레거시 스키마 27개를 받을 때 **지점 테이블(`TB_ACADEMY_INFO`)이 빠졌다**(§4). 그래서 9개는 **키오스크 백엔드 레포의 `R__seed_dsa_credentials.sql`에서 읽었다.**
        - ⚠️ **★ 그 파일은 "개발 환경 지점 시드"다 — 운영 데이터가 아니다.** 거기 9개뿐이라 대전·대구가 없는 줄 알았는데, **운영 키오스크 어드민에는 11개가 다 있었다**(2026-08-26). **레포에 커밋된 시드를 운영 현황으로 읽지 말 것** — 신규 지점은 시드에 안 들어간다.
        - 즉 지점 목록의 진실은 **운영 키오스크**에 있다. 대성전산도, 우리 레포도, 키오스크 레포도 아니다.
    - **분당 = 수내**다(신고 현황 문서 표기). 같은 지점이다.
    - **★ 동탄2관은 동탄 본관의 별관이다.** 본관 A·B·C구역과 **좌석번호가 동일**한데 DSA가 구분을 못 해서 **별관을 1000번대로 바꿔 운영 중**이다(1번→1001번). 클라이언트는 신규 시스템에서 **같은 번호를 쓰되 본관/별관이 구분되기를** 요구했고, 별관이 더 생겨도 추가 가능해야 한다.
        - ⚠️ **그대로 만들면 키오스크가 깨진다.** 키오스크는 `seat_cd` 하나로 좌석을 찾는데 같은 번호가 둘이면 어느 것인지 모른다 — DSA에서 1000번대로 우회하던 이유가 그것이다.
        - **우리 DB는 `(관, 좌석번호)`로 구분하고 키오스크에 내려줄 때만 별관을 1000번대로 변환**한다. 계약 무변경, 화면은 본래 번호로 표시된다. `seat_master`가 지금 `UNIQUE (academy_id, seat_cd)`라 **관 축을 추가해야 한다.**
    - **`store_code`는 전 지점 비워 둔다.** 기존 9개가 `DS-001`~`DS-009`로 알려져 있으나 대전·대구분을 몰라, **절반만 채우면 대조가 더 헷갈린다.**
    - **★ 지점 시드는 `db/migration`이 아니라 `db/seed`에 있다.** `db/seed/V20260807_1600__academy_seed.sql`이 정본이고, 2026-08-26에 대전·대구를 추가했다. 지점별 교습비·급식업체 연결은 `db/seed/V20260826_1000__branch_pricing_seed.sql`이다.
        - **★ 마이그레이션에 넣지 말 것.** 테스트가 스스로 지점을 만드는 구조라 **미리 심어두면 충돌한다** — 실제로 `db/migration`에 넣었다가 **388건이 깨졌다**(2026-08-26). 위치가 나뉜 게 실수가 아니라 의도다.
        - **지점별 가격·급식 시드가 여기 있는 이유도 같다.** `V20260821_1000`(교습비)·`V20260821_1700`(급식업체)의 지점별 INSERT가 `academy`를 JOIN하는데 마이그레이션 시점엔 지점이 없어 **전부 0건**이 됐다. 공통 행만 들어가 있으므로 **"공통과 다른 지점"만** 시드로 채운다.
        - **`db/seed`는 Flyway가 읽지 않는다 — 손으로 돌리는 로컬 세팅용이다.** 로컬 세팅은 **`./scripts/seed-local.sh`** 한 번으로 끝난다(지점·교시·지점별 가격 + 개발용 계정·학생 60명). 전부 멱등이라 여러 번 돌려도 된다. ⚠️ **자동 적용을 위해 `locations`에 넣지 말 것** — 시드는 자주 바뀌는데 Flyway가 관리하면 **고칠 때마다 체크섬 불일치로 기동이 실패하고**, `flyway_schema_history`에 남은 기록이 §4 컷오버 때 *"applied but not resolved"*로 validate를 깨뜨린다. `locations`가 전 프로필 `classpath:db/migration`뿐이고, 같은 폴더의 `period_master_dev.sql`은 `V` 접두사가 없어 애초에 Flyway 대상이 아니다. **누락이 아니라 그렇게 설계된 것이다.**
        - ⚠️ **`locations`에 `db/seed`를 추가하지 말 것.** 시드가 `V20260807_1600`인데 운영은 이미 그보다 높은 버전까지 적용돼 있어 **out-of-order로 기동이 실패한다**(운영은 `out-of-order: false`). 운영 지점 데이터는 **컷오버 체크리스트**를 따른다.
        - ⚠️ **`attendance_deadline`이 전 지점 기본값 09:00이다.** 미등원 감지 배치와 지각 판정이 이 값을 보므로 **컷오버 전에 지점별 실제 등원 기준시각으로 채워야 한다.**
        - **`store_code`는 11개 전부 채워져 있다** — `DS-001`~`DS-009`(기존) + **대전 `DS-010` · 대구 `DS-011`**(2026-08-26 키오스크 어드민에서 확인). ⚠️ **값을 추측해서 채우지 말 것** — 어긋난 `store_code`가 들어가면 "설정은 됐는데 연동이 안 되는" 상태가 되어 원인을 찾기가 더 어려워진다.
- **키오스크 백엔드 소스가 우리 응답의 검증 기준이다.** DSA 응답을 실제로 파싱하는 코드(`DsaAreaService`·`DsaAttendanceService`·`DsaRequestService` 등)가 곧 스펙이다. 구현·테스트 시 이 코드를 기준으로 맞추면 어긋나지 않는다.
- **대성전산 레거시 스키마 27개 수령 (2026-07-31).** 7/22 요청서에 대한 회신. 단 **출결·좌석/구역·지점·사유신청 4종은 미확보**이고 추가 제공 협의가 난항 중 → §4 블로커. 받은 파일 자체에도 문법 오류가 있으나(일부 `)';` 종료, `COLLATE` 값 누락, 전 파일 `//` 주석이라 MySQL 실행 불가, 인코딩 CP949/UTF-8 혼재) **PostgreSQL로 신규 설계하므로 실행할 일이 없어 보정 불필요**. 컬럼·주석만 참고자료로 쓴다.
- **★ 선생님/직원 구조 확정 (2026-08-03).** `teacher`에는 **담당선생님(사감)만** 들어가고 **행정선생님은 `employee`** 쪽이다. 지점마다 강사 조직과 행정 조직이 실제로 분리되어 운영되기 때문이다.
    - **겸직은 없다** — 한 강사가 여러 지점을 맡거나 다른 지점에서 행정을 보는 경우가 없어, `teacher`가 `academy_id`를 직접 갖는다(배정 테이블 불필요).
    - 이 구조 덕분에 `class_master.homeroom_teacher_id`와 `approval_request.escalation_teacher_id`의 **FK가 곧 "담당선생님 보장"**이 된다 — 배정할 때마다 역할을 검사할 필요가 없다. 합치면 이 보장이 사라지니 합치지 말 것.
    - **공지 작성 권한은 두 테이블에 걸친다** — 전체공지=행정선생님(`employee`), 반공지=담당선생님(`teacher`). `notice.author`를 다형 참조(`author_type` + `author_id`)로 둬야 한다.
    - **학생 가입 승인은 행정(`employee`)이 한다** — 담당선생님이 아니다.
    - RBAC는 요구사항정의서가 정본이다: **5단계**(`SUPER_ADMIN`/`BRANCH_ADMIN`/`TEACHER`/`STAFF`/`READONLY`). 담당/행정 구분을 `role`과 별도 컬럼에 이중으로 두지 않는다.
- **★ 본인인증은 휴대폰 인증만 한다 — PASS(본인확인기관)는 채택하지 않는다 (2026-08-05 확정).** 우리가 인증번호를 보내고 확인하는 방식이고, 확인되는 것은 **"이 번호의 주인이 맞다"뿐**이다. 실명·생년월일은 알 수 없다.
  **PASS를 뺀 이유**: 계약·건당 비용·앱 SDK가 필요한데, **PASS를 붙여도 "진짜 그 학생의 학부모인가"는 검증되지 않는다** — 그건 학생 고유ID를 아는 사람이면 연결 가능한 구조이고 클라이언트가 감수한 부분이다(§3). 즉 실익이 실명 확보와 CI 기반 동일인 추적뿐인데, 학생 실명은 관리자 승인에서 대조하고 재등록 동일인 추적은 이미 `student` 2단 구조가 푼다. CI는 민감정보라 저장하면 관리 부담만 늘어난다(발주처가 개인정보 건에 민감한 상태다).
  **따라서 가입 시 이름은 사용자 입력값을 그대로 쓴다.** 인증이 실명을 보장하지 않는다는 것을 전제로 설계할 것 — 가족 명의 폰이면 그대로 통과한다.
  ⚠️ 다만 **결제(Phase 6) 법적요건에서 본인확인이 요구되면 재검토 대상**이다. 그때는 `VerifiedIdentity`에 필드를 더하고 `PhoneVerificationService` 앞단만 교체한다(가입 서비스는 안 바뀐다).
  발송 수단은 아직 목업(`LoggingSmsSender`)이다 — 업체 확정 시 `SmsSender` 구현체만 갈아끼운다.
- **★ 앱 약관 동의는 받는다 (2026-08-06 확인). ⚠️ 요구사항정의서에는 없는 항목이다.** 두 시트 전 탭을 검색해도 "약관"이 나오지 않고 `A-21`에 *"알림 수신 동의"*만 있는데, 확인 결과 **이용약관·개인정보 수집 동의는 받는 것이 맞다.** 앱 스토어 심사(12월 단 한 번)와 개인정보 취급 때문이다. **시트 반영을 요청해야 다른 담당자도 안다.**
    - **★ 약관은 문구를 고치지 않고 버전을 올려 새 행을 추가한다.** 덮어쓰면 **이미 동의한 사람들의 근거가 사라진다** — "환불 7일"에 동의한 100명이 있는데 문구를 "3일"로 고치면 그 100명이 3일 조항에 동의한 것으로 기록이 바뀐다. 그래서 **수정 API를 만들지 않았고** 같은 `code`+`version` 재등록도 막았다. 오타 수정도 버전을 올린다.
    - **동의는 플래그가 아니라 이력이다.** `term_agreement`가 `code`가 아니라 **`terms` 행을 가리키므로** "그때 어떤 문구에 동의했는가"에 답할 수 있고, 철회도 행으로 남긴다(append-only). 이 구조 덕분에 **개정하면 새 버전이 자동으로 미동의가 되어 앱에서 다시 뜬다.**
    - **필수 약관은 철회할 수 없다** — 서비스를 쓰면서 이용약관 미동의는 모순이라 탈퇴로 처리할 일이다. 선택 약관(마케팅 등)은 철회된다.
    - **약관 종류·문구는 미정이지만 착수를 막지 않는다** — `terms.code`가 enum이 아니라 문자열이라 정해지면 관리자 화면에서 행만 넣으면 된다(코드 변경·마이그레이션 불필요).
- **★ 2026-08-05 시트 갱신 — 앱 가입 흐름이 정합됐다.** 요청한 4건이 모두 반영됐다.
    - **온보딩 상태머신에 승인 2단계가 들어갔다**: `REGISTERED → PENDING_APPROVAL → APPROVED → OT_DONE → PARENT_LINKED → SCHEDULE_SET → ACTIVE` (A-2 · F-4.12-1). 0803판에 관리자 승인이 누락됐던 것을 정정한 것이다.
    - **가입 경로는 자가가입 하나뿐이다** — *"관리자는 계정을 직접 생성하지 않고 승인·초기화(분실 시 임시 비밀번호 재발급)·잠금 해제를 담당"*. **관리자 계정 발급 API를 만들지 말 것** — 만들면 승인 절차를 우회하는 두 번째 가입 경로가 생긴다.
    - **학부모 연동 키 = 학생 고유ID + 학부모 휴대폰 인증**으로 명시됐다(0803의 "학생 연락처 기반"은 폐기된 카카오 로그인 흔적이었다). 휴대폰 인증은 **가입하는 본인의 번호**를 인증하는 것이고, 목적은 본인확인·비밀번호 찾기·알림톡 수신처 확보다. **관계 자체는 여전히 검증하지 않는다.**
    - **가입 시 내신 성적 입력이 명시됐다**(모의고사는 더프리미엄 자동조회라 제외). 양식은 여전히 미확정 → §4.
    - **승인 대기 중에는 로그인 자체가 막힌다 (2026-08-05 확정).** 시트 H열의 *"로그인 시 승인 대기 화면만"*은 무시하고 F42/G열의 *"승인 전 앱 전면 차단"*이 맞다 — 현재 `AuthService`가 `SIGNUP_PENDING`으로 로그인을 거부하는 동작이 정답이므로 **고치지 말 것.**
    - **⚠️ 남은 것: 상태머신에 `REJECTED`가 없다.** 시트는 반려 사유를 기록하고 재신청한다는데 상태값이 없다. **로그인이 막히는 것이 확정됐으므로 더 걸린다** — 앱에 못 들어오는 사용자에게 반려 사유를 어떻게 전달하고 재신청시킬 것인지(알림톡? 재가입 시 안내?)가 같이 정해져야 한다.
- **★ 성적 입력 양식 확정 — 내신 블로커 해소 (2026-08-20).** 입학서류 신상기록부 3종(`md/준비서류/`) 실물로 풀렸다.
    - **내신은 칸이 하나다** — *"내신성적(주요교과평균)"*. 상상했던 학년별 항목·학기 단위·등급/원점수 구분이 **전부 없다.** 숫자 한 개라 별도 테이블 없이 컬럼으로 뒀다.
    - **단위를 서버가 판정하지 않는다.** 양식에 표기가 없어 등급(1.00~9.00)으로 적는 학생과 원점수로 적는 학생이 섞인다 — 범위를 좁게 잡으면 정상 입력이 거부된다. 해석은 상담 교사가 한다.
    - **모의고사는 학년마다 구성이 통째로 다르다.** 예비고2·예비고3 = 6·9·10월 **학력평가** / 국어·수학·영어·**통합사회·통합과학**, N수·현고3 = 6·9월 **평가원** + 전년도 **수능** / 국어·수학·영어·**탐구1·탐구2**. 각 성적은 표준점수·백분위·등급 3종이고 **한국사는 절대평가라 등급뿐**이다.
    - **그래서 과목·시험을 enum에 박지 않고 `exam_master`/`exam_subject` 마스터로 뒀다.** 학년마다 다르고, **2028 수능 개편으로 통합사회·통합과학이 또 바뀌며**, 탐구 과목은 학생 선택에 따라 갈린다. enum이면 해마다 마이그레이션을 새로 쓰게 되고 그때 **과거 학생 성적이 어느 과목이었는지가 흐려진다.**
    - ⚠️ **연도가 바뀌면 양식 행을 새로 넣어야 한다.** 마이그레이션에 2026년분만 있다 — 안 넣으면 그 해 가입자가 전부 `EXAM_FORM_NOT_FOUND`를 받는다. **기수 시작 전 체크리스트 항목**이고, 등록은 `/api/v1/admin/exam-forms`에서 한다.
    - **"성적을 모른다"를 0으로 채우게 두지 않는다.** 가입 시점에 성적표가 없는 학생이 실제로 있는데(자퇴·검정고시·분실) 필수로 막으면 가입 자체를 못 하고, 0을 넣으면 **통계에서 진짜 0점과 구분되지 않는다.** 사유를 남기고 건너뛴다.
    - ⚠️ **`GradeType`은 "가입 시점의 현재 학년"으로 해석했다.** 신상기록부가 `예비고3`(현 고2, 학력평가)과 `현고3`(평가원+수능)을 다른 양식으로 두는데 우리 enum은 `HIGH2/HIGH3/N_SU` 3종이라, **현 학년 기준**으로 `HIGH2`=학력평가 / `HIGH3`·`N_SU`=평가원+수능으로 seed했다. 학원이 "예비고3"을 `HIGH3`으로 등록하는 운영이면 양식이 어긋나므로 **확인 필요** — 다만 마스터 데이터라 행만 고치면 된다.
- **★ 학생 자가가입은 승인 대기(`PENDING`)로 만들어지고 성적은 별도 API다 (2026-08-20).**
    - **가입 요청에 성적을 싣지 않는다.** 시험 3회차 × 과목 6개짜리 긴 입력이라 중간에 실패하면 **휴대폰 인증부터 다시** 해야 한다. 가입을 끝내고 `/api/v1/app/grades`로 이어진다.
    - **"내 가입 상태" 조회 API를 만들지 말 것.** 승인 전에는 로그인이 막히니 무인증으로 열어야 하는데, 그러면 **전화번호만 넣어보면 그 학생의 고유ID가 나온다** — 고유ID는 학부모 연결의 유일한 키이고 관계 자체는 검증하지 않으므로(§3), 아무나 남의 자녀로 연결할 수 있게 된다. 승인 여부는 로그인 응답의 `SIGNUP_PENDING`으로 이미 알 수 있다.
    - **지점 목록(`GET /api/v1/app/signup/academies`)만 무인증으로 연다.** 지점을 골라야 가입이 되는데 그 시점엔 토큰이 없다. **이 응답에 운영정보(키오스크 자격증명·PG 코드)를 실지 말 것.**
    - 학번 채번이 여기서 일어나 **모집 시즌 동시 가입에 유니크 제약이 걸린다** — `StudentService`와 같은 방식으로 **새 트랜잭션을 열어 재시도**한다.
- **★ 재원 상태는 5종이고 전이 규칙이 있다 (P1-04).** V1 CHECK가 4종이라 **제적(`EXPELLED`)이 빠져 있던 것을 추가**했다(`재원/휴원/퇴원/제적/수료`). 퇴원(자진)과 제적(강제)은 후속처리가 같아도 재등록 심사·환불 산정·통계에서 다르게 취급되므로 합치지 말 것.
    - **전이 규칙**: 재원↔휴원 자유 / 어느 상태에서든 종료(퇴원·제적·수료) 가능 / **종료 상태에서는 재원으로만 복귀**. 종료끼리 직접 전이는 막는다 — "퇴원을 제적으로 정정"을 곧바로 허용하면 이력에 한 줄만 남아 **왜** 바뀌었는지 알 수 없다. 재원을 거치게 하면 "퇴원 취소 → 제적" 두 줄이 남는다.
    - **종료 → 재원은 착오 정정 전용이지 재등록이 아니다.** 1년 코호트라 재등록은 **새 등록 행 추가**다(§3). 지난 등록 건을 되살리면 그해 출결·상벌점이 새 기수에 섞인다.
    - **`current` 플래그를 상태와 함께 뒤집는다.** 종료되면 `current=false`가 돼 `findCurrentByRfidNo`에서 빠진다 — 빠뜨리면 퇴원생 카드로 태깅이 통과한다. **휴원은 `current`를 안 건드리므로 휴원생 카드는 여전히 통과한다** — 출결 경로(P1-07)에서 상태를 따로 확인해야 한다.
    - **후속처리는 `EnrollmentStatusFollowUp` 구현체를 빈으로 등록해 확장한다.** 실행가이드가 "배정 해제·급식 중단·앱 계정 비활성을 같은 트랜잭션으로"를 요구하는데 **급식·좌석 도메인이 아직 없다.** 서비스에 직접 박으면 나중에 그 도메인을 만드는 사람이 알아채야만 추가되고, 못 알아채면 퇴원생 급식이 계속 결제된다. 지금 구현된 건 반 배정 해제·앱 계정뿐이고 **급식·좌석은 해당 도메인 담당이 자기 패키지에 빈만 추가하면 된다.**
    - 상태 변경 이력은 `enrollment_status_history`에 남긴다. `created_by`는 등록 시점 작성자라 이후 변경자를 못 남기고 `@LastModifiedBy` 컬럼도 없어서, 이게 없으면 **"누가 퇴원 처리했나"에 답할 수 없다.** `effective_date`(효력일)와 `created_at`(처리일)을 구분한다 — 소급 처리가 실제로 있고 환불 일할계산(I-26)이 효력일을 쓴다.
- **★ 교습비·환불 규정 수령 (2026-08-20, `md/디랩 교습비 및 환불규정_0820.xlsx` 3탭). I-26이 해소되고 결제 구조가 바뀐다.**
    - **★ 결제는 "교습비 + 독서실비" 2축이다. 한 줄로 두면 환불 계산이 성립하지 않는다.** 교습비는 할인 6단계(10/20/30/50/70%)에 환불이 **구간**(이용기간 1/3까지 2/3, 1/2까지 1/2, 1/2 이후 없음 — 학원법 기준)이고, 독서실비는 **할인이 없고** 환불이 **일할**이다. 같은 청구 건인데 계산 방식이 둘이라 `billing` 한 줄(750,000)로는 나눌 수가 없다.
    - **★ 그래서 `billing`을 두 줄로 쪼개지 말고 하위 항목을 둔다.** 청구를 나누면 키오스크 `getReceiptInfo` 영수증이 750,000 한 줄에서 660,000/90,000 두 줄로 바뀐다. **청구는 그대로 두고 항목만 추가하면 영수증은 안 바뀌고 환불 계산만 항목 단위로 돈다** — 키오스크 무영향이다(`billing_item`).
        - **항목마다 정가를 따로 들어야 한다.** 환불 차감이 정상가 기준이라(아래), 청구 합계의 정가만으로는 교습비 몫을 되짚을 수 없다.
        - **독서실비에 할인이 들어오면 무시한다.** 규정상 할인이 없고, 통과시키면 환불 차감이 정가보다 작아져 학원이 손해를 본다.
        - **`billing`에 이용 연·월이 필요하다.** 환불이 그 달 교습일수를 찾아야 하는데 표시명·납부기한만으로는 알 수 없다. **청구 1건 = 한 달분**이고, 최초 입학의 두 달치는 **청구 2건**으로 만든다 — 한 건에 두 달을 담으면 그중 한 달만 환불하는 계산이 성립하지 않는다.
    - **가격표** (N수 750,000 = 교습비 660,000 + 독서실비 90,000, 전 지점 동일)

      | | 교습비 | 독서실비 |
      |---|---|---|
      | 재학생(고1~3) 목동·분당 | 660,000 | 90,000 |
      | 재학생 그 외 지점 | 400,000 | 90,000 |
      | 1인실 분당 | 660,000 | 190,000 |
      | 1인실 목동 | 820,000 | 130,000 |
      | 1인실 대구 | 822,000 | 128,000 |

    - **★ "1일 교습비 자동계산" 요청의 함정 — 나누는 일수가 달력 일수가 아니다.** 표가 **2월 27일 · 9월 29일**로 잡혀 있다(달력은 28·30). 설·추석 당일을 하루씩 뺀 것으로 보이나 미확인이고, **삼일절·어린이날·광복절 등은 안 뺐다**(3·5·8·10·12월이 전부 31일).
        - **그래서 `holiday` 테이블을 그대로 걸면 안 된다.** 그건 급식 가능일용이라 법정공휴일이 전부 들어가 3월이 30일이 된다. **"급식 쉬는 날"과 "교습비에서 빼는 날"은 다른 개념**이다.
        - **월별 일수를 관리자가 연 1회 입력하는 방식으로 간다.** 교습비·독서실비 두 값만 넣으면 할인율별 1일 단가가 전부 파생되므로(할인가가 정가×(1−할인율)로 정확히 맞는다) 요청한 부담은 이걸로 사라진다. 일수까지 자동 산출하는 건 규칙이 확정되기 전엔 틀릴 위험만 는다.
        - 절사는 **원 단위 버림**으로 보이나 대구 2칸이 올림이다 — §4.
    - **★★ 환불 차감은 "정상가 기준"이다 — 이게 규정의 핵심이고, 여기서 추가 징수가 나온다.** 규정에 *"할인적용을 받았을 경우 정상가 기준으로 차감 후 환불"*이라고 명시돼 있다. 그래서 계산이 **`납부액 × 환불비율`이 아니라 `납부액 − 정가 × 차감비율`**이다.
        - **두 식은 할인이 없을 때만 같은 값을 낸다.** 할인을 받았으면 차감이 납부액보다 커질 수 있고, 그때 **결과가 음수** — 그게 곧 **추가 징수**다. 규정의 *"차감금액이 부족할 경우 원칙상 추가금 재결제를 해야하나"*가 이 경우다.
        - 실제로 *"3~5월 할인 → 5월 퇴원 시 할인 교습비 모두 정상가 재결제"* 예시가 이 산식으로 그대로 재현된다 — 50% 할인 후 1/2 초과 퇴원이면 `330,000 − 660,000 = −330,000`으로 **할인받은 만큼 정확히 다시 낸다.**
        - **★ 음수를 0에서 자르지 말 것.** 자르면 받아야 할 돈이 사라지고, **데스크는 화면에서 그 사실을 알 방법이 없다.** `RefundCalculator.Result.requiresAdditionalPayment()`가 이걸 드러낸다.
        - **구간 비교는 정수 곱으로 한다** — `usedDays * 3 <= teachingDays`. 실수로 나누면 27일 × 1/3 = 8.999…처럼 경계에서 한 칸씩 어긋난다.
        - **전 기간을 다 쓰면 독서실비 환불은 0이다.** 1일 단가 × 일수로 계산하면 절사분이 남아 다 쓰고도 몇 원이 환불된다(90,000 − 2,903×31 = 7원). 중도 입학 결제도 같은 이유로 한 달을 다 쓰면 월 정액을 쓴다.
        - **계산 근거를 항목별로 남긴다**(`refund_calculation.detail`). 금액만 남기면 구간·일할·정상가차감이 얽혀 나중에 재현이 안 되고, 클라이언트가 요청한 *"추가결제 시 상세 내역 출력"*도 이걸 쓴다.
    - **★ 할인 소급 재결제 — 요구사항에 없던 개념이다.** 위가 "이번 달 청구"를 되돌리는 것이라면, 이건 **지난 몇 달치까지 거슬러 올라가** 그동안 받은 할인을 정상가로 다시 받는 것이다. 지금 결제 도메인은 받고→돌려주는 흐름뿐이라(`payment_transaction.amount > 0`) **여러 달치를 훑어 새 청구를 만드는 경로가 없다.** 판정은 **퇴원하는 달이 할인 기간 안이냐 밖이냐**로 갈리는 것으로 읽히나 확인 대기 — §4. **계산기는 이미 "정상가 차감" 구조라 판정만 확정되면 그 위에 얹힌다.**
    - **입학 시 결제**: 1일 입학이면 정액, 그 이후면 `수업일수 × 1일 교습비`. **최초 입학이 1개월 미만이면 다음 달분(750,000)까지 함께 납부**한다.
    - **급식업체가 지점별로 다르고 단가도 다르다** — 디온푸드 7,700(분당·이매·김포·일산·광명·목동·송파) / 니즈푸드 7,700(동탄) / 한샘푸드 7,700(부천) / 한끼애 7,700(대전) / **성림푸드 8,000(대구)**. **지금 `meal` 도메인에 금액 개념이 아예 없다** — 업체·단가 마스터가 필요하다.
    - **장학 취소 요건 확정 + 자동 체크 채택(A안).** 벌점 **40점 이상**(제적 기준과 동일) / 6·9월 평가원 **3과목 등급합 5** 미달 / 더프리미엄 모의고사 **2회 미응시**.
        - **★ 자동으로 "취소"하면 안 된다.** 시트가 *"개인사정으로 응시 못하면 더프모 성적으로 대체하는 경우도 있다"*, *"예외를 두는 경우가 많이 발생한다"*고 명시했다. **판정은 자동, 확정은 사람**이다.
        - **규칙을 `penalty_rule`과 같은 방식으로 데이터에 둔다.** 지점별·연도별로 기준이 다르다. ~~"더프리미엄 2회 미응시"는 응시 이력이 없어 못 돈다(E-2 대기)~~ → **2026-08-30 해소: 성적표가 매월 발행되므로 월별 성적 등록 이력 자체가 응시 증거다. 별도 API가 필요 없다.**
        - **벌점 40점은 제적 기준과 같은 값이다.** 상벌점 쪽과 **한 군데서만 관리**할 것 — 두 벌 두면 한쪽만 바뀐다.
    - **퇴원 시 장학금 환원은 "정상가 재결제" / "재결제 없이 퇴원"을 고를 수 있어야 한다.** 지점 상황·운영방침에 따라 다르므로 지점별 기본값 + 건별 변경. 추가결제 시 **상세 내역 출력**도 요청됐다.
    - **`Scholarship` 엔티티에 할인율밖에 없다** — 취소 요건·판정 이력·예외 사유가 전부 없다. 장학은 사실상 신규 도메인이다.
    - **급식 환불정보를 업체에 자동 발송**해달라는 요청이 있다(지금은 단톡방 수기). **환불계좌가 개인정보라 제3자 제공 동의 범위 확인이 먼저**다.
        - ⚠️ **★ 타이밍이 있다 — 약관 문구를 지금 고치면 공짜고 나중이면 비싸다.** 약관은 문구 수정이 아니라 **버전을 올리는 구조**라, 나중에 고치면 **이미 동의한 학생 전원이 재동의**해야 하고 그동안 급식 신청이 막힌다(`MEAL_THIRD_PARTY_CONSENT_REQUIRED` 게이트). **아직 아무도 동의하지 않은 지금이 유일하게 싼 시점이다.**
        - 발송은 **이메일**이 현실적이다 — 업체가 사업자라 알림톡은 발신프로필·템플릿 심사를 또 받아야 하고, 문자로 계좌를 보내는 것도 부적절하다. **취소 즉시가 아니라 하루치 묶음**이 낫다(당일 취소가 있어 마감 시각 기준). **발송 실패를 화면에 표시하지 않으면 아무도 모른다** — 업체가 환불을 안 했는데 우리는 보낸 줄 안다.
    - **급식 취소 "환불은 제한없이 가능하도록" 요청** — 앱은 D-n(`meal_policy`, 기본 3일)이고 **데스크는 이미 무제한**이다. 데스크 경로로 되는지 앱도 열어야 하는지 확인 대기 — §4.
    - **신고 현황 탭에 지점별 `학원등록번호`·`독서실등록번호`가 있다** — I-25(교육비 납입증명서)에 필요한 값이다. 논술·국어·수학·영어 강좌비와 모의고사비(12,000)도 지점별로 들어와 있다.
    - **내년에 교습비·독서실비 인상 계획이 있다** — 가격은 연도별로 관리돼야 한다(전 테이블에 `year`가 있어 구조는 이미 맞다).
- **★ 개인정보 마스킹은 `common/privacy`를 쓴다.** 실행가이드 3.2 "엑셀 다운로드 마스킹 기본 ON". 전화는 하이픈 위치를 믿지 않고 **숫자만 뽑아 국번을 재판별**한다(서울 `02`는 2자리 — 일괄 3자리를 떼면 `02-123-4567`이 `021-`로 나와 없는 번호처럼 보인다). 판별 실패 시 전부 가린다. 생년월일은 **연도만** 남긴다(월일까지면 주민번호 앞자리가 복원된다). **마스킹 판정은 표현 계층에서** 하고 도메인 서비스는 열람자를 모른다. 응답에 `masked` 플래그를 실어 화면이 마스킹 여부를 알게 한다 — 모르면 "번호가 잘못 저장됐다"는 오인 문의가 생긴다.
    - ⚠️ **"상위 관리자"를 `SUPER_ADMIN`+`BRANCH_ADMIN`으로 해석**했다(지침이 5단계 중 어디까지인지 미명시). 지점 관리자는 보호자에게 직접 연락하는 게 업무라 제외하면 화면이 안 돌아간다. `permission` 매트릭스 수령 시 데이터로 옮길 것.
- **★ 2026-08-03 시트 대규모 갱신 — 아래는 전부 시트가 정본이다.**
    - **결제 구조 최종 확정 (D-9·D-3·D-6 해소, E-3 재정의).** "디랩 자체 명의 PG 직접결제"로 확정됐고 **급식업체 PG 연동은 폐기**돼 디랩 자체 PG로 단일화됐다. 대성전산 수납 API는 **과거 이력 이관(D-1) 용도로만** 잔존한다. → §3의 공용 결제 도메인 원칙이 그대로 유효하고, 이제 "미확정"이 아니라 확정이다.
    - **학부모는 최대 1인으로 확정** (I-12 ▷[0803]). 부·모·기타 중 누가 승인자인지 모호하던 문제가 사라졌다 — 승인 로직에서 "여러 학부모 중 누가 승인?"을 고민할 필요 없다.
    - **앱 요구사항이 별도 탭(`A-1`~`A-21`)으로 분리**됐다. 이전엔 없던 상세도다. 앱 작업은 이 탭이 기준.
    - **화면 번호 체계가 바뀌었다** — 방화벽 `F-4.11-5`→`F-4.11-10`, 좌석이탈 `F-4.11-8`→`F-4.3-2`, 연간행사 →`F-4.11-9`, 정기일정 `F-4.1-7`. **이 파일과 `docs/**`의 F번호 인용은 대부분 옛날 것이니 시트로 재확인할 것.**
    - **SMS 폴백 폐기** (E-5) — 알림톡 실패 시 대체 수단이 없다. 미등원처럼 중요한 알림은 **심사 통과가 곧 발송 가능 여부**다.
    - **가족 채팅방 폐기** (E-6) — 그룹 스레드 요건이 빠져 SDK 후보를 재검토한다. `chat`은 1:1만.
    - **순찰 폐기가 완전 반영됨** — 상벌점 `PATROL_SLEEP_2` 트리거 삭제(I-5), 순공시간 산출을 **"입퇴실 + 좌석 이탈/복귀 로그"** 기준으로 재정의(I-6).
- **★ 0803 대성 답변서 확정분 (2026-08-11 시트 반영 완료).** 답변서(`0803_회신서_답변_최종.pdf`)가 시트에 하나도 안 들어가 있어 전수 대조 후 반영했다. 상세는 `md/0803시트반영요청.md`.
    - **좌석 이탈/복귀 앱 연동 = 2차 이관 확정** (I-16). 앱 `A-5`는 1차 범위에서 빠진다. 2차 예상 2개월·추가비용 1,000.
    - **생체인식 2차 이관** (I-18). 1차 식사체크·출결 입력은 **학번 4자리 키패드**(태블릿, 4자리 입력 시 OK 터치 없이 자동 완료). 학번은 퇴원까지 불변인 4자리 고유번호. 지점당 키오스크 최소 1대 필수(태블릿 대체 가능).
    - **이행 표시 = O/X 2단계 확정** (I-19). 우리가 권고한 ○/△/✗ 3단계가 아니라 **부분이행 표현이 없다** — 통계 해석에 유의.
    - **승인 구조 확장 확정** (I-20). 타임아웃 10분은 그대로지만 **지점별 설정 가능**, 등록 시 **우선 승인자(직원/학부모) 선택 + 안내·동의 필수**, 학부모 미응답 시 **1회 자동 재승인 요청 후 직원 이양**. → **2026-08-11 구현 완료**(아래 참고).
    - **⛔ 학습계획 전면 재설계** — 클라이언트가 0710에 스스로 준 **교시×요일 그리드를 철회**하고 열품타 방식(순번 + 시작시각 + 소요시간 자유 입력)으로 바꿨다. `learning_plan_items`가 교시 기반 → **순번 기반**이 된다. **교시 20/30분 편집·교시 마스터 논의는 전부 무의미해졌다.** 미착수라 다행이지만 Phase 최대 공수 화면이라 일정 재산정 트리거다.
    - 그 외 신규: **학생 QR 코드 출력**(관리 프로그램, 업체 "탑재 가능") · 질의응답 노출설정/사진첨부/시간조정 · 특강 노출설정 · 데일리루틴 빈 날 학원일정 표시 · 상담 예약(담임 가능일정 → 학생 선택 + 알림).
- **★ 순공시간 1차 산식 확정 — 좌석이탈은 집계하지 않는다 (2026-08-12).** 시트 I-6이 *"입퇴실 + 좌석 이탈/복귀 로그 기준으로 재정의 필요"*였으나, **좌석이탈 앱 연동이 2차로 이관되면서 1차에서는 이탈 로그를 쓰지 않기로 확정**됐다.
    - **1차 산식 = 재실 − 외출 − 급식 − 쉬는시간.** 지금 `StudyTimeCalculator`가 하는 그대로다 — **코드 변경 없다.**
    - **키오스크와 같은 값이 된다.** 대성전산 DSA도 좌석이탈을 빼지 않으므로, 컷오버로 우리가 그 자리에 앉아도 학생이 보는 숫자가 달라지지 않는다.
    - ⚠️ **따라서 좌석이탈 로그를 1차에 받아야 할 이유는 「장시간 미복귀 감지」 하나만 남는다.** 그것도 2차인지 확인 중이다(I-5) — **2차면 좌석이탈 로그는 1차에 아예 불필요하다.**
- **★ 순공시간 계산 주체는 컷오버 후 우리가 된다 (2026-08-11).** 진규님이 *"키오스크는 DSA가 내려주는 순공시간을 쓴다 → 앱과 값이 달라진다"*고 지적한 건에 대한 정리다.
    - **키오스크는 순공시간을 자체 계산하지 않는다.** `getStudyTimeList`·`getLastWeekStudyTimeList` 등 통계 API가 내려주는 `study_tm`을 표시만 한다. 컷오버로 그 자리에 우리가 앉으면 **키오스크·앱·관리자 웹이 전부 같은 계산기 결과**를 본다 — 값이 갈릴 구조가 없다.
    - ⚠️ **다만 계산식 차이만큼은 컷오버 순간 값이 바뀐다** — 우리는 급식·쉬는시간을 교시 마스터 기준으로 뺀다. 대성전산 산식과 다르면 학생이 보는 숫자가 한 번 달라지므로 **안내가 필요하다.**
    - ⚠️ **앱이 컷오버보다 먼저 나가면 그 기간에는 두 값이 어긋난다.** 앱 출시(12월 심사)와 컷오버 시점을 맞추는 게 안전하다.
- **★ 좌석이탈 로그 수집 방식 — push로 정했으나 보류 (2026-08-11~12).** 순공시간에서 이탈이 빠지면서 **요청 근거 자체가 흔들렸다.** I-5(미복귀 벌점이 1차인지) 답을 받고 유지/철회를 정한다.
    - 방식은 **키오스크가 우리에게 push**로 정했다. 처음엔 우리가 조회(pull)하는 방향으로 검토했다가 뒤집었다 — pull은 유실 복구가 되지만 **우리 서버에 아웃바운드 코드가 하나도 없어** HTTP 클라이언트·방화벽 경로·역방향 인증을 전부 새로 만들어야 한다. 반면 **키오스크→우리 방향은 이미 뚫려 있고 인증도 돈다.**
    - **대신 키오스크 쪽 재시도를 요청했다.** push는 호출이 실패하면 그 기록이 사라지고 **우리는 놓친 줄도 모른다.**
        → **2026-08-18: "요청"이 아니라 우리 작업이다.** 키오스크가 당사 개발이라(§2) 대성전산 회신을 기다릴 이유가 없다. **다만 보류 사유는 그대로다** — 순공시간에서 이탈이 빠져 근거가 흔들린 것이지 협의가 막힌 게 아니었다. I-5 답이 오면 그때 우리가 양쪽을 함께 구현한다.
    - 받을 값: 지점(`acad_cd`) · 학생(`rfid_no`/`std_no`) · 좌석·구역 · 이탈/복귀 구분 · 발생 시각.
    - **벌점을 1차에 넣는다면 반드시 우리 DB에 적재해야 한다** — 자동부여가 멱등키(`학생:일자:규칙`) 기반이라 외부 조회로는 성립하지 않는다.
    - **미확정**: 장시간 미복귀 임계값(I-16) · 미복귀 벌점 트리거가 1차인지 2차인지(I-5).
- **★ 우선 승인자·자동 재승인 구현 (2026-08-11).** 0803 답변서의 승인 구조 확장을 반영했다.
    - **우선 승인자는 학생마다 다르다** — `approver_preference`에 선택 + 동의를 남기고, 신청 시 **학생 선택 > 지점 정책** 순으로 정한다. 선택이 없으면 지점 정책을 쓴다(선택을 강제하면 **이미 등록된 학생들이 아무것도 신청할 수 없게** 된다).
    - **이건 설정이 아니라 동의라 덮어쓰지 않고 쌓는다.** *"10분 내 미응답 시 직원에게 승인권이 넘어간다"*에 동의한 기록이라, 바꿨다고 이전 동의를 지우면 그 기간에 무엇에 동의했는지 답할 수 없다. 약관과 같은 방식이고 **현재값은 가장 최근 행**이다.
    - **`AUTO`는 고를 수 없다** — 학생이 자동 승인을 고르면 승인 절차가 무의미해진다.
    - **재요청은 `reminder_sent_at` 컬럼 하나가 "1회만"을 보장한다.** 횟수를 따로 세면 그 규칙이 코드에만 남아 배치가 두 번 돌면 두 번 나간다.
    - **재요청 후 같은 대기시간을 한 번 더 준 뒤 이양한다.** 동시에 넘기면 학부모가 알림을 보고 들어올 틈이 없어 재요청이 형식적인 절차가 된다. **이양은 권한 변경이 아니라 알림**이다 — 담당선생님은 원래 타임아웃 후에도 승인할 수 있었고, 없으면 대기 목록을 직접 열기 전까지 모른다.
    - **`STAFF_PRIMARY` 케이스를 새로 뒀다** — `STAFF_BEFORE_TIMEOUT`(학부모가 기다리는 중 담임이 끼어듦)과 합치면 *"시간이 남았지만 담임이 먼저 승인했습니다"*가 **애초에 승인자가 아니었던 학부모**에게 나간다.
    - 우선 승인자는 **신청 시점 스냅샷**이다. 학생이 나중에 바꿔도 처리된 건의 이력이 흔들리면 안 된다.
- **⚠️ Flyway 파일명 두 방식이 섞여 있다 (2026-08-11 발견, 08-12 정리).** `V<yyyyMMdd>_<HHmm>`은 `20260811.1400`으로, `V<yyyyMMddHHmmss>`는 `20260805220000`으로 파싱된다. **8자리+소수 쪽이 항상 작아서 밑줄 방식이 14자리 방식보다 먼저 실행된다.**
    - 실제로 `terms`(14자리)를 FK로 참조하는 마이그레이션을 밑줄로 만들었다가 *"relation terms does not exist"*로 깨졌다 → `V20260811140000__approval_primary_approver.sql`만 14자리다.
    - **★ 번호 충돌도 실제로 날 뻔했다.** 같은 날 두 사람이 각각 `V20260811_1400__approval_primary_approver`와 `V20260811_1400__daily_report`를 만들었다. 한쪽을 14자리로 바꾸지 않았으면 **`Found more than one migration with version` 으로 앱이 아예 안 떴다.** 타임스탬프도 분 단위면 겹친다.
    - **신규는 밑줄 방식(`V<yyyyMMdd>_<HHmm>`)으로 통일한다.** 단 **14자리 파일이 만든 테이블을 참조하면 순서가 뒤집히므로**, 그 경우만 14자리로 짓는다.
- **★★ 모의고사 성적은 `/api/students/grade`로 자동조회한다 (2026-08-31 명세 갱신).** ⚠️ **하루 만에 뒤집힌 항목이니 이력을 함께 읽을 것.**
    - **0830 시점**: 받은 명세(`md/모의고사/openapi.json`)에 `/api/students`(학생정보)와 `/api/students/subject`(**답안만**) 둘뿐이라 **표준점수·백분위·등급이 어디에도 없었다.** 그래서 *"자동조회가 성립하지 않는다, 담임용 엑셀(194열) 업로드로 가야 한다"*고 판단하고 클라이언트에 **성적 값 조회 API가 따로 있는지** 물었다.
    - **0831 회신**: 대성전산이 **`GET /api/students/grade`를 추가**했다(`openapi(성적조회)_0831.json`). **자동조회가 된다 — 엑셀 업로드는 불필요하다.**
    - **주는 것**: 과목별로 원점수(공통/선택/합계)·표준점수·백분위·등급·전체평균·표준점수최고점·**예상등급/예상표준점수/예상백분위/예상석차**, 그리고 **총점군(`totals`)** — 국수탐/국수사/국수과/국수/국수영탐의 원점수합·표준점수합·백분위합과 각 백분위·등급합·등급평균.
    - **★ 과목코드 문제가 같이 풀린다.** `grade`가 `subjects` 배열을 통째로 주고 각 항목에 **`name`(예: `"언어와 매체"`)과 `subjectSubCode`**가 들어온다. **단일 과목 조회(`/subject`)를 안 쓰면 과목코드표가 필요 없고**, 아래 "2028 전용 표" 문제도 비켜간다.
    - **행 구성이 학년마다 다르다** — 1·2학년은 국어/수학/영어/한국사/통합사회/통합과학, **3학년은 국어/수학/영어/한국사/선택1/선택2/계열**. 미응시 항목은 `null`이다. (소켓 프로토콜 402와 같은 구성이라고 명시)
    - ⚠️ **`/subject`(답안)는 그대로 남아 있다.** 정답·배점은 여전히 안 온다 — 채점은 우리가 하지 않는다.
    - ⚠️ **과목코드표는 여전히 2028학년도 전용이다** (국어1·수학3·영어6·한국사61·통합사회62·통합과학63). 문서 자체 예시값 `22`와 명세 예시값 `51`도 표에 없다. **`/subject`를 쓸 일이 생기면 그때 목록을 받아야 한다.**
    - ⚠️ **"응답을 캐싱하지 말라"를 반대로 읽지 말 것.** 원문은 *"요청마다 timestamp와 signature를 새로 만드세요. **캐싱하면 5분 뒤 전부 401입니다**"* — **서명값 재사용 금지이지 응답을 우리 DB에 적재하는 것을 막는 게 아니다.** 반대로 읽으면 화면 진입마다 재조회하는 구조를 만들게 된다.
    - **조회 키는 `examdt`·`schoolId`·`grade`·`classNumber`·`bunho`다.** `schoolId`는 지점이며 **`997xx` 체계**(분당 `99700` · 목동 `99707`) — 담임용 엑셀의 학교코드·반·번호와 **같은 값**이라 엑셀에서 키를 확보할 수도 있다.
    - **HMAC-SHA256 아웃바운드 호출**이 필요한데 우리 서버에 **아웃바운드 HTTP 클라이언트가 아직 없다.** 서명 대상은 `{timestamp}:{METHOD}:{경로+쿼리}`이고 **서명한 문자열과 실제 요청 URL이 바이트 단위로 같아야 한다** — 파라미터를 Map으로 넘기면 순서·인코딩이 달라져 깨진다. 개발서버는 9월말 예정.
- **★★ 출결·담임·반은 대성전산이 DSA에서 직접 수집한다 — 컷오버로 끊긴다 (2026-08-31 회신).** 회신 원문: *"디랩의 **출결/담임/반 정보 데이터를 DSA에서 수집**하여 성적표에 표시하는 것으로 예상됩니다. 최대한 관련 부서에 문의드렸지만 **데이터의 흐름을 정확하게 설명해 줄 수 있는 분이 없는 상황**입니다."*
    - **학원이 전달하는 게 아니다.** 대성전산이 DSA 내부 데이터를 읽어간다 → **DSA가 없어지면 성적표 5쪽 출결이 안 찍힌다.**
    - **★ 키오스크 25개 계약으로는 대체되지 않는다.** `getStdInfoList`가 내리는 것은 이름·학번·`rfid_no`·전화 뒷자리·좌석뿐이고 **반·담임·출결이 없다.** 우리가 DSA 자리에 앉는 것만으로는 해결되지 않는다.
    - **★ 상대가 흐름을 모르므로 "물어서 받을" 수 없다.** 우리가 규격(무엇을·어떤 형식으로·언제 보낼지)을 제안해서 닫아야 하는 항목이다.
- **★ 성적표 PDF는 대성전산이 디멤버에 직접 올린다 (2026-08-31 회신).** *"디랩에서 업로드하는 것이 아닌 **대성전산 측에서 직접 업로드**하여 디멤버를 통해 누적 성적을 확인하거나 성적표를 다운받을 수 있습니다."*
    - 그래서 *"성적 파일 취합·정리 → 학원 → 대성전산 전달"*이라는 이해는 **성적표 파일에 관해서는 틀렸다.**
    - ⚠️ **컷오버 후 수급 경로가 없다** — 대성전산이 우리 시스템에 올려줄 리 없다. 지점이 통합본 파일을 갖고는 있으니 직원 업로드가 현실적이나 **확인이 필요하다**(§4).
- **★ 온라인 성적조회 인증코드는 받을 필요가 없다 (2026-08-31 회신, 종결).** *"응시 학생이 핸드폰번호를 기입하지 않았을 때를 대비한 장치"*이고 목록 제공은 불가하나, *"디랩은 재원생·외부생 모두 이름과 핸드폰번호를 받고 있어 별도로 제공하지 않아도 될 것 같다"*.
- **★ 더프리미엄 성적표에 "산출 불가" 항목이 전부 인쇄되어 있다 (2026-08-30).** 등급구분점수(등급컷)·문항별 정답과 전국정답률·탐구조합 순위·동점자 비교·정시 지망대학 진단·**월별 누적 성적**, 그리고 **출결(담임·결석·조퇴 일자)**까지 있다. **우리가 산출하지 못하는 것과 화면에 표시하지 못하는 것은 다르다** — 받아서 보여주는 것은 된다. 클라이언트 안내 시 이걸 섞으면 "그럼 앱에서 못 보나"로 읽힌다.
- **★ 통합 성적표 PDF에 텍스트 레이어가 있다.** 지점 단위 통합본(반 표지 + 학생 1인 1페이지)으로 오지만 학교코드·반·번호·이름·**온라인 조회 인증코드**가 추출된다 → **학생별 분할을 우리가 한다.** 개별 파일이나 인증코드 목록을 별도로 받을 필요가 없다.
- **★ 성적표는 두 종류다** — 학부모용 상세본(8쪽: 영역별·총점별·월별 누적·지원가능대학·출결·문항별 채점표) / 학생용 개인성적표 A·B(1인 1페이지). **앱에 올릴 쪽은 미확정**(§4).
- **★ 더프리미엄은 월례고사다** — 실시일 7/16·8/18처럼 **매월** 발행된다. 그런데 0826 답변서는 **6평·9평·수능만 "직원 입력"**으로 정리했다. **월례분 입력 주체가 비어 있다**(§4). `exam_master`가 회차 마스터 기반이라 행만 넣으면 되지만, 앱 누적 성적에 포함할지가 먼저다.
- **★ 입학예약 학원코드(`acid`)는 9개뿐이다 — 대전·대구가 없다 (2026-08-30).** `md/모의고사/DSA_DLab_연동_정의서_v1.2.doc`(2026-01-30자) 기준 `분당 F · 일산 I · 동탄 T · 김포 P · 부천 C · 이매 E · 광명 J · 목동 O · 송파 A`. 개정 이력이 *"v1.2 — 송파점 학원코드 추가(ACID: 'A')"*라 **지점이 생길 때마다 대성전산이 붙여주는 구조**다. 대전(6월)·대구(7월) 오픈분이 아직 없어 **두 지점은 홈페이지 입학예약 접수가 불가**하다.
    - ⚠️ **키오스크 `acad_cd`(31~48, 11개)와 규칙이 없는 별개 체계다** — 분당이 `31`이면서 `F`이고 목동이 `45`면서 `O`다. **한쪽에서 다른 쪽을 유추할 수 없으므로 매핑을 어딘가 들고 있어야 한다.** 입학예약 연동은 아직 우리 레포에 없다(홈페이지 쪽 처리).
    - 같은 문서에서 `setStdTest`의 `subject`는 **`idx`(국어1·수학2·영어3·탐1:4·탐2:5)**로 보인다(3.6 머리말 + 3.7 샘플). 다만 그러면 **선택과목(`gm_cd`, 화작/언매 등)을 넘길 자리가 없다** — 의도인지 확인 중.
- **★ 장학 취소 기준을 "여러 행이 한 판정"으로 재설계했다 (2026-08-30, `V20260830_1000`).** 0826 기준표가 **장학 등급별로 다르고**(수능100%/수능50%/평가원50%/평가원30%), 그 안에 **OR 대안**((국+수+탐) 또는 (국+수+영))과 **AND 조건**(평가원 계열은 **영어 ≤2등급**), **탐구 1과목 / 2과목 평균** 구분이 있다. 기존 테이블은 **임계값 하나 + 과목목록 하나**뿐이라 사실상 평가원 30% 한 줄이었고 영어 조건이 빠져 있었다.
    - 추가 컬럼: `scholarship_type`(적용 장학, NULL=무관) · `alternative_group`(OR) · `exam_codes`(수능/평가원) · `elective_mode`(SINGLE/AVG2) · `extra_subject_code`+`extra_max_grade`(AND).
    - **★ 하나라도 충족하면 통과다.** 반대로 하면(하나라도 미달이면 걸림) *"유리한 쪽을 골라준다"*는 규정 취지가 뒤집힌다. 전부 미달일 때만 검토 대상이 되고 **근거에 대안 전체**를 남긴다 — 담당자가 예외를 판단하려면 "얼마나 모자랐나"를 봐야 한다.
    - **★ 등급합은 2배 스케일 정수로 다룬다.** 탐구 2과목 평균이 `3.5`처럼 정수가 아닐 수 있어 반올림해 비교하면 **경계에서 한 칸씩 어긋난다**(교습비 구간 비교를 정수 곱으로 하는 것과 같은 판단). `scholarship_review.detected_value`·`threshold`도 2배로 저장되고, 사람이 읽는 값은 `detail`에 있다.
    - **★ 장학이 없는 학생은 판정 대상이 아니다.** 취소할 것이 없고, 어느 장학인지 모르면 **어떤 기준을 적용할지도 정해지지 않는다.** 장학이 여러 건이면 **전부** 본다 — 하나만 고르면 실행할 때마다 결과가 달라진다.
    - **연도별로 바뀌는 것은 값으로 흡수된다.** 답변서가 *"해마다 탐구 1과목만 반영하기도 하고, 기준도 전년도 난이도에 따라 변경된다"*고 명시했으므로 **올해 값을 코드에 박지 않는다** — 관리자 화면에서 행만 고친다.
    - ⚠️ **여전히 `active=FALSE`다.** *"수능 기준 장학의 판정 시점"*은 설정으로 흡수되지 않아서다(§4) — 수능 성적이 12월에 나오는데 그때는 그 해 교습비 납부가 끝난 뒤라, **당해 취소·정상가 재결제**냐 **다음 해 재등록 심사용**이냐에 따라 환불 로직이 도느냐 마느냐가 갈린다.
    - ⚠️ **시드의 장학 이름(`CSAT_100`·`KICE_50` 등)은 답변서 표기를 옮긴 것이다.** `scholarship.scholarship_type`이 자유 문자열이라 **지점이 실제로 쓰는 값과 다르면 규칙이 아무에게도 안 걸린다** — 켜기 전에 대조할 것.

## 3. ★ 헷갈리기 쉬운 대전제

- **"DB는 학원 소유"의 의미는 계약/명의 문제이지, 학원 직원이 직접 DB 콘솔을 들여다본다는 뜻이 아니다.** 핵심은 "인프라 계정이 누구 명의인지, 대성전산이 접근권한/종속력을 안 갖는지"이지, 실사용 편의성(대시보드 UI 등) 문제가 아니다. 그래서 이 원칙 자체가 "반드시 Supabase여야 한다"를 의미하지 않는다 — 학원(또는 개발사가 학원을 위해 운영하는) 명의 계정 하의 RDS 등으로도 이 원칙은 동일하게 만족된다. Supabase 채택은 별도의 실용적 선택(관리형이라 운영부담 적음, 소규모에 적합한 비용)이었을 뿐, 원칙상 고정된 요구사항은 아니다.
- 대성전산 API는 "조회"만 하는 대상이지, 이 시스템이 거기 종속되는 게 아니다. 반대로 착각해서 대성전산 스키마를 그대로 복제하지 말 것.
- **★ 키오스크는 Webhook을 쏘는 게 아니라, 우리를 호출하는 클라이언트다.** 대성전산을 거치지 않는다는 건 맞지만 방향을 반대로 알고 있었다 — 키오스크는 `POST /kiosk/setAttendStd` 같은 엔드포인트를 **호출하고 응답을 기다린다**. 그래서:
    - **동기 응답이어야 한다.** `att_gn`(등원/하원/지각…)을 즉시 돌려줘야 키오스크 화면에 결과가 뜬다. "받고 202 던지고 비동기 처리"는 불가
    - **왕복이 있다.** `code 113`(시간표 없어 자동판별 불가)이면 키오스크가 사용자에게 하원/외출 선택지를 띄우고 `con_gn`을 실어 **재호출**한다. 단 서버는 그 사이 상태를 들고 있지 않는다(stateless 2-phase)
    - **경로에 `/api/v1` prefix를 붙이지 않는다.** 키오스크가 `/auth/**`·`/kiosk/**`를 하드코딩하고 있다 — §5 경로 규칙의 명시적 예외
    - 따라서 이건 `integration/`(우리가 외부를 호출)이 아니라 **우리가 제공하는 API 구획**이다
    - 상세 계약·주의사항은 `docs/dsa-compat.md`. **응답 형식을 "예쁘게" 고치면 키오스크가 깨진다**(§7 참고)
- **★ 우리 장애가 키오스크에서는 안 보인다 — 별도 모니터링이 필수다.** 키오스크 백엔드는 DSA 장애에 대비해 **관대한 폴백**을 갖고 있다: `getMealApplyYN`이 실패하면 **전부 허용**(급식 신청 안 한 학생도 통과), `att_gn`이 없으면 로컬 판별로 폴백. 우리가 DSA 자리에 들어가면 **이 폴백이 우리 서버 장애를 조용히 가린다** — 서버가 죽어도 키오스크 화면은 정상으로 보이고 급식이 다 뚫린다. **키오스크를 손대지 않기로 했으므로 이 폴백은 제거할 수 없다.** 따라서 `/kiosk/**` 엔드포인트의 에러율·응답시간을 **우리 쪽에서 독립적으로 감시**해야 한다. 키오스크는 장애를 알려주지 않는다.
- **출결 태깅 수단은 RFID 카드와 QR 두 가지지만, 서버에서는 구분되지 않는다.** 앱이 표시하는 출결 QR에는 **그 학생의 `rfid_no`를 담는다**(카드 미소지자용 대체 수단). 키오스크가 QR을 스캔해 `rfid_no`를 얻고 카드와 **동일하게 `POST /kiosk/setAttendStd`를 호출**한다 → **DSA 계약 변경도, 별도 엔드포인트도 없다.** 출결 로직에 "QR인지 카드인지" 분기를 만들지 말 것. (`rfid_no`가 노출되므로 캡처 전달에 의한 대리출석 여지는 있다. ~~TTL·회전 토큰을 얹으면 키오스크 수정이 필요해져 "키오스크 무수정" 전제와 상충한다~~ → **2026-08-18 정정: 키오스크가 당사 개발이라 상충하지 않는다**(§2). 대리출석을 막아야 하면 단말 검증부를 고쳐 동적 토큰으로 간다.)
- **출결 상태는 7종이다** — `S`등원 `T`하원 `A`지각 `D`외출 `N`사유외출 `C`조퇴 `R`복귀. 요구사항정의서 2시트의 5종(`ON_TIME/LATE/ABSENT/OUT/EXCUSED`)은 **틀렸다** — 하원·복귀가 빠져 있고, 하원 없이는 순공시간 계산이 불가능하다. 그리고 **`ABSENT`(결석)는 태깅 이벤트가 아니다** — "안 찍은 것"이라 로그에 남을 수 없고, 배치 스케줄러가 일자 단위로 확정하는 **상태값**이다. 이 둘을 같은 enum에 섞지 말 것.
- **키오스크는 하드웨어 입력 창구 중 하나일 뿐, 로직·데이터는 전부 백엔드/DB(RDS)에 있다.** 앱·관리자 웹은 키오스크가 만든 데이터를 **조회**한다. 한때 "자리이탈은 앱도 쓰기 가능"으로 뒤집혔다가 **0803에 다시 조회 전용으로 확정**됐다(§2 결정로그) — 앱 쓰기 경로는 현재 없다. "키오스크가 하는 건 앱에서도 볼 수 있어야 한다"가 기본값이고, 앱이 쓰기도 하는지는 **기능마다 시트로 확인**할 것. 이 항목만 세 번 뒤집혔으니 특히 조심.
- **학번은 매년 초기화**된다. 학번을 학생 PK나 외부 연동 키로 쓰면 다음 해에 깨진다. 내부 고유 ID를 별도로 둘 것.
- **★ 학생은 한 테이블이 아니라 "사람 + 등록 건" 2단이다.** 레거시 실측(`TB_STUDENT_MST` / `TB_PDTL_RLS`)에서 확인됐고, 이 구조가 여러 문제를 한 번에 푼다:
    - **학번(`STD_NO`)과 RFID 카드번호(`RFID_NO`)가 사람이 아니라 "등록 건"에 붙는다.** 그래서 학번 매년 초기화가 구조적으로 보장되고, 카드가 다음 기수에 재사용돼도 과거 기수 출결이 섞이지 않는다
    - 여기는 **독학재수라 1년 단위 코호트**다. 기수가 통째로 갈리므로 연도를 넘는 학생 연속성을 가정하지 말 것. 대신 **삼수로 재등록하면 같은 사람에 등록 행만 추가**되므로 동일인 추적이 공짜로 된다(시안의 "상담 이력은 입학예약 상담부터 연속 기록"이 이걸로 성립)
    - **`RFID_NO`는 UNIQUE가 아니다**(이력이 쌓이므로). 현재 유효한 카드는 `STAT_GB='T'` 같은 현재-플래그로 걸러야 한다 — 이걸 빠뜨리면 퇴원생 카드로 태깅이 통과한다
    - 컬럼별 설계 근거는 `docs/schema-guide.md`
- **수납·급식 결제는 전부 "대성 PG"를 통해 이 시스템이 직접 소유·처리한다.** (원 기획서 "수납=대성전산 소유" 확정사항은 뒤집힘 — §2 결정로그 참고) 대성전산은 더 이상 수납의 진실의 원천이 아니다. **대성 PG는 대성전산과 무관한 별도 계열사**이고, **급식업체가 원래 쓰던 PG가 바로 이 대성 PG였다** — 즉 신규 벤더 선정이 아니라 기존 것을 수납까지 확장해서 쓰는 것이고, 요구사항정의서 E-3("급식업체 기존 PG 사용")과 충돌하지 않는다. 결제 코드/스키마를 수납·급식 두 곳에 따로 만들지 말고 **공용 결제 도메인**으로 설계할 것. (레거시도 청구 상품구분 `RES_GB`에 `E:식대`가 이미 있어 원래 같은 청구 체계였다.)
- **방화벽 해제 승인은 단순 "먼저 승인한 쪽이 이김" 레이스가 아니라, 타임아웃 기반 에스컬레이션 모델이다** (#40 / I-20, I-12 참고). 신청 시 학부모+**담당선생님(사감)**(관리자 웹)에게 동시에 알림이 가지만, 실제로는 시점에 따라 3가지 케이스로 갈린다:
    1. 타임아웃(**10분으로 확정**) **전에 학부모가 승인** → 정상 승인, 양쪽에 승인 완료 알림
    2. 타임아웃이 **지나도록 학부모 무응답** → 담당선생님이 승인(에스컬레이션) → 학부모 쪽엔 **"승인 시간이 지나 담임이 승인했습니다"**
    3. 타임아웃이 **안 지났는데** 학생이 재촉해서 담당선생님이 먼저 승인 → 학부모 쪽엔 **"승인 시간이 남았지만 담임이 먼저 승인 처리했습니다"** — 2번과 다른 문구, 절대 같은 메시지로 통일하지 말 것 (학부모가 "왜 시간 남았는데 담임이 승인했지" 혼란 겪음)
       그리고 여전히 **동시성 이슈는 남아있다** — 학부모 승인과 담당선생님 승인이 정확히 같은 순간 들어올 수 있으므로, 실제 상태 전이(승인 확정)는 원자적으로 처리하고, 위 1/2/3 중 어떤 케이스인지는 **승인 시각을 신청시각+타임아웃과 비교**해서 판별한다. 단순히 두 엔드포인트를 나눠 만들고 끝내면 안 됨.
       **담당선생님(사감)이 누구인지는 "반배정"에서 자동으로 나온다** — 반 엔티티에 담임 필드를 두고, 학생을 그 반에 배정하면 담당선생님이 자동 결정됨(§2 결정로그). 그래서 "학생별로 승인자를 따로 지정하는 화면"은 만들지 않는다.
- **회원가입은 학생과 학부모가 완전히 다른 흐름이다.** 학생은 **승인제**(휴대폰인증+내신입력 → 관리자 승인 전엔 앱 접근 자체가 완전히 막힘, 중간 상태 없음), 학부모는 **비승인제**(휴대폰인증+학생 고유ID만 맞으면 즉시 가입). 이 둘을 같은 흐름으로 착각해서 설계하지 말 것. 학생 고유ID는 마이페이지에 상시 노출되는 값이고, 학부모-자녀 관계 자체는 검증하지 않는다(그 ID를 아는 사람이면 누구나 연결 가능) — 이건 클라이언트가 인지하고 감수한 부분이니 추가 검증 로직을 임의로 만들지 말 것.
- **학부모 계정은 자녀 수만큼 나누지 않는다** — 계정 1개 + 여러 자녀를 연결하는 구조(로그인 1번 + 앱 내 자녀전환 UI)로 확정(§2 결정로그). 완전히 분리된 별도 계정으로 설계하지 말 것.
- **모든 학생 관련 알림 템플릿에는 학생명 변수가 필수**다. 다자녀 학부모가 "이거 누구 얘기지" 헷갈리지 않게 하기 위함 — 템플릿 하나라도 이 변수 빠뜨리면 안 됨.
- **미등원 알림은 지금까지의 로직과 반대 방향이다.** 대부분의 로직(출결, 자리이탈, 방화벽 등)은 "키오스크 이벤트가 발생하면 반응"하는 구조인데, 미등원 알림은 **"이벤트가 없었음"을 감지**해야 하는 유일한 유형이다 — 등원시간(지점 공통) 정각에 스케줄러(배치)가 돌아서, 그 시각까지 출결 태깅 기록이 없는 학생만 걸러 카카오 알림톡을 보낸다. 사전에 결석 사유를 제출한 학생은 제외(무단결석만 대상). Webhook 리스너만으로는 절대 구현 못 하는 부분이니, 별도 스케줄러 컴포넌트가 필요하다는 걸 잊지 말 것.
- **채팅은 SDK 사용으로 확정됐다 (자체구현 폐기, §2 결정로그).** 다만 **어떤 SDK**를 쓸지는 아직 미정 — 후보 여러 개를 우리 팀이 직접 프로토타입으로 만들어 클라이언트에 전달하고, 클라이언트가 최종 선택한다. SDK를 쓰더라도 메시지 원본은 우리 DB에 동기화해야 한다는 "학원 소유 DB" 원칙은 그대로 유지 — Webhook 동기화 설계 필요(§4 블로커). 프로토타입 제작은 §6 개발원칙(연동은 핵심 도메인 완성 후)에 따라 순서를 지킬 것.
- **다지점 구조**: 지점 간 데이터는 독립적이되, 상위 관리자는 전 지점을 조회할 수 있어야 한다. 엔티티에 branch_id류 컬럼을 빠뜨리기 쉬우니 신규 테이블 만들 때마다 체크할 것.
- **인프라는 서버 하나를 개발용→운영용으로 그대로 전환하는 방식이다(별도 dev/prod 서버 분리 아님).** 그래서 이름(`dev`)이 주는 "막 다뤄도 됨"이라는 착각이 실제 사고로 이어질 위험이 있다 — 개발 중인 이 DB에는 실제 학생·학부모 개인정보를 절대 넣지 않고 테스트 더미데이터만 사용할 것. 실사용 전환(컷오버)은 이름만 바꾸는 게 아니라 §4 "컷오버 체크리스트"를 거쳐 의식적으로 수행한다.

## 4. ⚠️ 착수 전 블로커 / 미확정 사항

아래가 확정되기 전에는 관련 기능의 화면·API를 확정 설계하지 말 것 (원칙: §6 참고). 작업 중 이 항목을 마주치면 임시 가정으로 넘어가지 말고 먼저 확인한다.

- **채팅 SDK 최종 선택 미정**: SDK 사용 자체는 확정(§2 결정로그, §3 대전제) — Sendbird 등 후보 여러 개를 프로토타입으로 만들어 클라이언트에 전달, 최종 선택 대기. **SDK를 쓰므로 메시지 원본을 우리 DB로 가져오는 Webhook 동기화 설계가 반드시 필요하다**("학원 소유 DB" 원칙, §3). `domain/chat`과 `integration/<벤더명>/` 실제 구현 착수는 이 선택 이후 — 벤더가 안 정해졌으니 패키지명을 미리 박지 말 것.
- **결제 — 구조는 확정, 법적요건·잔여 스펙만 남음** (0803): **디랩 자체 명의 PG 직접결제로 확정**(D-9·D-3·D-6 해소)돼 스키마 설계를 막지 않는다. 남은 것은 ①전자금융거래법·PG 등록·보증보험 ②디랩 PG 가맹점정보(MID)·카드/가상계좌 API 스펙·테스트 가맹점·정산주기 **내부 확보**(E-3, 외부 수급 아님) ~~③환불 일할계산 산식(I-26)~~ → **2026-08-20 교습비·환불 규정 수령으로 해소**(§2). ④현금영수증 발급 채널·교육비 납입증명서(I-25) — 다만 **지점별 학원·독서실 등록번호는 확보**됐다(신고 현황 탭).
- **★ 교습비·환불 규정 후속 미확정 (2026-08-20 질의 발송, 답변 대기)**. 전부 §2 결정로그 항목의 후속이다.
    - ~~대전·대구 `acad_cd`~~ → **2026-08-26 확정: 대전 47 · 대구 48** (§2). 수내=분당, 동탄2관=동탄 별관도 확인됐다.
    - ~~원 단위 절사~~ → **"소수점 절사" = 버림 확정**(2026-08-26). 우리 기본값이 `Rounding.FLOOR`라 코드 변경 없다. 대구 2칸은 오타로 확인됐다.
    - ~~월별 일수 기준~~ → **확정: 설날·추석 *당일*만 수업일수에서 제외**하고 매년 그렇게 한다(2026-08-26). 다른 법정공휴일은 운영한다. **음력이라 날짜는 코드로 계산하지 말고 데이터로 넣는다**(§7 휴일 원칙). 지금은 월별 일수를 직접 입력받는데, 휴원일 등록 기반 자동 산출로 개선 가능하다.
    - ~~할인 소급 재결제 판정 기준~~ → **확정(2026-08-26): 퇴원하는 달이 할인 기간 안이면 그동안 받은 할인을 전부 정상가로 재결제하고, 할인이 끝나고 정상가로 다니다 퇴원하면 소급이 없다.** 환불 계산기가 이미 "정상가 차감" 구조라 그 위에 얹으면 된다.
    - ~~급식 제3자 제공 동의 범위~~ → **클라이언트가 동의 문구에 "환불계좌 전달" 항목을 추가하기로 했다**(2026-08-26). 문구를 받으면 **새 버전으로 등록**한다 — 아직 아무도 동의하지 않은 지금이 싼 시점이다(§2).
    - ~~급식 취소 무제한 환불~~ → **확정: 앱은 D-3까지, 데스크는 기한 제한 없이**(2026-08-26). **지금 동작 그대로다 — 고치지 말 것.**
    - **급식 업체 자동 발송** → 수단·시점은 **확정: 알림톡, 취소 즉시**(2026-08-26). 동의 문구에 "환불계좌 전달"을 **클라이언트가 추가하기로** 했다. **남은 것은 수신처 5곳**과 ⚠️ **E-5(알림톡 발신프로필·템플릿 심사)** — 업체는 사업자라 학생·학부모용과 **별도 템플릿 심사**가 필요할 것이다.
    - ~~급식 신청중단 경고 알림 대상·시점~~ → **확정: 학생·학부모 둘 다에게, 철회하는 순간**(2026-08-26). 발송 자체는 E-5·E-7 대기다.
    - ~~추가결제 상세 내역 출력 형태~~ → **확정(2026-08-26): 결제항목·금액·결제기간을 결제일자순으로 모두 조회하고 한꺼번에 출력**. 결제 내역을 묻는 학부모가 많아서다.
    - ~~장학 퇴원 처리 선택 주체~~ → **확정: 데스크에서 퇴원 처리할 때 건별로 선택**(2026-08-26). 지점 기본값을 두는 방식이 아니다.
    - ~~재학생 1~2월 운영 여부~~ → **확정: 윈터스쿨로 운영하고 750,000원으로 동일**(2026-08-26). 표에 따로 안 적혀 있던 이유다. ⚠️ **교습비/독서실비 구성이 N수와 같은지(660,000+90,000)는 확인 중** — 환불 산식이 갈려서 나뉘어 있어야 한다.
    - ~~재학생 490,000 지점 범위·1인실 지점~~ → **확정: 대전·대구도 490,000, 1인실은 분당·목동·대구 3곳뿐**(2026-08-26).
- ~~**내신 성적 입력 양식 미정**~~ → **입학서류 신상기록부 3종으로 해소** (2026-08-20, §2 결정로그 참고).
- **★ 0821 확인서 회신 수령 (2026-08-26) — 「각종 제출 기능」만 남고 나머지는 정해졌다.**
    - **장학 취소 자동판정** — 우리가 제안한 *"자동으로 판정하되 「검토 대상」으로 띄우고 최종 확정은 사람"* 방식이 **승인됐다.** 벌점 40점을 상벌점과 한 곳에서만 관리하는 것도 확인됐다.
    - **급식 동의 시점** — *"신청 흐름 안에서 동의를 받는다"*로 확정. **이미 그렇게 되어 있다**(`MEAL_THIRD_PARTY_CONSENT_REQUIRED` 게이트) — 별도 화면을 만들지 말 것.
    - **홈페이지 합격자 노출은 범위 밖**으로 회신했고, **대학/학과/전형 DB 업로드 대신 "학생 입력 + 직원 정리 → 다음 학생부터 자동완성"**을 제안해 둔 상태다.
    - **동의서는 수정이 아니라 버전 등록**으로 회신했다. 필수/선택이 나뉘는 동의서는 **동의 단위를 나눠 등록**한다 — 하나로 두면 선택 항목만 철회할 수 없다.
    - **장학 동의서가 지점별·종류별로 다를 수 있다**고 확인됐다. 지금 전 지점 공통 구조라 **지점별로 열어야 한다**(약관 `academy_id` 규약을 그대로 쓰면 된다).
    - ⚠️ **원생마스터 파일을 받아야 한다** — 명단 조회·출력을 엑셀 대체로 만들기로 했고, 클라이언트가 *"전달드렸었는데 없으시면 다시 보내겠다"*고 했다.
- **★ 모의고사 성적 후속 미확정 (2026-08-31 회신 반영)**. 자동조회 자체는 해소됐다(§2) — 남은 것은 아래다.
    - **★★ 출결·담임·반을 대성전산에 넘길 경로가 없다 — 우리가 규격을 제안해야 한다.** 지금은 대성전산이 DSA에서 직접 수집하는데 **컷오버로 그 경로가 끊기고**, 키오스크 25개 계약에도 반·담임·출결이 없다. ⚠️ **상대도 흐름을 모른다**(*"설명해 줄 수 있는 분이 없다"*)라 질의로는 안 닫힌다. **안 하면 전환 직후 성적표에 출결이 빠진다.**
    - **★ 컷오버 후 성적표 PDF 수급 경로** — 지금은 대성전산이 디멤버에 직접 올린다. 우리 시스템에는 안 올려준다. 지점이 통합본을 갖고 있으니 직원 업로드가 현실적이나 확인이 필요하다.
    - **`examdt` 연간 시행일 목록** — 필수 파라미터인데 회차 조회 수단이 없다. 성적표 실시일로 일부 유추는 되지만 목록이 필요하다.
    - **N수생 `grade` 값** · **`idxSeq` 유효범위**(연도를 넘어도 같은 값인지) · **개발서버 오픈**(9월말 예정, 앱 11/30 심사 제출 기준 촉박).
    - **월례고사 입력 주체** — 성적표는 매월 나오는데 0826 답변서는 6평·9평·수능만 정했다. ⚠️ 자동조회가 되면서 성격이 바뀌었다 — *"누가 올리나"*가 아니라 **어느 회차를 앱 누적 성적에 넣느냐**의 문제다.
    - **앱에 게시할 성적표 종류** — 학부모용 8쪽 상세본 / 학생용 A·B 1페이지.
    - ~~구체제 과목코드 전체 목록~~ → **`/api/students/grade`가 과목명까지 주므로 불필요해졌다.** `/subject`(답안)를 쓸 일이 생기면 그때 받는다.
    - ~~엑셀 업로드 전환 승인~~ → **철회.** 자동조회가 되므로 제안 자체가 없어졌다.
- **★ 장학 취소 — 수능 기준 판정 시점 미확정 (2026-08-30 질의)**. 탐구 1과목/2과목 평균과 영어 등급 조건은 **규칙 데이터로 흡수**하므로 답을 기다리지 않는다(§2). 그러나 **수능 기준 장학(100%·50%)을 언제 판정하는지**는 정책이라 흡수되지 않는다 — 당해 취소·정상가 재결제냐 다음 해 재등록 심사용이냐에 따라 **환불 계산이 도느냐 마느냐가 갈린다.**
- **★ 대전·대구 입학예약 학원코드(`acid`) 미배정 (2026-08-30 요청)**. 배정 전까지 두 지점은 홈페이지 입학예약 접수가 불가하다. 상세는 §2.
- **★ 성적 입력 양식이 문서 두 곳에서 다르다 (0821 확인서, 답변 대기)**. 어느 쪽이 정본인지 확인 전까지 **양식 seed를 늘리지 말 것**.
    - **신상기록부(0814)** — 예비고3용이 따로 있고 **통합사회·통합과학** + **6·9·10월 학력평가**다(신 체제).
    - **0821 확인서** — **국어 화작/언매 · 수학 확통/미적/기하 · 사탐 8 + 과탐 8 택2**에 회차가 **6평·9평·수능가채점·수능 4종**이다(구 체제). **10월 학평도 통합사회·통합과학도 없다** → N수·현고3 대상으로 보인다.
    - **재학생 성적을 받는지부터 확인해야 한다** — 학년에 따라 입력 화면이 통째로 갈린다.
    - 0821에서 새로 나온 것 중 **스키마에 없는 것 셋**: ①**선택과목**(국어·수학·탐구) ②**가채점 원점수**(표준점수·백분위·등급이 아니다) ③**성적표 첨부파일**. 나머지(회차 추가, 영어 등급만)는 마스터 데이터라 행만 고치면 된다.
    - **★ 입력 주체 확정 — 학생이 앱에서 직접 입력한다 (2026-08-26 회신).** ~~정식은 직원(관리자 웹 단건 + 엑셀 일괄)~~ → **그건 우리가 제안한 안이고 채택되지 않았다.** 회신 원문: *"입학시에는 성적을 **직접입력**하고"*, *"**처음 입력시 학생, 이후 수정시에는 직원을 통해서** 가능하도록"*.
        - 우리가 직원 입력을 제안했던 이유(*성적통지표가 가로로 넓은 표라 세로 화면에서 오입력이 는다*)는 **기록으로만 남긴다.** 대신 **수정에 직원 검증이 걸리는 것**으로 그 위험을 흡수한다.
        - ⚠️ **장학 취소 판정이 이 등급을 근거로 돈다.** 학생 입력값이 판정에 쓰이므로, 장학 신청 시에는 **성적표 원본 제출이 필수**다(아래).
    - **★ 함께 대기 중이던 4건도 같은 회신에서 해소됐다.**
        - **탐구 사탐+과탐 교차** → **2026년까지 교차 가능. 2027년부터 통합사회·통합과학이 필수라 택1이 불가**해진다
        - **제2외국어/한문** → **입력받지 않아도 된다**(반영 대학이 있으나 비율·효용이 미비)
        - **가채점 원점수를 등급 환산할지** → **환산하지 않는다. 원점수 그대로 보관·표시**한다. 수능 이후 면접·논술 응시 판단 기준으로 쓴다. → **"등급컷 출처가 필요하다"는 문제가 통째로 사라졌다**
        - **6·9평을 더프리미엄에서 받아올 수 있나** → **못 받아온다.** 더프는 평가원 성적을 주지 않고, DSA에 입력된 평가원 성적을 더프 성적과 **누적해서 보여주는** 구조다
    - **★ 장학 신청 시 성적표 원본 제출이 필수다.** 수능성적표는 문서번호로 원본 확인이 되지만 **평가원 성적표는 문서번호가 없어**, 학생이 평가원에 신청해 **학원 팩스로 원본이 온다** — 즉 평가원 성적표는 **학생이 앱에서 첨부하는 대상이 아니다.** 문서번호 입력란 대신 **"직원의 원본 확인 체크"**를 둔다.
    - ⚠️ **평가원 성적은 두 번 처리된다** — 시행 당일 학원에서 한 번, 이후 평가원 성적이 나오면 다시. **시행 당일 처리분은 학생에게 표시하지 않고 선생님에게만** 보인다(정식 성적과 어긋날 수 있어서다).
    - **스키마에 없는 것 둘**: **선택과목**(국어 화작/언매 · 수학 확통/미적/기하 · 탐구 택2)과 **가채점 원점수**. 회차 4종(6평·9평·수능가채점·수능)은 `exam_master` 행만 넣으면 된다.
- **★ 첨부파일 보관 정책 미정 (0821, 답변 대기)**. 성적표 원본·합격증에 **성명·생년월일·수험번호**가 그대로 있다. **열람 범위**(담임만/행정 포함/학부모)와 **보관 기간**(개인정보 동의서의 "퇴원 후 3년"과 같은지)이 정해져야 한다. ⚠️ **파일 업로드·보관 기능 자체가 아직 없다** — 확정되면 별도로 붙여야 한다. 성적표는 **지점 통합 PDF**로 오지만 텍스트 레이어가 있어 학생별 분할은 우리가 한다(§2).
- **★ 수시/정시 지원대학 (0821, 신규)**. 수시 6개·정시 3개 입력란에 전형명·대학명·학과 + **합격증 첨부**, 학생 입력 → **직원이 합불 확인**. 클라이언트는 *"사전 대학/학과/전형 DB 업로드"*를 요구했으나 **전국 대학 전형은 매년 바뀌어 갱신 부담이 크다** — 학생이 직접 입력하고 직원이 정리한 명칭이 **다음 학생부터 자동완성**으로 쌓이는 방식을 제안해 둔 상태다. *"홈페이지 노출"*은 **범위 밖**으로 회신했다(우리는 데이터 관리·조회까지).
- **명단 구분 항목**: 기획서 4.9에 "현재 구분 항목 불명확, 운영팀과 재정의 필요"로 명시됨 — 해당 화면 착수 게이트. (노션 "D.Lab 확인 필요 목록"에 상세 정리됨, 우선순위 높음 — 기획서에서 직접 "!!!" 강조)
- **알림 템플릿 문구 설계**: 기획서 3.13 "템플릿 설계를 위해 추후 논의 필요" — 채널 구분(카카오 알림톡 vs FCM Push), 방화벽 3케이스별 문구, 학생명 변수 반영 등 요구사항은 늘어난 상태(§2/§3 참고)이나 정확한 문구 자체는 여전히 미정. 카카오 알림톡은 사전심사가 필요해 조기 확정이 유리 — 알림 발송 로직 확정 전 선행.
- **자리이탈 (I-16)**: **앱 신청 경로는 없다(조회 전용)로 0803 확정** — 두 채널 충돌 규칙 문제는 소멸했다. 남은 미확정은 **위치 구분값 마스터, 좌석표 실시간 반영 방식, 장시간 미복귀 임계값, 키오스크(잔여 6대)+태블릿 배치**다. 구역·좌표(`area_cd`/`xpos`/`ypos`) 레거시 스키마도 미확보라 관련 엔드포인트 4개가 막혀 있다. 0723에 개발팀이 실시간 좌석표 UI의 2차 개발 이관을 제안한 상태라 **스코프 자체가 아직 유동적**이다.
- **레거시 스키마 4종 미확보 (출결·좌석/구역·지점·사유신청)**: 7/22 요청서에 대해 2026-07-31 회신으로 **27개 테이블을 받았고 원생 마스터도 확보**했다(§2 결정로그). 그러나 아래 4종이 빠져 있고 추가 제공 협의가 난항 중이다.
    - **출결** — DSA 호환 엔드포인트 10개가 여기 걸려 있어 **가장 급하다**
    - **좌석/구역** — 학생 쪽 배정(`STRM_*`)은 확보했으나 구역·좌표(`area_cd`/`xpos`/`ypos`)가 없어 엔드포인트 4개가 막힌다
    - **지점** — 테이블명은 확보됨: **`DB_INFO.TB_ACADEMY_INFO`**(받은 원생MST의 `ACAD_CD` 주석이 직접 지목)
    - **사유신청** — `reg_gn` 값 목록 미상
    **단, 이건 착수를 막는 블로커가 아니다.** DSA 호환 API 계약(`docs/dsa-compat.md`)에서 필요한 필드를 역산할 수 있어 우리가 직접 설계한다(`docs/schema-guide.md` §3). 받으면 교차검증용으로만 쓴다. 실제로 반드시 필요한 시점은 **컷오버 직전 기존 재원생 데이터 이관** 때다 — 그때는 스키마를 몰라도 데이터를 받아야 하므로 협의는 계속 진행할 것.
- **인증번호 발송 수단 미정 (SMS 업체 / 알림톡).** 본인인증 방식 자체는 휴대폰 인증으로 확정됐으나(§2 결정로그), **누가 문자를 보내는지**가 안 정해졌다. 현재 `LoggingSmsSender`가 로그로만 남긴다 — **운영에 이대로 올라가면 아무에게도 문자가 안 가는데 API는 200을 준다**(기동 시 경고 로그로 표시해뒀다). E-5(알림톡 발신프로필·사전심사)와 같이 정리할 항목이다.
- **비밀번호 정책 상세 미상 (NF-05·NF-06 참조가 끊겨 있다).** 앱 요구사항 A-1이 *"비밀번호 정책·로그인 실패 5회 계정 잠금(4시트 NF-05·NF-06)"*을 참조하는데 **NF-05·NF-06이 요구사항정의서·실행가이드 어느 탭에도 없다**(2026-08-05 전수 검색). 확정된 것은 **실패 5회**(A-1 본문)와 **잠금 해제는 관리자**(F-4.12-1)뿐이다. 나머지(자릿수·조합·만료 주기)는 `common/security/PasswordPolicy`에 잠정값으로 두고 한 파일에 모아뒀다 — 확정되면 그 상수만 바꾼다.
- **★ E-7 (신규·최우선) — Firebase FCM 프로젝트 + iOS APNs 인증키(.p8), dev/stg/prod 3환경 분리**: 실행가이드가 **Phase 0 착수 블로커**로 잡았다. **앱 스토어 심사는 12월 단 한 번의 기회**이고, 이것과 S-5(스토어 계정 위임)가 밀리면 **앱 Phase 전체가 빌드 불가 상태로 지연**된다 — 리드타임이 곧 마감이라 지금 착수해야 한다.
    - **일정 마일스톤(실행가이드 원문)**: **`11/30 개발 완료·심사 제출` → 12월 심사 대기 → 승인 후 사용자 QA.** 클라이언트와 얘기할 때 "12월 심사"보다 **11/30**을 쓰는 게 구체적이다.
    - **"단 한 번"은 문자 그대로 심사 횟수가 아니다.** 같은 시트가 *"12월 심사 반려 시 QA·컷오버 연쇄 지연"*을 상정하고 **11월 중 TestFlight/내부테스트로 완충구간 확보**를 권한다. 일정에 여유가 없다는 뜻으로 읽을 것.
    - **진짜 데드라인은 D-8이다** — 대성전산 DSA 계약종료. *"2027년 신규기수 개원 전 전환이 사실상 데드라인"*(실행가이드).
    - **★ 계정은 "새로 만들어 받는" 게 아니라 "기존 계정에 초대받는" 것이다.** 클라이언트에 Apple Developer 계정이 이미 있다(2026-08-24 확인). 계정을 통째로 넘겨받으면 **2단계 인증 코드가 그쪽 담당자 폰으로 가서** 빌드할 때마다 연락해야 한다. Apple은 *사용자 및 액세스*에서 `App Manager` 초대, Firebase는 프로젝트 멤버 추가가 맞다. 소유는 학원에 남고 인수인계 때 빼기만 하면 된다.
    - **APNs 인증키(.p8)만은 파일로 받아야 한다** — 초대로는 안 된다.
    - **서버 쪽은 이미 준비돼 있다** — `PushToken`(토큰 등록)·알림 수신 설정·템플릿/이벤트 매핑·`NotificationSender` 인터페이스까지 있다. **남은 건 구현체 하나**이고 자격증명 없이는 검증이 안 되므로 **미리 짜지 말 것.**
- **★ 앱 동적 QR 태깅 방식 미확정 (D-2 종속)**: 앱 요구사항 `A-4`가 "**동적** QR 태깅"이고 D-2에 "앱 동적 QR 토큰 검증 방식 추가 협의 필요"가 붙었다. **`docs/roadmap.md`는 "QR에 `rfid_no`를 담아 카드와 동일 처리, DSA 계약 변경 없음"으로 단정했는데 시트는 아직 미확정**이라 어긋난다. 실행가이드 지침은 "**확정 전까지 QR 발급 인터페이스만 구현하고 검증 로직은 서버 목업**". 출결 착수 전 반드시 확인할 것.
    → **2026-08-18: 기술 제약은 사라졌다.** 키오스크가 당사 개발이라 단말 검증부를 고칠 수 있다(§2) — *"동적으로 가면 키오스크를 못 고쳐서 안 된다"*는 이유가 없어졌다. **남은 것은 "동적으로 갈 것인가"라는 결정 하나**이고, 그건 대리출석을 어디까지 막을 것인가의 문제다. 협의 대기가 아니라 우리가 제안해서 닫을 수 있는 항목으로 옮긴다.
- **신규 미확정 (0803)**: I-23 진도 입력 주체·표시 방식(보류) · I-24 앱 온보딩 OT 게이트 범위 · I-25 현금영수증·납입증명서 · ~~I-26 환불 일할계산 산식~~(0820 해소) · I-27 정기일정 관리자 등록분과 앱 등록분의 병합·중복 규칙.
- **공휴일 등록 주체·시점 미정 (요구사항 누락)**: 문서 전체에서 공휴일은 *"주말+공휴일 제외"*라는 **규칙으로만** 등장하고, **누가·언제·어떻게 넣는지 정의가 없다.** 기초관리(F-4.10-1) 항목에도 공휴일이 빠져 있어 화면 근거가 없다. 개발문서 Phase 0 체크리스트의 *"☐ 공휴일 데이터 소스 확보"*가 유일한 단서다. **일단 수기 입력으로 진행**하기로 하고 `/api/v1/admin/holidays` CRUD를 만들어뒀다(등록 권한 규칙은 §7 참고). 연 15~20건을 매년 손으로 넣어야 하므로, 운영 부담이 확인되면 **엑셀 일괄 등록**이나 공공데이터포털 특일정보 API 적재를 검토할 것.
- **★ `getStdInfoList`에 대기생이 빠져 있다 (D-7·S-1 종속, 신규)**: 연동규격서는 *"재원생과 **대기생** 전체 리스트"*를 내리라고 하는데 우리는 **재원생만** 내린다. 대기자 도메인(F-4.2-1)이 대성전산 입학예약폼(D-7)·연동방식(S-1) 대기라 만들 수 없기 때문이다.
    - **지금은 무해하다** — 대기생은 카드(`rfid_no`)가 없어 태깅 대상이 아니고, 키오스크는 `rfid_no`·`std_nm`이 없는 행을 조용히 버린다.
    - **대기자 도메인 착수 시 이 계약을 같이 고쳐야 한다.** 안 그러면 키오스크 학생 동기화에 대기생이 영영 안 들어온다. 대기생에게 카드를 발급하는 운영이 실제로 있는지(입학 전 시설 이용 등)도 그때 확인할 것.
- **DSA 호환 폴백 정책 미정**: 기존 키오스크는 DSA 장애 대비 관대한 폴백을 갖고 있다(`getMealApplyYN` 실패 시 **전부 허용**, `att_gn` 없으면 로컬 판별). 우리가 DSA를 대체하면 **이 폴백이 우리 서버 장애를 가려버린다**(급식 무단 허용이 조용히 발생). 살릴지 죽일지 결정 필요.
    - **로컬 E2E로 실증됨(2026-08-05)**: `getMealApplyYN` 미구현 상태에서 키오스크는 에러 없이 전원 통과시켰고, `getReceiptInfo` 500은 빈 배열로 폴백해 화면만 비었다. **에러율 감시가 없으면 둘 다 안 보인다.**

### 해소된 블로커 (기록용, §2 결정로그에 근거 있음)

- ~~키오스크 응답값 2종 미상(`state`·`reg_gn`)~~ → **연동규격서 수령으로 확정**(2026-08-05). `state`는 4값(`S`등원 `D`외출 `N`미출석 `B`공석), `reg_gn`은 7값(휴가권 1 … 사유외출 7). 상세는 `docs/dsa-compat.md` 머리말
- ~~순공시간 산출 정의(I-6)~~ → **재실 − 외출 − 급식 − 쉬는시간**으로 확정(2026-08-05). 급식·쉬는시간은 시각 상수가 아니라 `period_master.period_type`(MEAL/BREAK)으로 가른다

- ~~방화벽 담당 교사 매핑~~ → 담당선생님(사감), 반배정 시 자동 연동으로 해소
- ~~방화벽 승인 타임아웃 값~~ → 10분으로 확정
- ~~승인류 기능 사전지정 UI 여부~~ → 반별 담임 지정 화면으로 대체, 별도 UI 불필요
- ~~상담 양식~~ → 목업 존재, 그대로 활용
- ~~급식 PG 확인~~ → 급식업체 PG 연동 자체가 폐기되고 디랩 자체 PG로 단일화(0803, E-3 재정의)
- ~~등록비 결제 주체(D-9)·수납 API(D-3)·급식 결제 수납반영(D-6)~~ → 디랩 자체 명의 PG 직접결제 확정으로 0803 해소
- ~~학부모가 여러 명일 때 승인자가 누구인가~~ → **학부모 최대 1인 확정**(I-12 ▷[0803])
- ~~자리이탈 두 채널 충돌 규칙~~ → 앱 신청 경로 없음(조회 전용)으로 0803 확정되며 소멸

### 컷오버(개발용 서버 → 운영 전환) 체크리스트

서버를 새로 만드는 게 아니라 개발 중이던 서버를 그대로 운영으로 전환하는 방식이라, 아래를 반드시 의식적으로 거칠 것 (§3 대전제 참고):

- [ ] 테스트 더미데이터 전부 삭제
- [ ] 실제 재원생 데이터 이관 (위 블로커 해소 후)
- [ ] **★ 지점(`academy`) 등록 — 그리고 지점에 딸린 마스터를 같이 넣을 것.**
      마이그레이션의 지점별 시드는 `academy`를 JOIN해서 넣는데 **`academy`에 시드가 없다.**
      빈 DB에 전부 적용해도 지점 관련 행은 **0건**이다(2026-08-21 실측: 급식업체 5·교습비 공통 3·교습일수 12는 들어가고, 지점별은 전부 0). 지점을 넣는 시점에 아래를 같이 하지 않으면 **조용히 빠진다**:
    - [ ] 지점별 **급식업체 연결 + 단가** (`/api/v1/admin/meal-vendors/assignment`) — 대구만 8,000원
    - [ ] **목동·분당 재학생 교습비** 660,000 (그 외 지점은 공통 400,000으로 떨어진다)
    - [ ] **1인실 가격** — 분당 660,000+190,000 · 목동 820,000+130,000 · 대구 822,000+128,000
    - [ ] **월별 교습일수**는 공통본이 들어가 있다. 지점별로 다르면 그 지점만 추가
- [ ] **★ 연도가 바뀔 때마다** 교습일수 12행·교습비 가격·성적 입력 양식을 새로 넣을 것.
      마이그레이션에 2026년분만 있어 안 넣으면 그 해 청구·가입이 막힌다(`TEACHING_DAYS_NOT_REGISTERED`·`EXAM_FORM_NOT_FOUND`).
- [ ] 자동 백업 설정 켜기
- [ ] DB 접근권한 최소화 (개발자 전원 → 필요한 인원만)
- [ ] 프로필 이름 `dev` → `prod`로 변경 (애플리케이션 코드/설정 전반 점검)
- [ ] 이 전환이 실제로 언제 일어났는지 §10에 기록

## 5. 아키텍처 / 패키지 구조

3개 클라이언트가 이 서버 하나만 바라본다. 서버는 나누지 않고 **API 경로 prefix로만 구분**한다.

```
/api/v1/app/...      학생·학부모 앱 (dlab-app)
/api/v1/admin/...    관리자 웹 (dlab-admin-web)
/api/v1/webhook/...  외부 시스템 수신 전용 (키오스크 아님 — 아래 참고)

/auth/**             ★ DSA 호환 구획 — 키오스크 전용, prefix 없음
/kiosk/**            ★ DSA 호환 구획 — 키오스크 전용, prefix 없음
```

**★ DSA 호환 구획은 위 prefix 규칙의 예외다.** 키오스크가 경로를 하드코딩하고 있어 `/api/v1`을 붙일 수 없다(§3 대전제). 그리고 이 구획은 **응답 형식도 다르다**(`ApiResponse` 아님 — §7 참고). 두 구획을 섞지 말 것. 계약은 `docs/dsa-compat.md`.

```
com.dlab
├── common/          공통 인증(JWT), 예외처리, 응답 포맷, 로깅
├── domain/          엔티티·서비스·리포지토리 (비즈니스 로직, 도메인별 — 아래는 그대로 유지)
│   ├── user/        학생/학부모/관리자 + 권한(다지점 접근 제어)
│   ├── attendance/  출결 태깅 원장·일자 집계·자리이탈 — **벌점은 penalty, 사유 승인은 approval로 분리**(아래 ★).
│   │                   자리이탈은 키오스크 태깅으로만 발생하고 앱은 조회 전용
│   │                   (0803 확정, 사감 순찰 QR 방식 아님, §4 참고)
│   ├── schedule/    정기일정 등록·승인
│   ├── payment/     **수납+급식 결제 공용** (대성 PG 연동, §2 결정로그) — 대성전산과 무관한 별도 계열사
│   ├── meal/        급식 신청/취소 (실제 결제 처리는 payment 도메인에 위임)
│   ├── grade/       성적 조회 (더프리미엄 API 연동)
│   ├── notice/      공지, 질의응답, 설문
│   ├── firewall/    와이파이 해제 신청 — 승인 자체는 approval에 위임 (§3 3케이스 에스컬레이션)
│   ├── approval/    **공통 승인 라우팅** — 사유신청·정기일정·방화벽이 전부 여기를 탄다 (아래 ★)
│   ├── penalty/     상벌점 + 규칙엔진 — attendance 하위에 두지 말 것 (아래 ★)
│   ├── chat/        교사-학생 1:1 메시지 — SDK 사용 확정, 어느 SDK인지만 대기 (§4)
│   └── notification/ 알림톡/FCM 발송
├── api/             **컨트롤러 전용, 클라이언트별로 분리** (§2 결정로그 — 도메인 폴더 안에 컨트롤러 안 둠)
│   ├── app/         학생·학부모 앱이 호출하는 컨트롤러 (도메인별 하위폴더: attendance, payment, firewall ...)
│   ├── admin/       관리자 웹이 호출하는 컨트롤러 (동일하게 도메인별 하위폴더)
│   ├── kiosk/       ★ DSA 호환 컨트롤러 (`/auth/**`, `/kiosk/**`) — 우리가 키오스크에 응답하는 쪽.
│   │                   prefix·응답형식 모두 예외 (§7). 계약은 docs/dsa-compat.md
│   └── webhook/     키오스크 외 외부 시스템 수신 컨트롤러
└── integration/     **우리가 외부를 호출**하는 코드만. 도메인 로직과 분리
    ├── zyxel/       Nebula 방화벽 API
    ├── premium/     더프리미엄모의고사 API
    ├── pg/          대성 PG — 수납+급식 결제 공용 (대성전산과 무관한 별도 계열사, §3)
    └── (chat SDK)/  SDK 확정 후 벤더명으로 생성 — §4 블로커
```

**★ `integration/kiosk`·`integration/daesung`은 두지 않는다.** 키오스크는 우리가 호출하는 대상이 아니라 **우리를 호출하는 클라이언트**라 `api/kiosk/`가 맞고(§3), 대성전산 API는 우리가 DSA를 **대체**하기로 해서 호출할 일이 없다(§2 결정로그).

**★ `approval`을 `firewall` 안에 넣지 말 것.** 요구사항정의서 F-4.11-5는 `approval_items(approver_type: PARENT|TEACHER|AUTO)` + `approval_requests`로 **사유신청·정기일정·방화벽을 하나의 라우팅 엔진**에 태운다. `firewall` 안에 두면 사유신청에서 같은 로직을 또 짜게 된다.

**★ `penalty`를 `attendance` 안에 넣지 말 것.** 상벌점 규칙엔진은 **출결과 데일리루틴 양쪽에서 트리거**된다(요구사항정의서 I-5). `attendance` 하위에 두면 `daily_routine` → `attendance` 순환 참조가 난다.

위 트리는 확정 도메인만 적은 것이다. 요구사항정의서 4.11 신규확장(학습계획·데일리루틴·상담·신상기록부·연간행사 등)까지 포함한 **전체 도메인 목록과 도메인별 착수 가능 여부는 `docs/domain-map.md`** 참고.

**컨트롤러는 `api/{app,admin,kiosk,webhook}/도메인명/` 아래에, 서비스·엔티티·리포지토리는 계속 `domain/도메인명/` 아래에 둔다.** 클라이언트가 각자 자기 컨트롤러 폴더만 보면 되게 관리 편의를 얻으면서도, `payment`(수납+급식 공용)나 `approval`(사유·일정·방화벽 공용) 같은 여러 클라이언트가 공유하는 비즈니스 로직은 `domain` 한 곳에만 있어서 중복 구현을 막는다. 컨트롤러를 도메인 폴더 안에 함께 두지 않는 것에 주의 — `api/` 밖에 컨트롤러 파일을 만들지 말 것.

**같은 도메인 서비스를 `api/app`·`api/admin`·`api/kiosk` 셋이 함께 쓰는 경우가 실제로 많다.** 예: 출결은 키오스크가 쓰고(태깅), 앱이 조회하고, 관리자 웹이 승인·수정한다. 컨트롤러만 셋으로 나뉘고 `domain/attendance`는 하나다.

채팅은 **SDK 사용으로 확정**됐고 어느 SDK인지만 대기 중이다(§4). 선택되면 `integration/<벤더명>/`을 만들고, 그 전까지 `domain/chat`은 스텁으로만 둔다. SDK를 쓰더라도 **메시지 원본은 우리 DB에 동기화**해야 한다("학원 소유 DB" 원칙, §3).

외부 연동사가 바뀌어도 `domain`은 안 건드리는 게 목표 — `integration` 밖으로 연동 코드가 새어나가지 않도록 리뷰 시 확인.

## 5-1. 화면별 기능 분담 (앱 vs 관리자 웹)

같은 백엔드·같은 도메인 테이블을 쓰지만, **role에 따라 "쓰기 가능한 범위"가 다르다.** 기획서 3장(앱)·4장(DSA)을 대조하면 패턴이 명확하다:

> **학생/학부모의 "행위"(제출·신청·조회)는 앱 `/api/v1/app/**`, 관리자의 "승인·집계·관리"는 DSA 웹 `/api/v1/admin/**`.**

같은 도메인 안에서도 엔드포인트를 역할별로 쪼개야 한다는 뜻 — 예: `attendance` 도메인이라고 컨트롤러 하나로 다 열면 안 되고, 학생용/관리자용 엔드포인트를 role 체크와 함께 분리한다.

| 도메인 | 앱 (`/app/**`) — 학생·학부모 | 관리자 웹 (`/admin/**`) — DSA |
|---|---|---|
| `user` | **회원가입**: 학생(휴대폰인증+내신입력→승인요청, 승인 전 앱 접근 불가), 학부모(휴대폰인증+학생고유ID→즉시가입, 자녀 여러 명은 계정 1개+자녀연결). 마이페이지에 본인 학생고유ID 노출 | 학생 검색·조회, 신규접수·학번부여, **학생 회원가입 승인**, 반 배정(배정 시 담당선생님 자동 연동, §3 참고), **반별 담임(담당선생님) 지정**, 대기자 관리, 직원·강사 계정/권한 관리 |
| `attendance` | 출결 조회(+동적 QR 태깅, 방식 미확정), 사유 제출, 정기일정 등록, **자리이탈 조회만**(0803 확정, 신청 경로 없음) | 출결 현황 조회, 사유 승인/수정, 상벌점 관리·수정, 자리이탈 이력 조회 |
| `schedule` | 정기일정 등록 | 정기일정 승인 여부는 **학부모 앱**에서 처리 (관리자 웹 관여 없음 — 기획서 3.4) |
| `payment` | 결제 수단 등록(카드/가상계좌), 결제 내역 조회 | 수납 통계, 미납자 명단, 결제 취소·환불 처리 |
| `meal` | 급식 신청·취소·변경 | 월별 신청현황 조회, 관리자 취소(3일 제한 없음) — 결제 자체는 `payment` 도메인 공용 |
| `grade` | 성적 조회(**모의고사는 `/api/students/grade` 자동조회** — §2), **내신 직접 입력**(양식 §4 블로커), 가채점 설문 응답 | 성적표 PDF 업로드(컷오버 후 수급 경로 §4), 가채점 설문 생성·배포·결과 수집 |
| `firewall` | 해제 신청(학생), 승인(**학부모** — 1차 승인자) — 담당선생님이 먼저 승인하면 케이스별로 다른 문구 표시(§3 3케이스 참고) | 승인(**담당선생님(사감)** — 타임아웃 10분 경과 시 에스컬레이션, 타임아웃 전에도 승인 가능). 상태 전이는 원자적으로, 케이스 판별은 승인시각 vs 신청시각+타임아웃 비교 (§3 참고) |
| `notice` | 질의응답 작성, 설문 응답, 공지 조회, 특강신청 | 특강 신청명단·출석부 관리, 설명회 신청 관리, **공지 작성**(조회는 전체 공유하되 작성은 역할별 제한: 전체공지=행정선생님만, 반공지=담당선생님만, §2 결정로그) |
| `chat` | (SDK 확정, 최종 SDK 후보 선택 대기 — §4 블로커) | (SDK 확정, 최종 SDK 후보 선택 대기) |
| `notification` | 알림 수신만 (미등원 등 중요 알림은 카카오 알림톡, 일반 알림은 FCM Push) | 개별·단체 문자/알림톡 발송, 자동발송 템플릿 관리, 미납자 알림, **미등원 스케줄러 결과 자동 발송**(§3 참고) |

**관리자 웹 전용(앱에 대응 기능 없음)**: 대기자 관리(4.2), 교무업무 명단조회·출력(4.9), 기초관리·배정관리 등 관리자 설정 전반(4.10). *(수납현황(4.8)은 더 이상 대성전산 API 조회가 아니라 `payment` 도메인 자체 데이터임 — 위 표 참고)*

**키오스크는 이 표에 없는 제3의 클라이언트다.** 출결 태깅(쓰기)·좌석 변경(쓰기)·급식 신청여부 조회·상벌점/수납 조회를 한다. 즉 **같은 도메인을 앱·관리자 웹·키오스크 셋이 함께 쓴다** — 예: 출결은 키오스크가 만들고, 앱이 조회하고, 관리자 웹이 승인·수정한다. 키오스크가 쓰는 엔드포인트 25개는 `docs/dsa-compat.md`.

## 6. 개발 원칙

1. **화면 선개발 금지**: 대응 API/스키마가 확정되기 전에 관리자 웹·앱 화면부터 만들지 않는다.
2. **연동은 핵심 도메인 로직 완성 후**: 외부 API(대성전산, Zyxel, 더프리미엄, PG, 채팅 SDK 확정 시)는 해당 도메인 로직이 안정된 뒤 붙인다. 순서를 앞당기면 나중에 구조가 깨지기 쉽다.
3. **§4 블로커는 임의로 가정하고 넘어가지 않는다.** 확정 전까지는 해당 도메인을 스텁/인터페이스로만 남겨둔다.
4. **API 스펙은 이 레포가 단일 진실 공급원**이다. springdoc-openapi로 `docs/openapi.yaml` 자동 생성 → `dlab-admin-web`(openapi-typescript), `dlab-app`(openapi-generator/Dart)이 각각 생성해서 쓴다. 다른 레포에 스펙을 수동 복붙하지 않는다.

## 7. 컨벤션

- **Flyway 파일명은 타임스탬프로 짓는다** — `V20260804143000__설명.sql` (`VyyyyMMddHHmmss__`).
  순번(`V5`, `V6`)은 두 사람이 동시에 작업하면 **같은 번호를 만들어 충돌**한다. 실제로 한 번 겪을 뻔했다.
  타임스탬프면 서로 뭘 만드는지 묻지 않아도 부딪히지 않는다.
    - **이미 적용된 `V1`~`V4`는 그대로 둔다.** 파일명을 바꾸면 체크섬·버전이 어긋나 다른 사람 환경이 기동하지 않는다. 신규분부터 적용.
    - Flyway는 문자열로 정렬하므로 `V4` 다음에 `V2026...`이 와도 순서는 정상이다.
    - 다만 **적용 순서가 "작성 시각 순"으로 고정**된다 — 순번처럼 중간에 끼워넣을 수 없으니, FK 의존이 있으면 먼저 만든 쪽이 먼저 적용된다는 걸 전제할 것.
- **전 테이블 공통 컬럼 필수** (실행가이드 Phase 0 첫 지시): `id`, `year`, `branch_id`, `created_at`, `updated_at`, **`created_by`**, **`is_deleted`**.
  `created_by`(감사로그)와 `is_deleted`(soft delete)를 빠뜨리기 쉬운데, 실행가이드가 경고하듯 **"뒤늦게 추가하면 과거 데이터의 이력이 소실된다."** 지금은 컬럼 추가지만 운영 데이터가 쌓인 뒤엔 그 기간을 영영 복구할 수 없다. 감사로그는 보안심사 직결 항목이다.
  `year`는 "전년도 복사"(YearlySnapshotService)의 기준이라 마스터성 테이블엔 반드시 있어야 한다. **복사 의존순서는 `department → course_type → class_group → curriculum → penalty_item → tuition`이고, 단순 `INSERT SELECT` 금지**(FK가 깨진다).
- **개인정보 처리 규칙** (실행가이드 3.2): 전화·주소·생년월일이 포함된 응답은 **상위 관리자만** 조회 가능. **엑셀 다운로드는 마스킹 기본 ON**(`010-****-1234`) — 교무업무 명단·학생 관리 Export 전반 공통. 발주처가 개인정보 유출 조사·벌금 사례로 민감도가 높은 상태이므로 임의로 완화하지 말 것.
    - **★ 업로드에서 마스킹된 값은 "변경 없음"이다.** 마스킹은 Export만의 문제가 아니다 — 실무에서 가장 흔한 흐름이 **"내려받아 수정 후 재업로드"**인데, 내려받은 파일의 연락처는 이미 `010-****-1234`다. 그대로 저장하면 **전 학생 연락처가 마스킹 문자열로 덮어써진다.** 그러면 미등원 알림톡이 학부모에게 가지 않는데, 화면에는 원래도 마스킹돼 보이니 **한동안 아무도 눈치채지 못한다.**
    - 판정·마스킹은 **`common/privacy/Masking`** 하나만 쓴다. Export와 Import가 **같은 판정**을 써야 왕복이 성립한다.
    - **기존 데이터 수정이면 그 칸을 건너뛰고**(DB 값 유지), **신규 등록이면 오류**다 — 유지할 원래 값이 없는데 저장하면 연락처가 `010-****-1234`로 박힌다.
    - `Masking.isMasked()`는 **`*`가 하나만 있어도 마스킹으로 본다.** 연속 2개 이상으로 좁히면 **이름 마스킹(`김*수`)이 빠져나가** 이름이 `김*수`로 저장된다. 실제 이름·연락처에 `*`가 들어갈 일은 없으니 넓게 잡는다.
- **★ 목록 조회는 `common/search` 모듈을 쓴다. 조건 조합을 직접 짜지 말 것.**
    - `SearchPredicates` — 값이 없으면 `null`을 돌려주고 QueryDSL이 그 조건을 무시한다. 그래서 `where(a, b, c)`에 **나열만 하면** 들어온 조건만 자동 적용된다. `if`로 감싸거나 `.and()`로 이어 붙이지 말 것 — 앞 조건이 null이면 NPE고, 학생 검색처럼 조건이 12개면 감당이 안 된다.
    - `SearchScope` — **지점·연도 범위. 목록 조회는 예외 없이 이걸 먼저 건다.** `SearchScope.of(principal, year)`로만 만들고, **`academyId`를 요청 파라미터로 받지 말 것** — 클라이언트가 값을 바꿔 보내는 것만으로 다른 지점 데이터가 새어나간다. 전 지점 권한자면 지점 조건이 자동으로 빠진다.
    - `SearchSupport` — 페이징·정렬. `page()`는 마지막 페이지일 때 **count 쿼리를 아예 안 돌린다**. `toOrders()`는 **허용 목록에 없는 정렬 필드를 무시**한다(임의 컬럼 정렬로 인덱스를 못 타는 것 방지).
    - 사용 예:
      ```java
      queryFactory.selectFrom(enrollment)
          .where(SearchPredicates.scope(enrollment.academy.id, enrollment.year, scope),
                 SearchPredicates.contains(enrollment.studentNo, keyword),
                 SearchPredicates.eq(enrollment.grade, grade))
      ```
    - `year`는 전 테이블이 `SMALLINT`라 `Short`다. `academy`는 연관관계라 경로가 `엔티티.academy.id`.
- **★ 새 마이그레이션은 타임스탬프로 만든다.** `V<yyyyMMdd>_<HHmm>__설명.sql` (예: `V20260804_1541__period_day_type.sql`). **기존 `V1`~`V4`는 그대로 둔다.**
    - 순차 번호를 버린 이유: 여러 명이 며칠씩 로컬에 쌓다가 푸시하는 방식이라 "만들기 전 `git pull`"로는 같은 번호를 못 막는다. 그리고 **파일명이 다르면 Git이 충돌로 보지 않아 조용히 머지되고**, 기동할 때서야 `Found more than one migration with version N`으로 터진다. 타임스탬프는 동시에 만들어도 겹치지 않는다.
    - Flyway가 `20260804.1541`로 파싱해 `V4` 뒤에 정렬한다(검증됨).
    - **순서 역전 주의**: 늦게 푸시된 과거 시각 마이그레이션이 이미 적용된 것보다 앞설 수 있다. 개발 환경만 `spring.flyway.out-of-order=true`로 허용한다(운영은 순차 배포라 발생하지 않는다).
- **★ `created_by`를 직접 설정하지 말 것.** `SecurityAuditorAware`가 저장 시점에 자동으로 채운다. 서비스마다 수동으로 넣으면 반드시 빠뜨리고, **감사로그는 나중에 붙여도 그 이전 기간을 복구할 수 없다** — "누가 이 학생 벌점을 지웠나"에 답할 수 없게 된다(보안심사 직결).
    - 인증 주체가 없는 경로(**배치·스케줄러·DSA 호환 구획**)는 **시스템 계정 `0`**으로 기록된다. `null`로 두면 "배치가 했다"와 "그냥 빠뜨렸다"가 구분되지 않는다. `account.id`는 `BIGSERIAL`이라 1부터 시작해 `0`과 겹치지 않는다.
    - `created_by`는 `updatable = false`다. 수정자를 남겨야 하면 `@LastModifiedBy`를 추가해야 하는데 **지금은 스키마에 컬럼이 없다**.
- **★ 상벌점 자동부여는 `PenaltyRuleEngine`을 쓴다. 규칙을 코드에 박지 말 것.**
    - 트리거→점수 매핑(I-5)이 미확정이라 **규칙을 `penalty_rule` 테이블 데이터로** 둔다. 확정되면 행만 채우면 되고, 규칙이 없으면 아무 일도 안 일어난다.
    - **`penalty_rule.active`는 기본 `false`다.** 행을 넣어도 명시적으로 켜기 전엔 안 돈다 — 미검증 규칙이 실수로 도는 걸 막는다.
    - **점수는 항목(`penalty_item.point_value`)을 복사한다.** 부여 시점 값을 `penalty_point.points`에 남겨야 항목 점수를 나중에 바꿔도 **과거 이력이 소급해서 바뀌지 않는다**. 수기 부여도 조정 없이 항목 점수 그대로다.
    - **자동부여는 반드시 멱등해야 한다.** 중복 경로가 셋이다 — 키오스크 재태깅 · 배치 재실행 · 다중 인스턴스 동시 처리. 앞의 둘은 사전 조회로 걸러지지만 **셋째는 DB 유니크 제약(`uq_penalty_point_idempotency`)만이 막는다.** 멱등키는 `학생:일자:규칙` 조합이라 하나라도 빼면 다른 건까지 막힌다.
    - 엔진은 **`REQUIRES_NEW`로 부모 트랜잭션과 분리**한다. 상벌점 부여가 실패해도 출결 기록은 남아야 한다 — 태깅이 롤백되면 학생이 등원한 사실 자체가 사라진다.
    - **`PATROL_SLEEP_2`(2회 연속 졸음) 트리거를 만들지 말 것** — 사감 순찰이 0803에 폐기됐다.
- **★ 엑셀 입출력은 `common/excel`을 쓴다. POI를 직접 다루지 말 것.**
    - `ColumnMapping` — **컬럼 위치가 아니라 헤더명으로 찾는다**(F-4.1-2 "유연 매핑"). 순서가 바뀌거나 열이 끼어들어도 안 깨진다. **별칭 지원**("학번"/"학생번호"), 헤더의 공백·대소문자 무시.
    - `ExcelReader` — 값을 **전부 문자열로** 읽는다. 셀 서식이 파일마다 달라서(학번이 숫자 서식이면 `20260001`, 텍스트면 `2026-0001`) 읽는 쪽에서 타입을 가정하면 그때마다 깨진다. 숫자 셀의 `.0` 꼬리와 날짜 서식은 여기서 흡수한다.
    - `ExcelRow` — 타입 변환. **실패를 예외로 던지지 않고 오류로 쌓는다.** 첫 오류에서 멈추면 사용자가 파일을 고쳐 올릴 때마다 오류를 하나씩만 발견하게 된다. 날짜는 `yyyy-MM-dd`·`yyyy/MM/dd`·`yyyy.MM.dd`·`yyyyMMdd`를 모두 받는다.
    - `ImportPreview` — **오류행이 있어도 정상행은 반영한다**(실행가이드 완료기준). 100건 중 3건이 틀렸다고 전부 되돌리면 실무가 안 돈다. 오류행 수는 **행 번호 기준으로 센다**(한 행에 오류가 여럿일 수 있음).
    - `ExcelExporter` — **스트리밍(`SXSSFWorkbook`)**. 명단 출력(F-4.9)은 재원생 전체가 대상이라 일반 워크북이면 OOM으로 간다. `dispose()`로 임시파일을 지운다.
    - **행 번호는 엑셀 기준 1-based**로 내려준다. 0-based로 바꾸면 사용자가 엑셀에서 그 행을 못 찾는다.
    - **Export한 파일을 다시 Import할 수 있어야 한다** — "내려받아 수정 후 재업로드"가 가장 흔한 흐름이라 왕복을 테스트로 고정해뒀다.
- **★ 휴일 판정은 `common/holiday`를 쓴다. 요일로 직접 계산하지 말 것.**
    - `HolidayCalendar.mealAvailableDates(academyId, month)` — 급식 가능일(주말+공휴일 제외). `operatingDates(..., excludeWeekend)`로 주말 포함 여부를 고른다.
    - **주말 제외는 급식에만 해당한다.** 이 학원은 **토요일에도 운영**한다(시안 `plan-3`이 토요일 학습계획, 교시 마스터에 주말 구성이 따로 있다). 학습계획·출결에서 주말을 빼면 안 된다.
    - **음력 공휴일·대체공휴일·임시공휴일을 코드로 계산하지 않는다.** 규칙이 해마다 바뀌고 임시공휴일은 규칙 자체가 없다 — `holiday` 테이블에 데이터로 넣는다. 공공데이터포털 API를 붙이더라도 **이 테이블에 적재하는 방향**이어야 한다(API 장애 시 급식 신청이 멈추면 안 됨).
    - `holiday.academy_id`가 `null`이면 전 지점 공통, 값이 있으면 그 지점만(개원기념일 등). 조회는 **둘을 합쳐서** 본다.
    - **앱이 자체 판정하지 못하게 서버가 내려준다** (앱 요구사항 A-9). 임시공휴일 추가에 앱 배포가 필요해지면 안 된다.
    - **등록은 수기다** (§4 미확정 참고). `/api/v1/admin/holidays` CRUD. 권한 규칙: **전 지점 공통 휴일은 본사(전 지점 권한자)만** 등록한다 — 지점 관리자가 넣으면 다른 지점 급식까지 막힌다. 지점은 **자기 지점 `ACADEMY` 유형만** 등록 가능하다(법정공휴일을 지점별로 넣으면 같은 날이 지점마다 다르게 처리된다). 삭제는 soft delete — 물리 삭제하면 과거 급식 신청이 어느 규칙으로 계산됐는지 추적이 끊긴다.
- **공통 API 응답 형식**: `/api/v1/**` 컨트롤러는 `ApiResponse<T>`로 감싼다 (`{ success, data, meta, error }` — 목록은 `meta`에 페이징 정보). 도메인 오류는 `throw new BusinessException(ErrorCode.XXX)` → `GlobalExceptionHandler`가 공통 실패 응답으로 변환. 에러코드는 `common/exception/ErrorCode`에 추가 (예: `BRANCH_NOT_FOUND`, `APPROVAL_ALREADY_PROCESSED`).
- **★ DSA 호환 구획(`/auth/**`, `/kiosk/**`)은 위 규칙의 예외다.** 응답이 `{ code, message, data, … }`이고 **성공 판정이 `code == 0`**(HTTP 상태코드 아님)이며, `total_inwon`·`study_tm` 같은 **부가 필드가 최상위에 흩뿌려진다**. `ApiResponse` 래퍼와 `GlobalExceptionHandler`를 이 구획에 적용하면 **키오스크가 응답을 못 읽는다** — 전용 응답 객체와 전용 예외 핸들러를 쓰고, `BusinessException` → DSA code 매핑 테이블을 둔다.
- **★ DSA 호환 응답을 "정리"하지 말 것.** `data`가 `[[{...}]]` 이중 배열로 오는 것, `study_tm`이 `"N시간 M분"` 문자열인 것, `meal_gb`가 요청은 `L`/`D`인데 응답은 `"점심"`/`"저녁"`인 것 — 전부 **의도적으로 그대로 유지**해야 한다. 하나라도 고치면 키오스크 파싱이 깨진다. 전체 목록은 `docs/dsa-compat.md` §2.
- **JWT role 체크 필수**: 학생/학부모/관리자 role 구분 없이 엔드포인트를 열지 않는다.
- **지점 필터링 필수**: 전체 지점 조회 엔드포인트는 상위 관리자 권한 체크 없이 노출하지 않는다.
- **브랜치 전략**: `main`은 배포 전용 브랜치, 절대 여기서 직접 작업하지 않는다. 새 작업(기능 개발, 버그 수정 등)은 항상 `develop`에서 브랜치를 따서 진행하고, `develop`으로 merge한다. `main`으로의 merge는 배포 시점에만, 별도 승인 후 진행.
- 언어: 커밋 메시지·주석·산출물은 한국어 우선.

## 8. 보안 / 비밀정보

- Zyxel, PG사, 카카오 알림톡, 더프리미엄 API 등의 계정정보·API 키·엔드포인트 상세는 이 파일이나 커밋에 절대 넣지 않는다. `.env` 등 별도 비밀관리 방식으로만 관리.
- 레거시(DSA/대성전산) 관련 취약점이나 접근 정보를 알게 되더라도 여기 기록하지 말 것.

## 9. 관련 레포

- `dlab-admin-web`: 관리자 웹 (React)
- `dlab-app`: 학생·학부모 앱 (Flutter)

## 10. 지금까지의 진행 (세션 컨텍스트)

<!-- 매 세션 끝에 여기 한두 줄 추가 — 다음 세션이 어디부터 이어가야 하는지 알 수 있게 -->
- 레포 세팅 완료: `dlab-api`, 브랜치(main=배포/develop=개발), 패키지 구조(domain 기능별 + api 클라이언트별), 로컬 Docker(PostgreSQL+Redis), CLAUDE.md/CLAUDE.local.md/.claude/settings.local.json 정리 완료. 아직 실제 도메인 코드(엔티티 등) 착수 전.
- 오늘 클라이언트와 확인되어 다수 블로커 해소: 방화벽 담당교사/타임아웃/사전지정, 자리이탈 식별, 상담양식, 채팅(SDK 확정), 결제 PG사(대성 PG 확정). 남은 블로커: 채팅 SDK 최종선택, 결제 법적요건, 내신 성적양식, 명단구분항목, 알림템플릿 문구, 기존데이터 이관.
- **키오스크 연동 전제가 뒤집혔다** — "Webhook 수신"이 아니라 "DSA 호환 API 제공"(우리가 응답 서버). 엔드포인트 25개 계약 확보 → 요구사항정의서 오픈이슈 **D-2(최우선) 해소**, 출결 Phase1 착수 가능. §3·§5·§7 전반 정정 완료.
- **대성전산 레거시 스키마 27개 수령**(원생 마스터 포함). 학생이 "사람 + 등록 건" 2단 구조이고 학번·RFID가 등록 건에 붙는다는 것 확인 → §3 반영. **미확보 4종(출결·좌석/구역·지점·사유신청)**은 추가 제공 협의 난항 중이나, API 계약에서 역산 설계하기로 함(§4).
- **가이드 문서 3종 작성**: `docs/dsa-compat.md`(키오스크 API 계약·함정), `docs/schema-guide.md`(테이블 설계 근거·마이그레이션 순서), `docs/domain-map.md`(도메인↔F번호↔오픈이슈, 착수 가능 여부). ※ `docs/domain-map.md` 등은 이번 CLAUDE.md 정정 이전에 쓴 것이라 일부 서술(승인 모델 미확정 등)이 낡음 — 다음 세션에 동기화 필요.
- **다음 작업**: Flyway V1(지점·학생 2단·학부모·좌석/구역·교시 마스터) → V2(출결 원장+일자집계·사유신청) → DSA 호환 계층 골격(`/auth/token` + 응답 껍데기 + code 매핑) → `setAttendStd`.
- **Phase 0 도메인 1차 구현 완료** (`e321e1e`, 브랜치 `feature/phase0-approval-notification-attendance`) — firewall 승인 3케이스·notification·미등원 배치. 앱 기동·Flyway 적용·테스트 15건 통과 확인. 상세는 §2. **단 위 키오스크/학생 2단 구조 정정 이전 설계라 V1~V4는 재작성 대상이다.**
- **요구사항정의서·실행가이드 시트 2종 전수 대조** (링크는 `md/개발문서.md`). 시트에만 있고 CLAUDE.md에 없던 것을 §7에 반영: **전 테이블 공통컬럼(`year`/`created_by`/`is_deleted`)**, 전년도 복사 의존순서, 개인정보 마스킹 규칙. 시트가 구버전인 항목(Supabase 전제, 출결 5종, 승인 타임아웃 미확약, QR 순찰)은 이 파일이 우선.
- **RBAC 축 정리 완료 (0803)**: 요구사항정의서의 **5단계**(`SUPER_ADMIN`/`BRANCH_ADMIN`/`TEACHER`/`STAFF`/`READONLY`)를 정본으로 채택하고 `role` 테이블에 seed로 넣었다. 담당/행정 구분을 별도 컬럼으로 이중 관리하지 않는다 — 소속은 `teacher`/`employee` 테이블이, 권한은 `role`이 담당한다.
- **신상기록부(F-4.11-9)는 뼈대가 0728에 확정됐다**(4종 양식·학년별 분기·설문형+PDF·관리자 승인 후 앱 열람·Phase1 입력/Phase3 열람 — `docs/entity-design.md` S 참고). 남은 블로커는 **4종 양식-학년별 매핑(I-17)** 하나뿐이고, `form_schema_json`으로 받게 해뒀으므로 **테이블 설계는 지금 진행 가능하다.**
- **회원가입 연계 의혹은 해소됐다 — 신상기록부와 무관하다.** 회의록의 *"회원가입 못하게. 휴대폰인증하면 그걸로 비교해서…"*는 신상기록부 제출 조건이 아니라 **학생 회원가입 중복방지 로직**이었다: 이미 승인요청을 넣은 학생이 재가입을 시도하면 **휴대폰번호로 대조해 차단**한다(PENDING 상태의 기존 요청까지 검사해야 한다). 학부모는 기존대로 휴대폰인증+학생고유ID로 즉시가입 — §3 가입 흐름에 변경 없음.
- **2026-08-03: Flyway·도메인 전면 재작성 완료.** 학생 2단 구조, 출결 7종 원장+일자집계, `approval`/`penalty` 분리, 전 테이블 공통컬럼(`year`/`created_by`/`is_deleted`)을 반영. 이름도 설계서 기준(`academy`/`employee`/`class_master`/`account`)으로 통일. 테스트 14건 통과, 앱 기동·마이그레이션 적용 확인.
- **2026-08-03: 시트 2종 재확인 후 반영.** 결제 구조 확정(D-9 등 해소), 좌석이탈 앱 쓰기 삭제, 학부모 최대 1인, 화면번호 체계 변경, SMS 폴백·가족채팅방 폐기, 신규 블로커 E-7/I-23~I-27. 머리말의 **정본 우선순위도 시트 최우선으로 재정의**했다.
- **다음 작업**: ①인증(JWT + Redis Refresh Token/블랙리스트) + Security ②DSA 호환 계층 골격(`/auth/token` + 응답 껍데기 + code 매핑) → `setAttendStd` ③`api/app`·`api/admin` 컨트롤러.- **2026-08-05: Phase 4 앞부분 완료 (PR #4, `develop` 머지됨).** 로그인 보안(A-1)과 학부모 가입(A-2 일부).
    - **로그인 보안** — 임시 비밀번호 최초 로그인 시 변경 강제(변경 전 다른 API 전면 차단, `PasswordChangeRequiredInterceptor`) · 실패 5회 계정 잠금(카운터는 Redis, 잠금은 `account.locked_at`, 자동 해제 없음) · 관리자 잠금 해제·임시 비밀번호 재발급. **잠금은 `REQUIRES_NEW`로 분리**했다 — 같은 트랜잭션에서 걸면 로그인 실패 예외가 잠금까지 롤백시켜 영원히 안 잠긴다.
    - **로그아웃이 실제로 되게 고쳤다** — `TokenBlacklist`에 쓰기만 하고 읽는 곳이 없어 로그아웃해도 Access Token이 만료까지 살아 있었다. 필터에서 대조한다.
    - **학부모 가입** — 휴대폰 인증(2단계, 1회용 토큰) · 가입 · 자녀 연결 · 자녀 목록. **인증번호 발송은 목업**(`LoggingSmsSender`)이라 로그로만 찍힌다.
    - **`student_guardian_link.is_approver` 도입** — "학부모 최대 1인"(I-12)은 **승인 주체**에 대한 제약이지 연락처 개수가 아니다. 연결 자체를 학생당 1건으로 묶었더니 DSA `getParentHpList`(3.25, 부·모·기타를 목록으로 내림)가 하나만 내리게 돼서 부분 유니크(`WHERE is_approver`)로 바꿨다. **연락처 다건은 계약상 필요하다.**
    - 테스트 290건 통과.
- **다음 작업 (앱 설정·초기화)**: ①**`app-config`** — `GET /api/v1/app/app-config`(최소 지원 버전·점검 모드) + 관리자 CRUD. 앱 부팅 첫 호출이라 앱 팀 작업이 여기서 시작된다(A-21 · F-4.12-3, 테이블명은 시트가 `app_configs`로 지정) ②**약관·동의** — 동의 시점의 **문구 버전까지** 이력으로 남긴다(플래그만으론 "그때 무엇에 동의했나"에 답할 수 없다) ③**알림 수신 설정**(유형별 on/off) + **FCM 토큰 등록** — 발송은 E-7·E-5 대기지만 **수신 동의·토큰 저장은 지금 가능**하다.
  → 그 다음은 **학생 가입**(내신 양식 확정 후). **홈 배너는 여기 넣지 않는다** — 시트 F-4.12-3의 데이터 항목이 `app_configs`와 `notices`로 나뉘어 있어 배너는 `notice` 도메인이다.
- **2026-08-30: 모의고사 성적 자료 전수 검토.** 클라이언트·대성전산 제공 8개 파일(`md/모의고사` 4 · `md/제출성적` 4)을 대조했다. **가장 큰 것은 "모의고사 자동조회"가 성립하지 않는다는 것** — 성적조회 API가 답안만 주고, 성적 값은 담임용 엑셀(194열)에 있다. 그 밖에 성적표에 등급컷·정답·전국석차·출결이 이미 인쇄되어 있다는 것, 통합 PDF를 우리가 분할할 수 있다는 것, 장학 `MOCK_EXAM_ABSENCE`가 E-2 없이 판정된다는 것, 입학예약 `acid`에 대전·대구가 빠져 있다는 것, `scholarship_cancel_rule`이 0826 기준표를 표현하지 못한다는 것을 확인했다. 상세는 §2, 미확정은 §4.
- **2026-08-31: 대성전산 회신 — 성적 자동조회가 되살아났다.** `GET /api/students/grade`가 추가돼 표준점수·백분위·등급·예상석차·총점군이 전부 온다. **하루 전 결론(자동조회 불가 → 엑셀 업로드)이 뒤집혔다.** 함께 확인된 것: 출결·담임·반을 대성전산이 DSA에서 직접 수집하며 **컷오버로 끊긴다**(상대도 흐름을 모른다), 성적표 PDF도 대성전산이 디멤버에 직접 올려 **우리 시스템에는 안 온다**, 인증코드는 불필요. 상세는 §2, 남은 것은 §4.
- **다음 작업**: ①~~`scholarship_cancel_rule` 재설계~~ **완료**(`V20260830_1000`, PR #51) ②~~장학 관리자 API~~ **완료**(PR #52) ③**성적조회 API 연동** — 개발서버(9월말) 대기지만 **HMAC 아웃바운드 클라이언트는 지금 만들 수 있다**(우리 서버 최초의 아웃바운드 호출이다). ④**출결·담임·반 전달 규격 제안** — 질의로 안 닫히는 항목이라 우리가 먼저 안을 내야 한다.

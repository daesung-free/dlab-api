# CLAUDE.md — dlab-backend

이 파일은 Claude Code가 세션마다 참고하는 프로젝트 컨텍스트다. 작업 시작 전 반드시 이 안의 원칙·블로커·컨벤션을 기준으로 판단할 것.

**정본(source of truth)은 `D.Lab 통합기획서`(구글 문서/Word)다.** 이 파일은 그 요약이며, 기획서가 업데이트되면 이 파일보다 기획서가 우선한다. 세부 사항이나 최신 확정 여부가 헷갈리면 기획서를 다시 확인한다.

## 1. 프로젝트 한 줄

D.Lab(대성학원)이 대성전산 소유의 레거시 DSA(웹 관리프로그램)에서 벗어나, **학원이 직접 소유하는 통합 플랫폼**(관리 웹 + 재원생 앱)을 신규 구축한다. 도메인 본질: 앱·키오스크·관리 웹이 실시간으로 같은 상태(출결/벌점/승인)를 공유하는 승인 워크플로우 — 학생 행위(출결·사유제출·신청) → 학부모 승인 → 자동 처리(방화벽 해제, 벌점 산정 등)의 3자 상태머신이다.

## 2. 현재 코드베이스 상태

<!-- 실제 진행 상황에 맞춰 이 섹션을 계속 갱신할 것. 아래는 착수 시점 템플릿. -->

- 스택: Spring Boot (Java) · **AWS RDS for PostgreSQL** · JPA (§2 결정로그 — 원래 Supabase였으나 전환)
- 진입점/패키지 구조: §5 참고
- 현재까지 구현: (착수 전 — 여기에 마일스톤마다 기록)
- 앱 기동 가능 여부: (DataSource 설정 여부 기록)

📌 **결정 로그** *(기획서에 명시 안 됐지만 Claude Code 세션 중 추가/합의된 사항을 여기 기록)*
- 예: springdoc-openapi 도입 → API 스펙 자동화 목적
- 기획서 3.6의 "사감 QR 순찰" 방식은 채택 안 함 — 대신 **자리이탈** 여부만 추적하는 방향으로 변경. 트리거는 **키오스크 카드 태깅뿐**이고, 앱은 그 결과를 가져와서 보여주기만 한다(쓰기 없음, 조회 전용). **자리이탈도 카드 태깅 기반으로 확정** — Webhook으로 백엔드 수신, 기존 `integration/kiosk` 채널 재사용 가능성 높음. 단, 태깅 리더/구역 식별 방식은 §4 미확정.
- **방화벽 해제 승인 흐름 변경** (기획서 3.7 원안: 학부모 단독 승인 → 변경): 학생 신청 시 **학부모+선생님(관리자)에게 동시 발송**, 둘 중 먼저 승인한 쪽으로 처리. 나중에 확인하는 쪽 화면에는 "○○ 승인으로 자동 승인되었습니다" 표시.
- **수납·급식 결제 구조 변경** (기획서 6.1 원 확정사항 "디멤버·수납 개발 주체=대성전산" 뒤집힘): 대성전산 수납 프로그램 사용 안 함. **수납·급식 결제 전부 D.Lab 자체 PG사**로 통합 처리 — 급식업체 명의 PG사 아님.
- (신규 결정 생길 때마다 이 형식으로 한 줄씩 추가)
- **컨트롤러 계층을 도메인 폴더에서 분리해 `api/{app,admin,webhook}/도메인명/`으로 재구성.** 이유: "관리하기 편함"(클라이언트별로 폴더 나눠서 각자 자기 것만 보게) + 비즈니스 로직 중복 방지(payment 등 여러 클라이언트가 공유하는 로직은 domain에 하나만 유지). 서비스/엔티티/리포지토리는 그대로 domain 아래.
- **Flyway 도입.** 요구사항이 계속 변경되는 프로젝트 특성상(수납 구조, 자리이탈, 방화벽 승인 흐름 등 이미 여러 번 변경됨) 스키마 변경을 버전관리할 필요 → `src/main/resources/db/migration/V*.sql`. 앱 기동 시 자동 적용.
- **DB를 Supabase → AWS RDS for PostgreSQL로 전환.** 이유: "학원 소유" 원칙은 계약/명의 문제일 뿐 Supabase여야만 하는 게 아니었음(§3 참고) + 백엔드(EC2/ECS 오토스케일링)와 같은 VPC에 둬서 네트워크 구조 단순화 + 5,000명 이상으로 확장 시 인스턴스 스펙업 → 필요하면 Aurora PostgreSQL Serverless v2로 전환 경로 확보. 둘 다 순정 PostgreSQL이라 애플리케이션 코드 변경 없이 연결정보만 교체하면 됨.
- **채팅 방식은 아직 미확정** (Sendbird 등 SDK vs 자체구현, §4 블로커). 자체구현으로 갈 경우의 설계 방향만 미리 검토해둔 상태: 앱↔서버 WebSocket(STOMP), 서버가 여러 인스턴스라 인스턴스 간 메시지 중계는 Redis Pub/Sub(ElastiCache) 필요, 오프라인 사용자는 기존 `notification` 도메인(FCM/카카오)이 담당. 이건 확정된 설계가 아니라 "이 방향으로 가면 이렇게 짠다"는 참고 메모일 뿐 — 착수 전 반드시 SDK vs 자체구현 결정부터.
- **Redis 도입 (채팅과 무관, `user`/인증 도메인 용도로 확정).** Refresh Token 저장, 로그아웃 시 Access Token 블랙리스트 처리, 로그인 실패 rate limiting 등에 사용. 로컬은 `docker-compose.yml`에 Redis 컨테이너 추가. (채팅 자체구현이 나중에 확정되면 그때는 Pub/Sub 용도로 같은 Redis를 재사용하되 별도 인프라 추가는 아님.)
- **방화벽 해제 승인 로직을 "단순 레이스"에서 "타임아웃+에스컬레이션 모델"로 정교화** (#40 / I-20, I-12): 학부모 우선 승인 → 타임아웃 지나면 담임 에스컬레이션 승인 → 이때 학부모에겐 "시간 지나 담임이 승인" 알림. 반면 타임아웃 전에 학생이 재촉해서 담임이 먼저 승인하면 **다른 문구**("시간 남았지만 담임이 먼저 승인")로 안내해야 함 — 상세는 §3 참고. 스키마에 `timeout`·`escalation`·`default_approver` 필드는 Phase0에 미리 마련해두고, 정확한 타임아웃 값과 사전지정 UI 여부는 §4 블로커로 대기.
- **방화벽 에스컬레이션 승인자 = 담당선생님(사감)으로 확정, 타임아웃 = 10분으로 확정.** (§4 블로커 해소)
- **담당선생님(사감)-학생 매핑은 "반배정" 시 자동 연동.** 반(class) 엔티티에 담임(담당선생님) 필드를 두고, "반 관리" 화면에서 반별로 담임을 지정 → 학생을 그 반에 배정하면 담당선생님이 자동으로 따라옴. **학생별 승인자 사전지정 UI는 불필요** — #40 "사전지정 UI 여부" 블로커 해소, 대신 반 관리 화면에 담임 필드만 추가.
- **선생님 role은 두 종류: 담당선생님(사감) / 행정선생님.** 공지(notice) 작성 권한이 이 구분에 따라 갈림 — **조회는 관리자 전체 공유**, **작성은 역할별 제한**: 전체공지는 행정선생님만, 반공지는 담당선생님만 작성 가능. `notice` 도메인에 작성 권한 체크 로직 필요.
- **회원가입 흐름 확정 — 학생과 학부모가 완전히 다르다.**
    - 학생: 휴대폰 인증 + 내신·모의고사 성적 입력 → **관리자 승인 요청** → 승인 전엔 앱 접근 완전 차단(중간 상태 없음, PENDING→ACTIVE 이분법) → 승인되면 가입 완료
    - 학부모: 휴대폰 인증 + **학생 고유ID** 입력 → 승인 절차 없이 **즉시 자동 가입**
    - 학생 고유ID는 마이페이지 화면에 노출 (문자 발송·별도 인쇄 등 다른 전달 수단 없음). 이 ID를 아는 사람이면 학부모 본인 확인 없이 연결 가능하다는 리스크가 있으나, 이건 클라이언트가 감수하기로 한 부분 — 추가 검증 로직 만들지 말 것.
    - 모의고사 성적은 **더프리미엄 API로 자동 조회** (학생 입력 불필요) — §4 "성적 입력 양식" 블로커 중 모의고사 부분은 해소, **내신**은 여전히 학생 직접 입력 필요하고 양식 미정.
    - 기획서의 "성인" 분류는 **n수생**을 의미하는 것으로 확정 (고2/고3/n수생 3분류).
- **학부모 다자녀 계정 구조: 계정 1개 + 자녀 여러 명 연결(로그인 1번 + 앱 내 자녀전환 UI)로 확정.** 자녀 수만큼 완전히 분리된 별도 계정을 만드는 방식은 기각 — 사용성(로그인/로그아웃 반복 불편)과 알림 발송 로직(어느 계정 기준으로 보낼지 모호해짐) 둘 다 더 복잡해짐.
- **미등원 알림 로직 확정.** 등원시간(지점 공통, 학생별 아님) 정각에 **스케줄러(배치)** 실행 → 그 시각까지 키오스크 Webhook으로 출결 태깅 기록이 없는 학생만 필터링 → 학부모에게 **카카오 알림톡** 발송(FCM Push 아님, 중요도 때문에 격상). **사전에 결석 사유를 제출한 학생은 제외** — 무단결석인 경우만 대상. 이건 지금까지의 로직(키오스크 이벤트 발생 시 반응)과 반대로 **"이벤트가 없었음"을 감지**해야 하는 유일한 유형이라 별도 스케줄러 구현 필요(§3 참고).
- **알림 템플릿 공통 원칙: 모든 템플릿에 학생명 변수 필수.** 다자녀 학부모가 "이거 누구 얘기지" 헷갈리지 않도록, 어떤 알림이든 `{학생명}` 변수를 반드시 포함해야 함.
- **채팅은 SDK 사용으로 확정, 자체구현 폐기.** 여러 SDK 후보(Sendbird, Stream Chat 등)를 **우리 팀이 직접 프로토타입으로 제작**해서 클라이언트에 전달 → 클라이언트가 최종 선택. 프로토타입 제작 자체도 개발원칙(§6-2, 연동은 핵심 도메인 완성 후)을 따라 `user` 등 핵심 도메인이 안정된 뒤 착수.
- **결제 PG사 = "대성 PG"로 확정.** 대성전산과 무관한 별도 계열사로 확인됨 — "학원 소유 DB" 원칙과 충돌 없음. 계약서/문서는 클라이언트가 전달 예정, 세부 법적요건(전자금융거래법 등)은 여전히 §4 블로커.
- **급식 PG 관련 오해 해소**: 요구사항정의서(E-3, "급식업체가 이미 쓰는 PG 그대로 사용, 신규 선정 아님")와 위 "대성 PG" 결정이 충돌하는 것처럼 보였으나, **급식업체가 원래 쓰던 PG 자체가 대성 PG였음**이 확인됨 — 신규 벤더 선정이 아니라 기존 것을 수납까지 확장해서 쓰는 것. 충돌 아님.
- **자리이탈 트리거 경로 재정정 — 키오스크 카드 태깅 + 앱 둘 다 가능** (요구사항정의서 F-4.11-8 "패드 소지 시 앱에서 신청 가능"으로 확인됨). 이전에 "앱은 조회만"로 정정했던 것을 다시 뒤집는다 — 이번엔 요구사항정의서 근거가 있어 확정으로 취급. `attendance`의 자리이탈은 `integration/kiosk`(Webhook)와 `/api/v1/app/**` 양쪽에서 쓰기가 들어올 수 있음. 두 경로가 겹칠 때(예: 앱에서 자리이탈 신청 후 키오스크 재태깅이 들어오는 경우) 처리 규칙은 §4 블로커.
- **자리이탈 리더/구역 식별 이슈 해소.** 별도 구역 식별 없이 기존 키오스크 Webhook을 그대로 받아 처리하면 되는 것으로 확인됨 (§4 블로커였던 항목 해소).
- **상담(입시/학습 상담, CS상담과는 별개) — 목업 존재, 이를 기준으로 진행.** 상담 종류가 여러 개이며 실제 목업이 있어 그대로 활용 가능 (§4 블로커 사실상 해소, 다만 도메인 패키지 구조는 목업 검토 후 별도 확정). CS 상담(민원성)은 기존대로 구글시트 유지, 변경 없음.

## 3. ★ 헷갈리기 쉬운 대전제

- **"DB는 학원 소유"의 의미는 계약/명의 문제이지, 학원 직원이 직접 DB 콘솔을 들여다본다는 뜻이 아니다.** 핵심은 "인프라 계정이 누구 명의인지, 대성전산이 접근권한/종속력을 안 갖는지"이지, 실사용 편의성(대시보드 UI 등) 문제가 아니다. 그래서 이 원칙 자체가 "반드시 Supabase여야 한다"를 의미하지 않는다 — 학원(또는 개발사가 학원을 위해 운영하는) 명의 계정 하의 RDS 등으로도 이 원칙은 동일하게 만족된다. Supabase 채택은 별도의 실용적 선택(관리형이라 운영부담 적음, 소규모에 적합한 비용)이었을 뿐, 원칙상 고정된 요구사항은 아니다.
- 대성전산 API는 "조회"만 하는 대상이지, 이 시스템이 거기 종속되는 게 아니다. 반대로 착각해서 대성전산 스키마를 그대로 복제하지 말 것.
- **키오스크는 대성전산을 거치지 않는다.** 키오스크 → 이 서버로 Webhook 직접 수신이 확정 사항이다. "기존처럼 대성전산 경유"로 설계하면 안 됨.
- **키오스크는 하드웨어 입력 창구 중 하나일 뿐, 로직·데이터는 전부 백엔드/DB(RDS)에 있다.** 앱·관리자 웹은 원칙적으로 키오스크가 만든 데이터를 조회하지만, **자리이탈처럼 앱도 같은 데이터에 쓰기(트리거)가 필요한 경우가 실제로 있다**(요구사항정의서 F-4.11-8, §2 결정로그) — 그럴 땐 두 입력 경로가 같은 도메인 레코드를 갱신하게 되므로 충돌 규칙을 반드시 같이 설계할 것. "키오스크가 하는 건 앱에서도 볼 수 있어야 한다"는 기본값은 유지하되, 앱이 조회만 하는지 쓰기도 하는지는 기능마다 요구사항정의서로 확인.
- **학번은 매년 초기화**된다. 학번을 학생 PK나 외부 연동 키로 쓰면 다음 해에 깨진다. 내부 고유 ID를 별도로 둘 것.
- **수납·급식 결제는 전부 D.Lab 자체 PG사를 통해 이 시스템이 직접 소유·처리한다.** (원 기획서 "수납=대성전산 소유" 확정사항은 뒤집힘 — §2 결정로그 참고) 대성전산은 더 이상 수납의 진실의 원천이 아니다. 급식 결제도 급식업체 PG사가 아니라 동일한 D.Lab PG사를 쓴다 — 결제 관련 코드/스키마를 수납·급식 두 곳에 따로 만들지 말고 공용 결제 도메인으로 설계할 것.
- **방화벽 해제 승인은 단순 "먼저 승인한 쪽이 이김" 레이스가 아니라, 타임아웃 기반 에스컬레이션 모델이다** (#40 / I-20, I-12 참고). 신청 시 학부모+**담당선생님(사감)**(관리자 웹)에게 동시에 알림이 가지만, 실제로는 시점에 따라 3가지 케이스로 갈린다:
    1. 타임아웃(**10분으로 확정**) **전에 학부모가 승인** → 정상 승인, 양쪽에 승인 완료 알림
    2. 타임아웃이 **지나도록 학부모 무응답** → 담당선생님이 승인(에스컬레이션) → 학부모 쪽엔 **"승인 시간이 지나 담임이 승인했습니다"**
    3. 타임아웃이 **안 지났는데** 학생이 재촉해서 담당선생님이 먼저 승인 → 학부모 쪽엔 **"승인 시간이 남았지만 담임이 먼저 승인 처리했습니다"** — 2번과 다른 문구, 절대 같은 메시지로 통일하지 말 것 (학부모가 "왜 시간 남았는데 담임이 승인했지" 혼란 겪음)
       그리고 여전히 **동시성 이슈는 남아있다** — 학부모 승인과 담당선생님 승인이 정확히 같은 순간 들어올 수 있으므로, 실제 상태 전이(승인 확정)는 원자적으로 처리하고, 위 1/2/3 중 어떤 케이스인지는 **승인 시각을 신청시각+타임아웃과 비교**해서 판별한다. 단순히 두 엔드포인트를 나눠 만들고 끝내면 안 됨.
       **담당선생님(사감)이 누구인지는 "반배정"에서 자동으로 나온다** — 반 엔티티에 담임 필드를 두고, 학생을 그 반에 배정하면 담당선생님이 자동 결정됨(§2 결정로그). 그래서 "학생별로 승인자를 따로 지정하는 화면"은 만들지 않는다.
- **회원가입은 학생과 학부모가 완전히 다른 흐름이다.** 학생은 **승인제**(휴대폰인증+내신입력 → 관리자 승인 전엔 앱 접근 자체가 완전히 막힘, 중간 상태 없음), 학부모는 **비승인제**(휴대폰인증+학생 고유ID만 맞으면 즉시 가입). 이 둘을 같은 흐름으로 착각해서 설계하지 말 것. 학생 고유ID는 마이페이지에 상시 노출되는 값이고, 학부모-자녀 관계 자체는 검증하지 않는다(그 ID를 아는 사람이면 누구나 연결 가능) — 이건 클라이언트가 인지하고 감수한 부분이니 추가 검증 로직을 임의로 만들지 말 것.
- **학부모 계정은 자녀 수만큼 나누지 않는다** — 계정 1개 + 여러 자녀를 연결하는 구조(로그인 1번 + 앱 내 자녀전환 UI)로 확정(§2 결정로그). 완전히 분리된 별도 계정으로 설계하지 말 것.
- **모든 학생 관련 알림 템플릿에는 학생명 변수가 필수**다. 다자녀 학부모가 "이거 누구 얘기지" 헷갈리지 않게 하기 위함 — 템플릿 하나라도 이 변수 빠뜨리면 안 됨.
- **미등원 알림은 지금까지의 로직과 반대 방향이다.** 대부분의 로직(출결, 자리이탈, 방화벽 등)은 "키오스크 이벤트가 발생하면 반응"하는 구조인데, 미등원 알림은 **"이벤트가 없었음"을 감지**해야 하는 유일한 유형이다 — 등원시간(지점 공통) 정각에 스케줄러(배치)가 돌아서, 그 시각까지 출결 태깅 기록이 없는 학생만 걸러 카카오 알림톡을 보낸다. 사전에 결석 사유를 제출한 학생은 제외(무단결석만 대상). Webhook 리스너만으로는 절대 구현 못 하는 부분이니, 별도 스케줄러 컴포넌트가 필요하다는 걸 잊지 말 것.
- **채팅은 SDK 사용으로 확정됐다 (자체구현 폐기, §2 결정로그).** 다만 **어떤 SDK**를 쓸지는 아직 미정 — 후보 여러 개를 우리 팀이 직접 프로토타입으로 만들어 클라이언트에 전달하고, 클라이언트가 최종 선택한다. SDK를 쓰더라도 메시지 원본은 우리 DB에 동기화해야 한다는 "학원 소유 DB" 원칙은 그대로 유지 — Webhook 동기화 설계 필요(§4 블로커). 프로토타입 제작은 §6 개발원칙(연동은 핵심 도메인 완성 후)에 따라 순서를 지킬 것.
- **다지점 구조**: 지점 간 데이터는 독립적이되, 상위 관리자는 전 지점을 조회할 수 있어야 한다. 엔티티에 branch_id류 컬럼을 빠뜨리기 쉬우니 신규 테이블 만들 때마다 체크할 것.
- **인프라는 서버 하나를 개발용→운영용으로 그대로 전환하는 방식이다(별도 dev/prod 서버 분리 아님).** 그래서 이름(`dev`)이 주는 "막 다뤄도 됨"이라는 착각이 실제 사고로 이어질 위험이 있다 — 개발 중인 이 DB에는 실제 학생·학부모 개인정보를 절대 넣지 않고 테스트 더미데이터만 사용할 것. 실사용 전환(컷오버)은 이름만 바꾸는 게 아니라 §4 "컷오버 체크리스트"를 거쳐 의식적으로 수행한다.

## 4. ⚠️ 착수 전 블로커 / 미확정 사항

아래가 확정되기 전에는 관련 기능의 화면·API를 확정 설계하지 말 것 (원칙: §6 참고). 작업 중 이 항목을 마주치면 임시 가정으로 넘어가지 말고 먼저 확인한다.

- **채팅 SDK 최종 선택 미정**: SDK 사용 자체는 확정(§2 결정로그, §3 대전제) — Sendbird 등 후보 여러 개를 프로토타입으로 만들어 클라이언트에 전달, 최종 선택 대기. SDK로 갈 경우 Webhook 동기화 설계 필요. `domain/chat`/`integration/sendbird` 실제 구현 착수는 이 선택 이후.
- **결제(수납+급식) 관련 법적 요건 미검토**: PG사는 "대성 PG"(대성전산과 무관한 별도 계열사)로 확정됐으나(§2 결정로그), 전자금융거래법·PG 등록·보증보험 등 세부 법적요건 확인은 여전히 미완료. 계약서/문서는 클라이언트가 전달 예정. 결제 도메인 스키마 확정 전 선행.
- **내신 성적 입력 양식 미정**: 모의고사는 더프리미엄 API로 자동조회 해결(§2 결정로그), **내신**은 여전히 학생이 직접 입력해야 하는데 정확한 양식(학년별 항목, 등급/원점수 여부, 학기 단위 등) 미정. `grade` 도메인의 내신 관련 스키마·API 확정 전 선행.
- **명단 구분 항목**: 기획서 4.9에 "현재 구분 항목 불명확, 운영팀과 재정의 필요"로 명시됨 — 해당 화면 착수 게이트. (노션 "D.Lab 확인 필요 목록"에 상세 정리됨, 우선순위 높음 — 기획서에서 직접 "!!!" 강조)
- **알림 템플릿 문구 설계**: 기획서 3.13 "템플릿 설계를 위해 추후 논의 필요" — 채널 구분(카카오 알림톡 vs FCM Push), 방화벽 3케이스별 문구, 학생명 변수 반영 등 요구사항은 늘어난 상태(§2/§3 참고)이나 정확한 문구 자체는 여전히 미정. 카카오 알림톡은 사전심사가 필요해 조기 확정이 유리 — 알림 발송 로직 확정 전 선행.
- **자리이탈 - 두 채널 간 처리 규칙 미정**: 트리거 경로 자체는 확정됨(키오스크 카드 태깅 + 앱 신청(패드 소지 시), 요구사항정의서 F-4.11-8, §2 결정로그). 단, 두 경로가 겹칠 때 규칙이 아직 없음 — 예: 앱에서 자리이탈로 표시한 상태에서 키오스크 재태깅이 들어오면 어느 쪽을 우선하는지, "복귀" 처리는 무엇으로 판단하는지(재태깅만 복귀인지, 앱에서도 복귀 가능한지). `attendance` 자리이탈 API/스키마 확정 전 선행.
- **기존 재원생 데이터 이관용 자료 미확보**: 대성전산에 2026-07-22 DB 스키마 제공 요청서 발송함(회원관리/디멤버, 수납현황 — 참고용 명시). 아직 회신 없고 전체를 다 받기 어려울 가능성 있음. **단, 이건 신규 스키마 설계 자체를 막는 블로커는 아님** — 이 프로젝트는 레거시 스키마 재현이 아니라 기획서 기반 신규 구축이라, `domain/user`·`domain/payment` 설계·구현은 이 회신과 무관하게 지금 진행 가능. 실제로 이게 필요한 시점은 **컷오버 직전 기존 재원생 데이터 이관** 때뿐(+ 관리자 웹 수납 리포트 포맷을 기존과 비슷하게 맞추고 싶을 때 참고용). 회신 오면 놓친 비즈니스룰 있는지 교차검증 용도로만 활용할 것.

### 해소된 블로커 (기록용, §2 결정로그에 근거 있음)

- ~~방화벽 담당 교사 매핑~~ → 담당선생님(사감), 반배정 시 자동 연동으로 해소
- ~~방화벽 승인 타임아웃 값~~ → 10분으로 확정
- ~~승인류 기능 사전지정 UI 여부~~ → 반별 담임 지정 화면으로 대체, 별도 UI 불필요
- ~~상담 양식~~ → 목업 존재, 그대로 활용
- ~~급식 PG 확인~~ → 급식업체 기존 PG = 대성 PG, 신규 선정 아님으로 확인, 충돌 아니었음

### 컷오버(개발용 서버 → 운영 전환) 체크리스트

서버를 새로 만드는 게 아니라 개발 중이던 서버를 그대로 운영으로 전환하는 방식이라, 아래를 반드시 의식적으로 거칠 것 (§3 대전제 참고):

- [ ] 테스트 더미데이터 전부 삭제
- [ ] 실제 재원생 데이터 이관 (위 블로커 해소 후)
- [ ] 자동 백업 설정 켜기
- [ ] DB 접근권한 최소화 (개발자 전원 → 필요한 인원만)
- [ ] 프로필 이름 `dev` → `prod`로 변경 (애플리케이션 코드/설정 전반 점검)
- [ ] 이 전환이 실제로 언제 일어났는지 §10에 기록

## 5. 아키텍처 / 패키지 구조

3개 클라이언트가 이 서버 하나만 바라본다. 서버는 나누지 않고 **API 경로 prefix로만 구분**한다.

```
/api/v1/app/...      학생·학부모 앱 (dlab-app)
/api/v1/admin/...    관리자 웹 (dlab-admin-web)
/api/v1/webhook/...  키오스크·외부 시스템 수신 전용
```

```
com.dlab
├── common/          공통 인증(JWT), 예외처리, 응답 포맷, 로깅
├── domain/          엔티티·서비스·리포지토리 (비즈니스 로직, 도메인별 — 아래는 그대로 유지)
│   ├── user/        학생/학부모/관리자 + 권한(다지점 접근 제어)
│   ├── attendance/  출결·벌점·자리이탈 (키오스크 연동, 사유 제출/승인) — 자리이탈은 키오스크 태깅 + 앱 신청(패드 소지 시) 양쪽에서 트리거 가능 (요구사항정의서 F-4.11-8, 사감 순찰 QR 방식은 아님, §4 참고)
│   ├── schedule/    정기일정 등록·승인
│   ├── payment/     **수납+급식 결제 공용** (D.Lab 자체 PG사 연동, §2 결정로그) — 대성전산 아님
│   ├── meal/        급식 신청/취소 (실제 결제 처리는 payment 도메인에 위임)
│   ├── grade/       성적 조회 (더프리미엄 API 연동)
│   ├── notice/      공지, 질의응답, 설문
│   ├── firewall/    와이파이 해제 신청 — 학부모·선생님 동시 승인 (먼저 승인한 쪽 확정, 동시성 처리 필요)
│   ├── chat/        (자체구현으로 확정될 경우) 교사-학생 1:1 메시지 — §4 블로커, 아직 착수 금지
│   └── notification/ 알림톡/FCM 발송
├── api/             **컨트롤러 전용, 클라이언트별로 분리** (§2 결정로그 — 도메인 폴더 안에 컨트롤러 안 둠)
│   ├── app/         학생·학부모 앱이 호출하는 컨트롤러 (도메인별 하위폴더: attendance, payment, firewall ...)
│   ├── admin/       관리자 웹이 호출하는 컨트롤러 (동일하게 도메인별 하위폴더)
│   └── webhook/     키오스크 등 외부 시스템 수신 컨트롤러 (kiosk 등)
└── integration/     외부 연동은 반드시 이 패키지 안에서만 (도메인 로직과 분리)
    ├── kiosk/       키오스크 Webhook 수신
    ├── daesung/     대성전산 API — **수납 조회 아님**, 남은 용도(학생 데이터 등) 재확인 필요
    ├── zyxel/       Nebula 방화벽 API
    ├── premium/     더프리미엄모의고사 API
    ├── pg/          D.Lab 자체 PG사 — 수납+급식 결제 공용 (급식업체 PG 아님)
    └── sendbird/    (SDK로 확정될 경우) 채팅 SDK — §4 블로커, 아직 착수 금지
```

**컨트롤러는 `api/{app,admin,webhook}/도메인명/` 아래에, 서비스·엔티티·리포지토리는 계속 `domain/도메인명/` 아래에 둔다.** 클라이언트(앱·관리자 웹)가 각자 자기 컨트롤러 폴더만 보면 되게 관리 편의를 얻으면서도, `payment`(수납+급식 공용)나 `firewall`(동시승인 레이스) 같은 여러 클라이언트가 공유하는 비즈니스 로직은 `domain` 한 곳에만 있어서 중복 구현을 막는다. 컨트롤러를 도메인 폴더 안에 함께 두지 않는 것에 주의 — `api/` 밖에 컨트롤러 파일을 만들지 말 것.

채팅은 `domain/chat`과 `integration/sendbird` 둘 다 후보 상태로만 잡아두고, §4 블로커(SDK vs 자체구현)가 풀리기 전엔 둘 다 실제 구현에 들어가지 않는다.

외부 연동사가 바뀌어도 `domain`은 안 건드리는 게 목표 — `integration` 밖으로 연동 코드가 새어나가지 않도록 리뷰 시 확인.

## 5-1. 화면별 기능 분담 (앱 vs 관리자 웹)

같은 백엔드·같은 도메인 테이블을 쓰지만, **role에 따라 "쓰기 가능한 범위"가 다르다.** 기획서 3장(앱)·4장(DSA)을 대조하면 패턴이 명확하다:

> **학생/학부모의 "행위"(제출·신청·조회)는 앱 `/api/v1/app/**`, 관리자의 "승인·집계·관리"는 DSA 웹 `/api/v1/admin/**`.**

같은 도메인 안에서도 엔드포인트를 역할별로 쪼개야 한다는 뜻 — 예: `attendance` 도메인이라고 컨트롤러 하나로 다 열면 안 되고, 학생용/관리자용 엔드포인트를 role 체크와 함께 분리한다.

| 도메인 | 앱 (`/app/**`) — 학생·학부모 | 관리자 웹 (`/admin/**`) — DSA |
|---|---|---|
| `user` | **회원가입**: 학생(휴대폰인증+내신입력→승인요청, 승인 전 앱 접근 불가), 학부모(휴대폰인증+학생고유ID→즉시가입, 자녀 여러 명은 계정 1개+자녀연결). 마이페이지에 본인 학생고유ID 노출 | 학생 검색·조회, 신규접수·학번부여, **학생 회원가입 승인**, 반 배정(배정 시 담당선생님 자동 연동, §3 참고), **반별 담임(담당선생님) 지정**, 대기자 관리, 직원·강사 계정/권한 관리 |
| `attendance` | 출결 조회, 사유 제출, 정기일정 등록, **자리이탈 신청(패드 소지 시, 요구사항정의서 F-4.11-8) + 조회** | 출결 현황 조회, 사유 승인/수정, 상벌점 관리·수정, 자리이탈 이력 조회 |
| `schedule` | 정기일정 등록 | 정기일정 승인 여부는 **학부모 앱**에서 처리 (관리자 웹 관여 없음 — 기획서 3.4) |
| `payment` | 결제 수단 등록(카드/가상계좌), 결제 내역 조회 | 수납 통계, 미납자 명단, 결제 취소·환불 처리 |
| `meal` | 급식 신청·취소·변경 | 월별 신청현황 조회, 관리자 취소(3일 제한 없음) — 결제 자체는 `payment` 도메인 공용 |
| `grade` | 성적 조회(**모의고사는 더프리미엄 API로 자동조회**), **내신 직접 입력**(양식 §4 블로커), 가채점 설문 응답 | 성적 PDF 업로드, 가채점 설문 생성·배포·결과 수집 |
| `firewall` | 해제 신청(학생), 승인(**학부모**) — 담당선생님이 먼저 승인하면 케이스별로 다른 문구 표시(§3 3케이스 참고) | 승인(**담당선생님(사감)**, 타임아웃 10분 확정) — 학부모보다 먼저 승인 가능, 두 경로 중 먼저 처리된 쪽이 확정 (§3 참고) |
| `notice` | 질의응답 작성, 설문 응답, 공지 조회, 특강신청 | 특강 신청명단·출석부 관리, 설명회 신청 관리, **공지 작성**(조회는 전체 공유하되 작성은 역할별 제한: 전체공지=행정선생님만, 반공지=담당선생님만, §2 결정로그) |
| `chat` | (SDK 확정, 최종 SDK 후보 선택 대기 — §4 블로커) | (SDK 확정, 최종 SDK 후보 선택 대기) |
| `notification` | 알림 수신만 (미등원 등 중요 알림은 카카오 알림톡, 일반 알림은 FCM Push) | 개별·단체 문자/알림톡 발송, 자동발송 템플릿 관리, 미납자 알림, **미등원 스케줄러 결과 자동 발송**(§3 참고) |

**관리자 웹 전용(앱에 대응 기능 없음)**: 대기자 관리(4.2), 교무업무 명단조회·출력(4.9), 기초관리·배정관리 등 관리자 설정 전반(4.10). *(수납현황(4.8)은 더 이상 대성전산 API 조회가 아니라 `payment` 도메인 자체 데이터임 — 위 표 참고)*

## 6. 개발 원칙

1. **화면 선개발 금지**: 대응 API/스키마가 확정되기 전에 관리자 웹·앱 화면부터 만들지 않는다.
2. **연동은 핵심 도메인 로직 완성 후**: 외부 API(대성전산, Zyxel, 더프리미엄, PG, 채팅 SDK 확정 시)는 해당 도메인 로직이 안정된 뒤 붙인다. 순서를 앞당기면 나중에 구조가 깨지기 쉽다.
3. **§4 블로커는 임의로 가정하고 넘어가지 않는다.** 확정 전까지는 해당 도메인을 스텁/인터페이스로만 남겨둔다.
4. **API 스펙은 이 레포가 단일 진실 공급원**이다. springdoc-openapi로 `docs/openapi.yaml` 자동 생성 → `dlab-admin-web`(openapi-typescript), `dlab-app`(openapi-generator/Dart)이 각각 생성해서 쓴다. 다른 레포에 스펙을 수동 복붙하지 않는다.

## 7. 컨벤션

- **공통 API 응답 형식**: 모든 컨트롤러는 `ApiResponse<T>`로 감싼다 (`{ success, data, error }`). 도메인 오류는 `throw new BusinessException(ErrorCode.XXX)` → `GlobalExceptionHandler`가 공통 실패 응답으로 변환. 에러코드는 `common/exception/ErrorCode`에 추가 (예: `BRANCH_NOT_FOUND`, `APPROVAL_ALREADY_PROCESSED`).
- **JWT role 체크 필수**: 학생/학부모/관리자 role 구분 없이 엔드포인트를 열지 않는다.
- **지점 필터링 필수**: 전체 지점 조회 엔드포인트는 상위 관리자 권한 체크 없이 노출하지 않는다.
- **브랜치 전략**: `main`은 배포 전용 브랜치, 절대 여기서 직접 작업하지 않는다. 새 작업(기능 개발, 버그 수정 등)은 항상 `develop`에서 브랜치를 따서 진행하고, `develop`으로 merge한다. `main`으로의 merge는 배포 시점에만, 별도 승인 후 진행.
- 언어: 커밋 메시지·주석·산출물은 한국어 우선.

## 8. 보안 / 비밀정보

- Zyxel, PG사, 카카오 알림톡, 더프리미엄 API 등의 계정정보·API 키·엔드포인트 상세는 이 파일이나 커밋에 절대 넣지 않는다. `.env` 등 별도 비밀관리 방식으로만 관리.
- 레거시(DSA/대성전산) 관련 취약점이나 접근 정보를 알게 되더라도 여기 기록하지 말 것.

## 9. 관련 레포

- `dlab-admin-web`: 관리자 웹 (React)
- `dlab-app`: 학생·학부모 앱 (Flutter)

## 10. 지금까지의 진행 (세션 컨텍스트)

<!-- 매 세션 끝에 여기 한두 줄 추가 — 다음 세션이 어디부터 이어가야 하는지 알 수 있게 -->
- 레포 세팅 완료: `dlab-api`, 브랜치(main=배포/develop=개발), 패키지 구조(domain 기능별 + api 클라이언트별), 로컬 Docker(PostgreSQL+Redis), CLAUDE.md/CLAUDE.local.md/.claude/settings.local.json 정리 완료. 아직 실제 도메인 코드(엔티티 등) 착수 전.
- 오늘 클라이언트와 확인되어 다수 블로커 해소: 방화벽 담당교사/타임아웃/사전지정, 자리이탈 식별, 상담양식, 채팅(SDK 확정), 결제 PG사(대성 PG 확정). 남은 블로커: 채팅 SDK 최종선택, 결제 법적요건, 내신 성적양식, 명단구분항목, 알림템플릿 문구, 기존데이터 이관.
- 다음: 로컬 PostgreSQL/Redis 붙여서 앱 기동 확인 → Flyway V1 마이그레이션 → `domain/user` 엔티티 설계(회원가입 승인 흐름, 다자녀 연결 구조, 반-담임 매핑 반영) 착수 예정.
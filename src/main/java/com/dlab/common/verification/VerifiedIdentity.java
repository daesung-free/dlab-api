package com.dlab.common.verification;

/**
 * 본인인증 결과.
 *
 * <p><b>휴대폰 인증만 한다 — 확인되는 것은 "이 번호의 주인이 맞다"뿐이다.</b>
 * 실명·생년월일은 알 수 없다.
 *
 * <p><b>PASS(본인확인기관)는 검토 후 채택하지 않았다</b>(2026-08-05). 실명·CI를 얻지만
 * 계약·건당 비용·앱 SDK가 필요하고, 결정적으로 <b>PASS를 붙여도 "진짜 그 학생의 학부모인가"는
 * 검증되지 않는다</b> — 그건 학생 고유ID를 아는 사람이면 연결 가능한 구조이고 클라이언트가
 * 감수한 부분이다(CLAUDE.md §3). 즉 실익이 실명 확보와 CI 기반 동일인 추적뿐인데,
 * 학생은 관리자 승인에서 대조하고 재등록 동일인 추적은 이미 {@code student} 2단 구조가 푼다.
 * CI는 민감정보라 저장하면 관리 부담만 늘어난다.
 *
 * <p>필드가 하나뿐이라 {@code String}으로 되돌리고 싶어질 수 있는데, 그러면 인증 수단이
 * 바뀔 때(결제 법적요건에서 본인확인이 요구되는 등) 인증을 쓰는 서비스의 시그니처가
 * 한꺼번에 바뀐다. 이음매로 남겨둔다.
 *
 * @param phone 인증된 전화번호
 */
public record VerifiedIdentity(String phone) {
}

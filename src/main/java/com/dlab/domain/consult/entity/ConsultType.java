package com.dlab.domain.consult.entity;

/** 상담 유형 5종 (화면 확정값). */
public enum ConsultType {
    /** 정기 상담. */
    REGULAR,
    /** 성적 상담. */
    SCORE,
    /** 생활·태도. */
    LIFE,
    /** 진학 상담. */
    ADMISSION,
    /** 학부모 상담. */
    PARENT
}

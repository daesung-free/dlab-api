-- ==========================================================================
-- 기숙사 제거
--
-- 요구사항정의서 F-4.10-3이 "배정 관리(기숙사·사물함·독서실·좌석)"이고 데이터 항목에도
-- "dorm/locker 배정 테이블"이 있어 만들었으나, 클라이언트 확인 결과 **기숙사는 없다**.
-- 배정 대상은 독서실(좌석)과 사물함 둘뿐이다 — 시트 쪽이 구버전이다(2026-08-04 확인).
--
-- 앞선 마이그레이션(V20260804170000)에서 만든 걸 여기서 지운다.
-- ★ 이미 적용된 파일을 고치지 않는다 — 체크섬이 깨지면 다른 사람 환경이 기동하지 않는다.
--
-- 운영 데이터가 없는 시점(같은 날 생성)이라 물리 삭제해도 잃을 이력이 없다.
-- ==========================================================================

DROP TABLE IF EXISTS dorm_assignment;
DROP TABLE IF EXISTS dorm_room;

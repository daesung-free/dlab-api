-- V20260925_1000: 감사 로그 업무영역 '계정'을 한국어로 통일
--
-- 다른 도메인은 전부 한국어(상벌점·성적·청구·학생 등록…)인데 직원 계정만 영문
-- 'Account' 였다. 화면은 업무영역을 목록에서 골라 거르는데 거기엔 한국어만 뜨므로
-- ★ 계정 변경 이력은 검색해도 항상 0건이었다.
--
-- 이미 쌓인 행도 함께 고친다 — 코드만 바꾸면 그 시점 앞뒤가 다른 이름으로 갈려
-- 한쪽은 영영 안 보인다.

UPDATE audit_log SET entity_type = '직원 계정' WHERE entity_type = 'Account';
